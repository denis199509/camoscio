===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-08-17 (traccia vuota del 24/07 cancellata dal database
su richiesta di Denis; punto 86 - livelli badge cime e contatore "xN" -
costruito, pushato E VERIFICATO da Denis coi suoi dati veri: "tutto
perfettamente funzionante" - vedi STATO ATTUALE sotto)
===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo,
    vedi 04-Da-Fare.md del vault).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — l'ex leggimi. Il racconto disteso di ogni lavoro fatto,
    con le misure. Ci si va quando serve il dettaglio di qualcosa di gia' fatto.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
solo se c'e' un lavoro nuovo da raccontare; cose_da_fare.txt quando un punto si chiude
o ne arriva uno nuovo.



>>> STATO ATTUALE (16/08/2026, notte): le quattro decisioni ferme sono
    state chieste (sessione aperta con /remote-control) e Denis ha
    risposto a tutte. Nella stessa sessione (con un riavvio imprevisto
    del PC nel mezzo - vedi [[07-Trappole-Tecniche]] del vault) sono
    arrivate anche tre richieste nuove, tutte chiuse. Dettaglio completo
    in [[04-Da-Fare]] del vault.

    Le quattro decisioni:
      1. Punto 42a (raggio zona) — DECISO: 20 km. Codice non scritto,
         resta legato ai "livelli" del punto 2 (ancora aperto).
      2. Punto 63b (nome) — FATTO: "Esperto locale". public/index.html,
         campo local-expert-area. I LIVELLI restano in sospeso, Denis
         ci deve ancora pensare.
      3. Dominio email proprio — "non ora": rimandato per sua scelta,
         non richiederlo finche' non ci torna lui.
      4. Colore grafico Passo & Fatica — FATTO: "Tuo Passo Rilevato"
         blu, "Standard CAI Alpino" arancione.
    Committate e pushate (camoscio `483c3ef`, vault `11fb54f`), verificate
    dal vivo e su Render.

    Tre lavori nuovi della stessa sessione, tutti committati, pushati e
    verificati dal vivo nel browser:

    A. Badge personali a Nicolo97 ("Fuori sentiero, dentro il football")
       e Scheldon ("Il Passeggero del Rottweiler") — stesso meccanismo
       gia' in uso per gli altri sei. camoscio `d508c34` / `172d0c9`.

    B. Tolto il campo Telefono dai contatti di emergenza — non serve a
       niente, chiesto da Denis. Era in DUE form separati (wizard di
       registrazione, pagina Sicurezza & Mesh): tolto da entrambi.
       Schema reso opzionale, non cancellato (l'array si riscrive per
       intero ad ogni salvataggio: cancellarlo avrebbe perso i numeri
       gia' salvati dei contatti esistenti). Corretto anche un
       "undefined" latente nel messaggio a schermo di scadenza del Dead
       Man's Switch. camoscio `09dad29`.

    C. Bug trovato da Denis: il "Passo & Fatica" mostrava 350/500 (il
       default dello schema) spacciato per "il tuo passo rilevato" a
       chiunque non avesse ancora nessuna escursione tracciata — visto
       su Scheldon, appena registrato. Ora il campo resta assente finche'
       non c'e' un'osservazione vera (schermo mostra "—", non un numero
       finto); i 4 account demo del seed non sono toccati. 7 utenti
       reali gia' registrati sistemati con una migrazione dedicata
       (Scheldon, Nicolo97, Camilla, Bob84, DaniWoll, Manu, Matteo — gli
       ultimi tre avevano gia' un'escursione ma senza un tempo
       dichiarato, quindi erano affetti anche loro). camoscio `b5c1305`.

    Sessione successiva (17/08/2026):

    D. Cancellata dal database una traccia vuota (0 km, 0 dislivello, 0
       punti GPS) del 24/07/2026 sull'account reale di Denis, trovata per
       _id esatto dopo conferma - nessuna delle due tracce vere del suo
       account toccata.

    E. Punto 86, i "livelli" del badge di una cima (Frequentatore -> ... ->
       Esperto della Cima ogni 5 salite, soglie sul totale gia' calcolato
       al punto 42b) + contatore "×N" in alto a destra sulla scheda dalla
       seconda salita. Nessun dato nuovo su MongoDB, solo lettura diversa
       a schermo (public/js/badges.js, schedaBadge - un solo punto di
       modifica, riusato da pagina Badge e profilo). Solo per le cime, non
       i rifugi (deciso con Denis). camoscio `8cbda25`. Prima verificato
       solo in simulazione, POI Denis ha importato le tracce .gpx vere per
       il Corno Grande sul suo account e confermato: "tutto perfettamente
       funzionante". Punto chiuso per intero.

    >>> RIPARTIRE DA: nessun lavoro tecnico pronto. Restano aperti solo i
    "livelli" del punto 63b - quelli della zona "Esperto locale", DIVERSI
    da quelli gia' fatti del punto 86 (Denis deve pensarci, non
    richiederlo) - e il dominio email (rimandato da lui).

>>> COSE FUORI SCOPE, NON RIPROPORLE finche' non torna Denis sull'argomento:
    - Tracciamento GPS a pagina chiusa (impossibile via browser, non difficile
      - serve un'app vera; rimandato da Denis stesso l'08/08).
    - Galleria di foto della squadra e altre funzioni social (rimandate
      insieme al tracciamento, "quando penseremo a un'app vera e propria").
    - Raggio della "zona di competenza" (punto 42a) e il suo nuovo nome
      (punto 63b): Denis ci deve ancora pensare.

>>> TRE COSE DA SAPERE PRIMA DI TOCCARE public/index.html o un componente
    condiviso tra piu' pagine (trovate il 07/08 e l'08/08/2026, non ovvie a
    prima vista):
      - create-hike-modal e complete-group-modal vivono FUORI da
        .page-container (vicino a generic-modal), non dentro una
        .page-section: un modale raggiungibile da piu' di una pagina deve
        stare li' con loro, altrimenti resta invisibile aprendolo dalla
        sezione "sbagliata".
      - un modulo che si collega SOLO da dentro initApp() (dopo
        await refreshState()) rischia la finestra "pulsante visibile ma
        senza listener" (gia' successo due volte: navigazione 26/07,
        profilo 06/08) - se un pulsante sembra rotto senza errori in
        console, controllare prima QUANDO si e' agganciato il suo
        ascoltatore, non se il codice del gestore e' giusto.
      - TUTTE le .page-section restano nel DOM insieme (navigateTo aggiunge/
        toglie solo la classe "active", non smonta nulla): un componente
        JS pensato per vivere dentro piu' di una pagina (es. chatpanel.js
        punto 55, le schede di buildHikeCard punto 69) non deve MAI usare id
        fissi per i propri elementi interni - due copie con lo stesso id
        (una visibile, una nascosta) fanno scrivere document.getElementById
        nel posto sbagliato, senza nessun errore in console. Sempre
        box.querySelector(...) scoped al contenitore, o - per una card che
        si apre/chiude da sola - il fratello nel DOM (nextElementSibling),
        mai un id.

>>> LEZIONE OPERATIVA: dopo qualunque interruzione di sessione non
    volontaria, controllare SEMPRE `git log origin/master..HEAD` in
    entrambi i repository (camoscio e vault) prima di fidarsi dell'ultima
    riga scritta qui - una volta le note sono rimaste indietro rispetto a
    un push gia' avvenuto per davvero. Il 09/08/2026 e' successo ANCHE il
    contrario, dopo lo spegnimento a meta' del PC: `git status` diceva 2
    commit locali non ancora pushati, ma era un ref locale non aggiornato -
    il push era gia' riuscito per davvero. Un secondo ref rotto
    (refs/heads/master-Denis, creato o scritto proprio nell'istante dello
    spegnimento) bloccava anche `git fetch` col repository intatto. Dettaglio
    completo, come si riconosce e come si e' risolto in
    ../camoscio memoria/07-Trappole-Tecniche.md.
