===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-09-05 (TRENTATREESIMA sessione, CHIUSA QUI su richiesta
di Denis ("ci fermiamo qui") — Tasto Indietro implementato/verificato/
committato/pushato; poi RITOCCHI VISIVI Fascia A uno alla volta, A1-A8
fatti e verificati dal vivo, fermati prima di A9 (prossimo passo, vedi
sotto); nel mezzo due segnalazioni/domande di Denis, entrambe solo da
segnare, nessun codice (completamento da tracciamento GPS + gate
recensioni, stessa causa; 2FA/login Google). SI RIPARTE DA A9.

--- TASTO INDIETRO / HISTORY API: FATTO, COMMITTATO, PUSHATO ---
Il tasto Indietro (telefono e PC) buttava fuori dal sito invece di tornare
alla pagina/sezione precedente. Implementato per intero (nucleo in
public/js/app.js: navigateTo() unico punto che pusha, gestore popstate
unico, registro modali apriModaleStorico/chiudiModaleStorico/
scollegaModaleStorico) e verificato DAL VIVO con l'estensione Chrome
(history.state e l'esito delle promise letti a ogni passo, non solo
schermate). Tre difetti veri trovati e corretti nella sessione (due solo
provando dal vivo): deep-link che non risincronizzava la sezione al
ritorno da un Indietro; modale orfano su una pagina con id che mostrava il
profilo sbagliato; Confirm risolto come Annulla. Denis ha detto "committa
e pusha entrambi": commit `7793426` (Tasto Indietro) + `617edde` (32a
sessione, B-5/M-4/B-2) pushati su origin/master, verificato con un secondo
`git push` di rete ("Everything up-to-date"). Render dovrebbe aver
ridistribuito. Dettaglio completo (i tre bug uno per uno): cose_da_fare.txt
sotto "TASTO INDIETRO / HISTORY API". Piano: C:\Users\lenovo\.claude\plans\
camoscio-tasto-indietro-history-api.md (aggiornato con lo stato).

--- RITOCCHI VISIVI FASCIA A: A1-A8 FATTI. SI RIPARTE DA A9 ---
Denis ha chiesto di proseguire con questo lavoro preparato (rapporto:
C:\Users\lenovo\.claude\plans\camoscio-audit-visivo-29a.md), UN PUNTO ALLA
VOLTA, fermandosi dopo ognuno per il suo ok esplicito prima del successivo
("mi raccomando un punto alla volta, proseguiamo dopo il mio ok"). Alla
fine ha detto "ci fermiamo qui" prima di A9 - RIPARTIRE PROPONENDO A9,
non chiedere di nuovo se continuare i ritocchi: e' gia' la richiesta in
sospeso di Denis. Solo public/css/styles.css toccato, tranne A5 (anche
social.js/squadpage.js/storico.js, per togliere lo stile inline) e A2/A8
(anche index.html). Ogni punto verificato dal vivo (misure/colori
renderizzati via javascript_tool, non solo a occhio) e con CSS a graffe
bilanciate. Rifusi gli 11 auto-save della sessione con `git reset --soft
7793426` e committato in un commit solo (vedi in fondo per l'hash) - NON
PUSHATO, in attesa del via di Denis (non ancora chiesto in questa sessione).

  FATTI:
   A1 - 19 regole CSS scritte solo per <i> (morte: Lucide sostituisce con
        <svg>) estese a ", <sel> svg"; + nuova regola generica
        ".btn i, .btn svg { width:16px; height:16px; flex-shrink:0 }".
   A2 - 6 classi utility usate nel codice (~55 punti) ma mai dichiarate:
        .text-center, .text-secondary, .py-2/3/4, .col-span-2. Aggiunte in
        "Utility Testi", scala py-2/3/4 = 8/16/24px (ritmo gia' in uso).
   A3 - .btn-success:hover era verde-acqua neon (#0D9F6E) invece di
        scurirsi in tinta come gli altri bottoni -> #3D6337 (stesso ~20%
        di scurimento di --accent-orange). Contrasto bianco 3,39->6,91:1.
   A4 - .hike-card-title era #FFF (bianco freddo, residuo neon) ->
        var(--color-text-primary) (#F3EFE6, bianco caldo del tema).
   A5 - 20 bottoni ("Mappa"/"Chat"/preferito/Accetta/Rifiuta/cestino/
        "Pubblica nel feed") con style="padding:2px..." inline, ~23px di
        tocco -> nuova classe .btn-xs (padding 6px 10px), tolto lo style
        da tutti e 20 in social.js/squadpage.js/storico.js. Altezza
        misurata dal vivo: ~30px.
   A6 - .notification-dropdown (320px ancorate a right:0 sul wrapper) e
        .toast-container (max-width 360px) sporgevano sotto ~380px di
        schermo -> entrambe con min(Npx, calc(100vw - Mpx)). NON
        verificato a schermo stretto per davvero (vedi limite sotto).
   A7 - .hikes-header-row (titolo + "Crea Nuova Escursione") senza
        flex-wrap -> flex-wrap:wrap; gap:12px.
   A8 - .form-row-ratings (Recensioni, 3 colonne) e .checkbox-group (16
        interessi registrazione, forzata a 3 colonne con uno style
        inline ora tolto) sfondavano sotto i 900px -> una colonna sola in
        quella @media; + .rating-group{min-width:0} fuori dalla media
        (senza, una colonna di griglia non si restringe mai da sola).

  LIMITE DI VERIFICA (A6/A7/A8): lo strumento di ridimensionamento
  finestra in questa sessione non ha mai cambiato la larghezza effettiva
  della pagina (resta ~1536px anche dopo il resize - limite dell'ambiente
  browser di questa sessione, non del codice). Confermato solo che il CSS
  e' valido e non rompe nulla a schermo largo; il comportamento sotto i
  380-900px si basa su @media/min()/calc() standard gia' usati con
  successo altrove in questo stesso foglio di stile, MAI visto renderizzare
  per davvero stretto. Da confermare al telefono vero o con un resize che
  funzioni, se si vuole la controprova visiva completa.

  DA FARE (in ordine dal piano, si riparte da qui):
   A9 - .safety-contact-row (lista contatti Dead Man's Switch) senza
        NESSUNA riga di CSS - l'unico componente completamente nudo del
        sito, nella schermata di sicurezza. PROSSIMO PASSO.
   A10 - titoli di scheda (h4/h5) a misura del testo corrente, nessuna
        gerarchia visiva.
   (da B4) - .settings-placeholder opacity .65 -> .85 (leggibilita').
  Blocco Fascia B/C del piano non ancora nemmeno proposti (empty state
  unificato, descrizione escursione tagliata, contrasti sotto soglia,
  palette fredda residua, responsive login/registrazione, colori JS
  duplicati) - restano nel piano, si decide quando/se affrontarli.

--- NUOVO, SOLO DA SEGNARE (nessun codice) ---
Denis, usando il sito dal vivo: ha completato un'escursione col
tracciamento GPS dell'app; il compagno (Marco Alpinista) e' rimasto SENZA
tasto di conferma, perche' il completamento da tracciamento dal vivo
(POST /api/hikes/:id/complete, chiamato da tracking.js completeLinkedHike)
non chiede mai "chi era con te" - solo POST /:id/complete-group (bottone
"Completa escursione") lo fa e aggiorna hike.participants. Seconda parte
della richiesta: dare a ogni partecipante confermato la possibilita' di
allegare la PROPRIA traccia GPS invece di ereditare per forza quella di
chi ha registrato (oggi complete-group applica un solo .gpx condiviso,
identico per tutti - il tempo di CAMMINO dipende dal passo personale
anche quando il tempo TOTALE e' uguale per tutti). Denis: "questo e' solo
da segnare. lo faremo in un'altra sessione". Dettaglio tecnico completo
(righe di codice vere, cosa fa oggi ognuna delle due rotte, le due
decisioni di UI ancora aperte) in cose_da_fare.txt, sezione
"COMPLETAMENTO DA TRACCIAMENTO DAL VIVO NON CHIEDE 'CHI ERA CON TE'".

CONFERMATO più tardi: Denis non riesce a recensire Marco ne' viceversa,
stessa causa. routes/reviews.js richiede hike.participants + un
Completion per ENTRAMBI; il bottone "Completa escursione" (social.js:975)
e' dietro if(isCreatorMe) - lo vede solo il creatore. Un partecipante che
non e' creatore E non traccia dal vivo lui stesso non ha oggi NESSUNA via
verso un proprio Completion. Il fix gia' previsto sopra risolverebbe
anche questo. Nessun codice.

--- NUOVO, SOLO DA SEGNARE (nessun codice) ---
Denis ha chiesto se si puo' introdurre "il Google authenticator" e/o
"accedi con Google" per l'iscrizione. Sono due cose diverse: 2FA TOTP
(standard aperto, non una dipendenza Google - segnato DA FARE, vedi sotto)
e login OAuth "Accedi con Google" (dipendenza vera, terza eccezione al
vincolo open source, Google di solito pretende una Privacy Policy
pubblicata che qui non esiste ancora - IN SOSPESO su decisione di Denis,
si riprende solo dopo la nota legale). Dettaglio (direzione del 2FA, cosa
resta da decidere) in cose_da_fare.txt, sezione "2FA (GOOGLE
AUTHENTICATOR) - DA FARE. LOGIN CON GOOGLE - IN SOSPESO".

>>> APERTI (follow-up sicurezza della 32a, invariati):
  - CONFORMITA' GDPR (professionista): invariata. Serve un consulente
    privacy/avvocato GDPR (non un DPO formale) per una vera Informativa
    Privacy + il testo di consenso prima di accettare un invito.
  - VERIFICA DAL VIVO di Denis su tutto l'invito squadra direzionale:
    invariata (invito a entrare in una squadra; invito squadra a
    un'escursione; uscita da una squadra e successione del creatore).

>>> LAVORO FUTURO PREPARATO, ancora agli atti (NIENTE codice scritto):
    FILONE B: collezione "I 2000 dell'Appennino" - bloccato, LA LISTA
    UFFICIALE DELLE VETTE LA FORNISCE DENIS. Piano:
      C:\Users\lenovo\.claude\plans\camoscio-2000-appennino-collezione.md

>>> AZIONI MANUALI DI DENIS, aperte (invariate dalla 30a):
  1. ACCOUNT_SCRUB_SECRET su Render (stringa a scelta, DIVERSA da
     SAFETY_CRON_SECRET) + un job giornaliero su cron-job.org verso:
       https://camoscio.onrender.com/api/users/scrub-eliminati?chiave=<segreto>
     Senza, gli account eliminati restano "Account eliminato" PER
     SEMPRE (nessuno scrub al giorno 30). Il server lo urla nei log
     all'avvio in produzione. Fail-safe, ma da fare.
  2. Ricaricare il .gpx/.fit col ⬆ per le vecchie escursioni da
     pubblicare nel feed (l'originale non era stato conservato).
  3. Camilla: `camilla2026` e' TEMPORANEA. Deve cambiarla da
     Impostazioni > Cambia password. Accede con EMAIL
     camilla.orsini@gmail.com (il login e' per email, non username).

>>> APERTO, non bloccante (invariato):
  - NOTA LEGALE: index.html fa accettare "Termini + Informativa
    privacy" senza documento dietro. Stesso professionista del punto
    GDPR sopra puo' chiuderli insieme.
  - M-5 (il vecchio, diverso dagli M della revisione sicurezza): race
    del cancella-contatto emergenza con 2 schede aperte (serve una
    DELETE per contenuto/id, non per indice posizionale).
  - TTL sulle Notification che nominano un utente: un "Mario Rossi
    ha chiesto di..." resta per sempre nelle notifiche altrui col
    nome vero anche dopo l'eliminazione di Mario. Cambio schema
    app-wide + migrazione -> a se'.
  - nascondere il cestino sulle card "completate in gruppo".

>>> ALTRE APERTE, non urgenti:
    dominio email proprio (rimandato) - causa vera delle email che non
    arrivano (spam, es. Camilla).

>>> RESTA VALIDO:
  - Hook "Stop" di auto-save: commit locale (MAI push) a fine di
    ogni risposta se ci sono modifiche non salvate. Va rifuso in un
    commit vero prima di ogni push.
  - Traduzione IT/EN (punto 102, CHIUSO): resta ITALIANO di
    proposito tutto il testo scritto dagli utenti, i nomi propri di
    luogo, i nomi dei percorsi salvati, titolo/descrizione dei badge
    personali, le schede "i" dei badge, il testo delle notifiche (lo
    genera il server), il segnaposto nativo di <input type=date>/
    <input type=file>, e componiRapporto() dentro diagnostica-gps.
    Le chiavi EN dei blocchi nuovi si aggiungono mano a mano.
  - "Account eliminato" (punto A-3.4) SI traduce:
    common.accountEliminato -> "Deleted account".

===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — congelato dal 6/08/2026: da quella data il racconto
    disteso di ogni sessione vive solo in ../camoscio memoria/06-Cronologia-Sessioni.md,
    non qui. Utile solo per il dettaglio di lavoro PRIMA di quella data.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
MAI piu' (congelato); cose_da_fare.txt quando un punto si chiude o ne arriva uno nuovo.

>>> LEZIONI OPERATIVE (git dopo un'interruzione, moduli che si collegano tardi,
    componenti condivisi fra piu' pagine, .toObject vs .toJSON, $unset di campi
    required, select('username') che scavalca la pseudonimizzazione, recupero
    password vs grazia dell'eliminazione, npm audit fix vs overrides, serializzatore
    aggiunto a una rotta senza try/catch, test verde su una premessa falsa,
    select:false non vale su un campo assegnato in memoria, select:false su
    findByIdAndUpdate va reso condizionale su una rotta condivisa, un gate privacy
    condiviso va dichiarato fail-closed, un subagent puo' dichiarare falsamente di
    aver aggiornato la memoria - due volte confermato, due funzioni di render sullo
    stesso dato ma DOM diversi non danno errore se chiami quella sbagliata, un
    history.back() e' asincrono e corre contro un pushState immediatamente successivo,
    una navigazione che cambia solo l'hash non ricarica la SPA - il codice di init
    non si rieseguisce): tutte in ../camoscio memoria/07-Trappole-Tecniche.md — quella
    e' la sede dove si guarda prima di toccare codice a rischio.
===================================================================
