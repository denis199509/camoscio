===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-09-12 (43a sessione, CHIUSA. Sei richieste evase in
fila: foto profilo -> prova telefono 2FA -> invito squadra confermato
-> 4 punti in sospeso (rifugi, colore assi, chat, percorso da seguire)
-> revisione code-reviewer + security-privacy sul cumulativo di tutto.
TUTTO VERIFICATO DAL VIVO E PUSHATO. Dettaglio completo di ogni punto in
cose_da_fare.txt e nel vault (04-Da-Fare.md + 06-Cronologia-Sessioni.md)
- qui solo il riassunto e cosa resta davvero aperto.)

FATTO E PUSHATO in questa sessione:
 - Foto profilo ingrandibile al click, verificata dal vivo (1af4a87)
 - Prova al telefono del 2FA con Google Authenticator: giro completo
   (accensione, login a due passi, disattiva/riattiva, reset con 2FA,
   recupero ritardato avviato/annullato e poi completato per davvero).
   Nessun bug trovato. Il 2FA sull'account VERO di Denis e' rimasto
   SPENTO dal completamento del recupero (comportamento corretto per
   progetto) - da riaccendere quando vuole, nessun'altra azione.
 - Invito squadra direzionale: confermato dal vivo solo il caso
   "invito a un'escursione" (corrisponde al progetto - consenso alla
   persona, non iscrizione automatica). Gli altri due casi del punto
   originale (entrare in una squadra da un invito diretto; uscita del
   creatore e successione) NON sono mai stati verificati dal vivo -
   Denis ha scelto di chiudere il punto cosi'.
 - Rifugi con emoji propria sulla mappa + colore assi Chart.js in
   palette calda (08419f5)
 - Data/ora sotto i messaggi di chat, stile WhatsApp, con separatori
   di giorno (1c76be3)
 - Percorso da seguire anche da un RouteDraft proprio, non solo da
   tracce copiate da altri (344f43f)
 - Promemoria "sto registrando" condizionato: chiuso SENZA CODICE dopo
   un'indagine che ha trovato il vincolo non aggirabile (la notifica
   persistente viene da un plugin nativo diverso da quello del
   promemoria periodico, nessuna API per sapere se e' stata scartata).
   Resta come oggi: il promemoria scatta sempre ogni ~1h.
 - Revisione code-reviewer + security-privacy sul cumulativo di tutto
   quanto sopra (`git diff 2f13451..HEAD`): 0 CRITICI, 3 ALTO + 6 MEDIO
   + 5 BASSO applicati e verificati dal vivo (479af95, a2f8bde,
   74e46ad). Tra i MEDIO: un flag di privacy (`saltaQuote`) per non
   mandare a un servizio esterno la geometria di un percorso quando
   serve solo disegnarlo; un rate limiter dedicato su /api/routing/plan.

  >>> DA DOVE SI RIPARTE (44a sessione):
   1. BLOCCO 9 del piano 2FA = prove automatiche complete + docs (la
      parte scritta - la prova dal vivo e' gia' chiusa, vedi sopra).
      Due buchi di test noti dalla 42a: `/2fa/recovery-codes` senza
      sezione dedicata; ramo `scartoMinuti` di `/2fa/enable` non
      asserito. i18n EN solo sul nucleo delle viste 2FA.
      Piano: C:\Users\lenovo\.claude\plans\camoscio-2fa-totp.md, sez. 11.
   2. Nessun'altra richiesta di Denis in sospeso al momento (le 4 della
      lista del 10/09 sono tutte chiuse) - proporre da soli il lavoro
      preparato ma non scritto (sotto, "LAVORO FUTURO PREPARATO") se
      Denis non porta lui stesso l'argomento a inizio sessione.

>>> APERTI (invariati):
  - CONFORMITA' GDPR + NOTA LEGALE: index.html fa accettare "Termini +
    Informativa privacy" senza documento dietro. Serve un consulente privacy/
    avvocato GDPR (non un DPO) per l'Informativa vera + il testo di consenso
    prima di accettare un invito. security-privacy 35a lo segna ALTO-2. Nel
    frattempo si sono accumulati altri trattamenti da far validare insieme:
    (a) l'automatismo D5 pubblica una traccia GPS al gruppo senza opt-in
    esplicito; (b) deadManLastFired conserva i nomi di contatti di emergenza
    non-utenti; (c) AccountRecovery (2FA) conserva per ~21 giorni la traccia
    di un tentativo di presa dell'account - serve una base giuridica anche
    per questo, e va nominato nell'informativa; (d) (43a) selezionare un
    RouteDraft proprio come "percorso da seguire" puo' ancora mandare la
    geometria a open-meteo per il route planner (non per tracking.js, che
    ora manda saltaQuote) - stesso trattamento del route planner, gia' da
    validare insieme al resto.
  - VERIFICHE DAL VIVO 40a: correttezza #9 e #11 in tracking.js; il riquadro
    "ultimo allarme fallito" nel pannello Sicurezza; il comando ⋮ "Togli la
    mia traccia" + la nota nel modale "Completa"; la prova al telefono del
    Punto 1 (+ RICHIESTA 2, un partecipante che dopo carica la sua traccia).
  - MEDIO-5 (security-privacy 35a): i tempi misurati dal creatore si applicano
    identici a ogni confermato e finiscono in "Velocita' Ascesa X m/h" sulla
    Dashboard. GDPR art. 5(1)(d). MA E' LA DECISIONE CHE DENIS HA GIA' PRESO
    APPOSTA (33a/35a: tempo di cammino uniforme, il singolo lo sostituisce
    col ⬆).

>>> LAVORO FUTURO PREPARATO, agli atti (NIENTE codice):
    FILONE B: collezione "I 2000 dell'Appennino" - bloccato, LA LISTA
    UFFICIALE DELLE VETTE LA FORNISCE DENIS. Piano:
      C:\Users\lenovo\.claude\plans\camoscio-2000-appennino-collezione.md

>>> AZIONI MANUALI DI DENIS, aperte:
  1. ACCOUNT_SCRUB_SECRET su Render (stringa a scelta, DIVERSA da
     SAFETY_CRON_SECRET) + un job giornaliero su cron-job.org verso:
       https://camoscio.onrender.com/api/users/scrub-eliminati?chiave=<segreto>
     Senza, gli account eliminati restano "Account eliminato" PER SEMPRE.
     NB: il job SAFETY_CRON_SECRET (scadenze DMS) ora fa ANCHE la spazzata
     dei deadManLastFired oltre 180 giorni - se non esiste ancora, va creato.
  2. Ricaricare il .gpx/.fit col ⬆ per le vecchie escursioni da pubblicare
     nel feed (l'originale non era stato conservato).

>>> ALTRE APERTE, non urgenti:
    dominio email proprio (rimandato) - causa vera delle email in spam.

>>> RESTA VALIDO:
  - Hook "Stop" di auto-save: commit locale (MAI push) a fine di ogni
    risposta se ci sono modifiche non salvate. Va rifuso in un commit vero
    prima di ogni push.
  - Traduzione IT/EN (punto 102, CHIUSO): resta ITALIANO di proposito tutto
    il testo scritto dagli utenti, i nomi di luogo, i percorsi salvati,
    titolo/descrizione dei badge, le schede "i", il testo delle notifiche
    (lo genera il server), il segnaposto nativo di <input type=date/file>,
    e componiRapporto() dentro diagnostica-gps. Le chiavi EN dei blocchi
    nuovi si aggiungono mano a mano.
  - "Account eliminato" (punto A-3.4) SI traduce: common.accountEliminato ->
    "Deleted account".

--- LAVORI PRECEDENTI FATTI E PUSHATI (per contesto, dal piu' vecchio) ---
  TASTO INDIETRO / History API (33a): riscritto il nucleo di navigazione in
    public/js/app.js. Pushato (7793426).
  RITOCCHI VISIVI FASCIA A/B/C (33a-37a): audit dell'agente designer.
    Pushati (fino a 6368ad3). Non fatte: B1 (empty-state), B5 (scala testo).
  PUNTO 1 (35a): completamento di gruppo da tracciamento dal vivo. Pushato.
  TRE PICCOLI PUNTI (35a): cestino card completate; M-5 rotte atomiche
    contatti emergenza; TTL 90gg Notification. Pushati.
  2FA COMPLETO, blocchi 1-8 + revisione (35a-42a): TUTTO PUSHATO
    (ab06f07, 2f13451). Resta solo il BLOCCO 9 - vedi sopra.
  43a SESSIONE (12/09): foto profilo, prova 2FA telefono, invito squadra,
    rifugi/assi/chat/percorso da seguire + revisione cumulativo - vedi
    riepilogo in cima a questo file, dettaglio in cose_da_fare.txt.

===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — congelato dal 6/08/2026: da quella data il racconto
    disteso di ogni sessione vive solo in ../camoscio memoria/06-Cronologia-Sessioni.md,
    non qui. Utile solo per il dettaglio di lavoro PRIMA di quella data.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
MAI piu' (congelato); cose_da_fare.txt quando un punto si chiude o ne arriva uno nuovo.

>>> LEZIONI OPERATIVE: tutte in ../camoscio memoria/07-Trappole-Tecniche.md — quella
    e' la sede dove si guarda prima di toccare codice a rischio. In breve, le piu'
    ricorrenti: git dopo un'interruzione; moduli che si collegano tardi; componenti
    condivisi fra piu' pagine; .toObject vs .toJSON; $unset di campi required;
    select('username') che scavalca la pseudonimizzazione; recupero password vs grazia
    dell'eliminazione; npm audit fix vs overrides; serializzatore aggiunto a una rotta
    senza try/catch; test verde su una premessa falsa; select:false non vale su un campo
    assegnato in memoria; un gate privacy condiviso va dichiarato fail-closed; un
    subagent puo' dichiarare falsamente di aver aggiornato la memoria (tratto ricorrente
    di security-privacy, non un caso isolato); due funzioni di render sullo stesso dato
    ma DOM diversi non danno errore se chiami quella sbagliata; una navigazione che
    cambia solo l'hash non ricarica la SPA (serve navigateTo); il service worker MV3
    dell'estensione Chrome si sospende; Math.max(...array) va in RangeError oltre
    ~1,2e5 elementi (usare .reduce); routeSource.kind:'saved' e' SEMPRE una traccia GPS
    registrata; le prove di prove/ NON si lanciano in parallelo (DB Atlas condiviso);
    crypto.createHmac con chiave vuota NON lancia su Node 24; le settings-card si
    nascondono con la CLASSE .hidden non con l'attributo; page.click di Puppeteer
    fallisce su un bottone in un pannello scrollabile (usare evaluate); UN RATE-LIMITER
    PER IP SU UNA ROTTA FRA ESTRANEI diventa un modo per bloccarsi a vicenda (quattro
    occorrenze ormai: DMS/deactivate 40a, 2FA/recovery 42a, route-planning 43a);
    un RouteDraft salva solo i punti scelti, MAI la linea calcolata (si ricalcola con
    /api/routing/plan); i tratti 'retta' di un percorso calcolato vanno SEMPRE
    distinti visivamente dai tratti 'sentiero' (vincolo 7); uno script Node con
    backtick dentro va scritto su file ed eseguito, MAI passato inline a bash -c/
    node -e dentro doppi apici (la shell li mangia come command substitution). NB dal
    06/09: il dislivello ha DUE soglie - 0 per le tracce importate da un file di
    dispositivo (grezzo), 10 per le quote stimate (percorso progettato, GPS dal vivo).
    Vedi lib/gpx.js.
===================================================================
