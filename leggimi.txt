===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-09-03 (VENTOTTESIMA sessione — HOTFIX REVISIONE
SICUREZZA "Invito squadra direzionale". Chiuso il triage che era
rimasto aperto a fine 27a. Verdetto del 2o giro dell'agente:
GO al push. Push `1bb0df5..1f98413` su `master`, Render redeploy
automatico.

--- COSA E' STATO CHIUSO ---
12 voci del rapporto (commit `b6e0edd`), + 2 rilievi del 2o giro
dell'agente (commit `1f98413`):

 C-1  (CRITICO)  la corsia "un partecipante propone un terzo in
      pendingApproval" e' CHIUSA. `canNonCreatorEditParticipation`
      (routes/hikes.js) -> `return false` dopo il ramo
      touchesOnlySelf. Client social.js: una sola strada (invito
      direzionale), riga disabilitata su escursione altrui ad
      approvazione manuale. DECISIONE DI DENIS (opzione A): rovescia
      la sua scelta #2 della 27a "la proposta resta".
 A-1  Squad.name maxlength:80 a schema + trim().slice(0,80) in
      POST /api/squads + maxlength HTML.
 A-2  DELETE /api/squads/:id/members/:userId: isSquadMember
      sull'attore PRIMA dei rami (i due controlli erano entrambi
      condizionati a !seStesso -> un estraneo faceva partire lo
      scioglimento di una squadra con members vuoto).
 A-3  overrides: { "qs": "6.16.0" } in package.json. `npm audit fix`
      da solo NON chiudeva l'advisory (express@4.22.2 fissa
      qs:~6.15.1, nessuna express 4.x sopra), `--force` -> express 5.
      `npm audit --omit=dev` = 0.
 M-1  successore del creatore uscente = primo membro VIVO (filtro di
      lib/squadAdmin.js); se nessun vivo -> la squadra si scioglie
      invece di restare col creatorId fantasma.
 M-2  PUT /api/hikes/:id: tetto 200 + dedup (new Set) su participants
      / pendingApproval, prima di ogni guardia.
 M-3  squadVisibileA (specchio di hikeVisibileA, toJSON) su GET /,
      invite-response, request-join, DELETE members: un non-membro
      vede la squadra ma solo il PROPRIO invito/richiesta.
 M-4  scrubAccount fa $pull da Squad/Hike .pendingInvites e
      Hike.pendingApproval + nuova DELETE /api/hikes/:id/invites/
      :userId (solo creatore).
 M-5  invitoLimiter nuovo (40/ora, SEPARATO da scritturaLimiter =
      Dead Man's Switch) su invite-squad, POST /squads,
      POST /squads/:id/invite, entrambe le DELETE .../invites.
 M-6  complete-group verifica la partecipazione: confirmedUserIds
      solo fra participants + pendingApproval (+ creatore).
      DECISIONE DI DENIS: revoca la "conferma di un walk-up mai
      iscritto" del punto 64. L'avviso "segnato come presente"
      resta per chi era in pendingApproval.
 B-1  $unset di pendingInvites condizionato a { $size: 0 } (3 punti).
 B-2  invite-squad filtra anche pendingDeletionAt.

 2o GIRO DELL'AGENTE (plans/...-giro2.md): GO, 0 CRITICO, 1 ALTO,
 3 MEDIO, 5 BASSO, nessuna regressione bloccante. Chiusi:
 ALTO-1  (residuo M-6) complete-group accettava anche chi era solo
         in pendingInvites (invitato che NON ha accettato) ->
         finalizzarlo era C-1 con un nome nuovo. Tolto dall'insieme.
 MEDIO-2 (regressione da M-3) GET /api/squads senza try/catch +
         squadVisibileA che tocca creatorId nullo -> crash del
         processo. Aggiunto try/catch + guardie in lib/squad.js.
 Notification.text maxlength:600 (300 taglierebbe l'esito del Dead
         Man's Switch, ~540 caratteri con 5 contatti).

>>> STATO A FINE 28a SESSIONE:
      - camoscio       origin/master = `1f98413` (codice hotfix;
                       sopra solo il commit di note). Working tree PULITO.
      - camoscio memoria (vault) origin/master aggiornato (04/06/03/07).
      - Tutto pushato in entrambi i repo.
      - Prove verdi: prova-invito-squadra 54/54, prova-partecipanti-
        forzati 35/35, prova-invito-direzionale 41/41,
        prova-squadre-consenso 53/53 (sez. 7b nuova: A-2/M-1/M-3),
        prova-eliminazione-account 57/57, prova-elimina-escursione
        53/53, prova-punto116 42/42, prova-punto77 17/17,
        prova-punto93 28/28, prova-punto113 103/103.
      - npm audit --omit=dev = 0.

>>> APERTI DELLA REVISIONE SICUREZZA (follow-up, NON bloccano):
  - MEDIO-1: POST /api/safety/activate condivide ancora
    scritturaLimiter con POST /api/hikes / complete-group /
    DELETE /api/users/me. Dietro NAT uno esaurisce la quota e un
    altro non arma il timer di sicurezza. Serve un sicurezzaLimiter
    dedicato (tocca routes/safety.js + prova-punto37).
  - MEDIO-3: Squad.photo (data URL 2 MB) senza limiter su
    PUT /:id/photo e senza select('-photo') in GET /. Fix pulito =
    nuova GET /api/squads/:id (la pagina squadra oggi legge la foto
    da CamoscioState.squads).
  - B-3 (tetti non atomici), B-4 (nessuna notifica a chi e' rimosso
    da una squadra / a chi ne eredita la proprieta'), B-5 (export
    GDPR senza gli inviti ricevuti), B-6 (tutti i partecipanti, non
    solo il creatore, vedono pendingInvites per intero).
  - CONFORMITA' GDPR (serve un professionista, NON e' consulenza
    dell'agente): il testo dell'invito dovrebbe dire, PRIMA
    dell'accettazione, che accettare = la propria posizione potra'
    essere condivisa coi partecipanti in caso di mesh/SOS; e
    l'uscita da participants andrebbe presentata come revoca del
    consenso, con la stessa evidenza.
  - VERIFICA DAL VIVO di Denis su tutto l'invito squadra (card
    Accetta/Rifiuta, gruppo "Inviti", creazione squadra -> inviti,
    "Lascia la squadra") - MAI provato su un telefono vero. E il
    punto 116 (linea su Esplora + card "Percorso previsto").

>>> SI LAVORA SU `master`, hotfix normali (commit vero + push -> Render
    ridistribuisce). L'hook di auto-save committa in locale (mai push):
    va rifuso in UN commit vero prima di ogni push.

>>> AZIONI MANUALI DI DENIS, aperte:
  1. ACCOUNT_SCRUB_SECRET su Render (stringa a scelta, DIVERSA da
     SAFETY_CRON_SECRET) + un job giornaliero su cron-job.org verso:
       https://camoscio.onrender.com/api/users/scrub-eliminati?chiave=<segreto>
     Senza, gli account eliminati restano "Account eliminato" PER
     SEMPRE (nessuno scrub al giorno 30). Il server lo urla nei log
     all'avvio in produzione. Fail-safe, ma da fare.
  2. Ricaricare il .gpx/.fit col ⬆ per le vecchie escursioni da
     pubblicare nel feed (l'originale non era stato conservato). E
     per farci comparire la LINEA sulla mappa (punto 116, 27a).
  3. Camilla: `camilla2026` e' TEMPORANEA. Deve cambiarla da
     Impostazioni > Cambia password. Accede con EMAIL
     camilla.orsini@gmail.com (il login e' per email, non username).

>>> APERTO, non bloccante (da prima della 28a):
  - PROVA MOBILE v2 sul telefono, SUL SITO LIVE: drawer/hamburger, i
    4 tab di hike-page, la nuova #settings, il layout stretto.
  - RESPONSIVE Dashboard <900px (regole @media viste solo in
    simulazione).
  - NOTA LEGALE: index.html fa accettare "Termini + Informativa
    privacy" senza documento dietro. Serve un professionista.
  - M-5 (il vecchio, diverso da M-5 della revisione): race del
    cancella-contatto emergenza con 2 schede aperte (serve una
    DELETE per contenuto/id, non per indice posizionale).
  - TTL sulle Notification che nominano un utente: un "Mario Rossi
    ha chiesto di..." resta per sempre nelle notifiche altrui col
    nome vero anche dopo l'eliminazione di Mario. Cambio schema
    app-wide + migrazione -> a se'.
  - nascondere il cestino sulle card "completate in gruppo".

>>> GIA' IN PRODUZIONE prima di oggi (origin/master era `1bb0df5`
    prima della 28a, `1f98413` dopo):
    27a sessione — punto 116 (traccia importata -> linea sulla
    mappa, Hike.routePath) + INVITO SQUADRA DIREZIONALE chiuso per
    intero (4 tappe + consenso squadra, 0fce557->adf9ff2).
    26a — DELETE /api/hikes/:id + due vulnerabilita' preesistenti
    (creatore aggiungeva chiunque; complete-group senza tetto).
    25a — tre rifiniture v2. 24a — menu "Squadre"; eliminazione
    account (A-3.4); password temporanea Camilla. Prima: TUTTA la
    revisione UX "Camoscio v2" (merge 02/09); traduzione IT/EN
    (102); timbri con traccia reale (108); moderazione segnalazioni
    (111); parte social (113); import .fit (114); nome uscita
    editabile (115).
    >>> LIMITE .fit/.gpx, e' il FILE non il codice: un file "smart"
        (o da Garmin Connect) ha buchi -> niente split cammino/pause.
    >>> Smartwatch (Garmin/Suunto/Amazfit): valutato e SCARTATO.

>>> ALTRE APERTE, non urgenti (dettaglio in vault 04-Da-Fare.md):
    livelli ORIGINALI del punto 63b + calcolo automatico del raggio
    (punto 42a) — Denis deve ancora pensarci, non richiederlo;
    dominio email proprio (rimandato da lui) — e' anche la causa
    vera delle email che non arrivano (spam).
    FILONE B: collezione "I 2000 dell'Appennino" — bloccato, LA
    LISTA UFFICIALE DELLE VETTE LA FORNISCE DENIS.
    Piano: C:\Users\lenovo\.claude\plans\camoscio-2000-appennino-collezione.md

>>> RESTA VALIDO:
  - Hook "Stop" di auto-save: commit locale (MAI push) a fine di
    ogni risposta se ci sono modifiche non salvate. Va rifuso in un
    commit vero prima di ogni push.
  - Traduzione IT/EN (punto 102, CHIUSO): resta ITALIANO di
    proposito tutto il testo scritto dagli utenti, i nomi propri di
    luogo, i nomi dei percorsi salvati, titolo/descrizione dei badge
    personali, le schede "i" dei badge, il testo delle notifiche (lo
    genera il server), il segnaposto nativo di <input type=date>/
    <input type=file>, e componiRapporto() dentro diagnostica-gps.
    Le chiavi EN dei blocchi nuovi si aggiungono mano a mano.
  - "Account eliminato" (punto A-3.4) SI traduce:
    common.accountEliminato -> "Deleted account".

===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — congelato dal 6/08/2026: da quella data il racconto
    disteso di ogni sessione vive solo in ../camoscio memoria/06-Cronologia-Sessioni.md,
    non qui. Utile solo per il dettaglio di lavoro PRIMA di quella data.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
MAI piu' (congelato); cose_da_fare.txt quando un punto si chiude o ne arriva uno nuovo.

>>> LEZIONI OPERATIVE (git dopo un'interruzione, moduli che si collegano tardi,
    componenti condivisi fra piu' pagine, .toObject vs .toJSON, $unset di campi
    required, select('username') che scavalca la pseudonimizzazione, recupero
    password vs grazia dell'eliminazione, npm audit fix vs overrides, serializzatore
    aggiunto a una rotta senza try/catch, test verde su una premessa falsa): tutte in
    ../camoscio memoria/07-Trappole-Tecniche.md — quella e' la sede dove si guarda
    prima di toccare codice a rischio.
===================================================================
