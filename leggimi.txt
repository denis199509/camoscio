===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-09-01 (VENTESIMA sessione — PASSO 7 fatto e committato
(`e33be1b`): sidebar a GRUPPI + rinomine menu (Home / Ispirazioni /
Mappa / Community). Verificato in locale IT+EN. Poi: SCRITTI I PIANI
ESECUTIVI DI TUTTI I BLOCCHI RIMANENTI (9 -> 14c) e chiuse tutte le
domande §15. 7 commit veri su `v2-ux`, NIENTE pushato. PROSSIMA
SESSIONE: CODICE, si parte da PASSO 9. Piano esecutivo:
.claude/plans/delegated-gathering-forest.md).

>>> LAVORO IN CORSO: revisione UX del sito ("Camoscio v2"). Si lavora
    ESCLUSIVAMENTE IN LOCALE sul branch `v2-ux`, un blocco alla volta.
    NIENTE push, NIENTE deploy: Denis manda online solo quando l'intera
    v2 lo convince ("quando vedo che stiamo per finire mando online").
    `master` resta intatto = la produzione attuale.
    Metodo: per ogni blocco approvato -> prima il piano (EnterPlanMode +
    AskUserQuestion per le ambiguita'), poi UN commit vero
    `V2 UX PASSO n: ...` su `v2-ux` (locale). Alla fine: merge su
    `master` + push. L'hook di auto-save committa comunque sul branch;
    va rifuso in un commit vero (git reset --soft) prima di ogni tappa.
    La DASHBOARD e' finita (Fasi 1-5). Il RIORDINO MENU/NAVIGAZIONE e'
    in corso: PASSO 7 fatto (sidebar desktop). I PIANI DI TUTTI I BLOCCHI
    RIMANENTI SONO GIA' SCRITTI (Denis: "prima i piani, poi il codice") -
    dettaglio file-per-file in .claude/plans/delegated-gathering-forest.md.
    Ordine: 7 -> 9 -> 10 -> 11 -> 11b -> 12 -> 13 -> 8 (drawer mobile) ->
    14a/b/c. Da qui in poi la prossima sessione FA CODICE, non piani.

>>> FATTO su `v2-ux` (7 commit, NON pushati) — DASHBOARD (Fasi 1-5) + PASSO 6-7:
  - `7915762` PASSO 1 — apertura Dashboard contestuale. Via "Benvenuto" +
    le 3 statistiche; nuovo header .dash-hero (fuori dalla .dashboard-grid):
    "Ciao [Nome] 👋" + sottotitolo contestuale + [🥾 Trova un'escursione]
    [🗺️ Esplora]. Titolo sez. "Esplora Escursioni di Gruppo" -> "Escursioni
    di Gruppo".
  - `48fb101` PASSO 2 (Fase 2) — card "La tua prossima avventura":
    titolo, giorno+data+conto alla rovescia, i 3 numeri SOLO se presenti,
    "da <trailhead>", "👥 N partecipanti", [Apri escursione ->]. Solo con
    un'escursione CONFERMATA in programma (prossimaAvventura).
  - `5e2c01d` PASSO 3 (Fase 3) — due aggiunte alla card (il resto era gia'
    coperto da Fase 2): link "Vedi tutte le mie escursioni ->" (solo se
    >1 futura confermata); "Organizzata da te" se l'ho creata io.
  - `0f42aa5` PASSO 4 (Fase 4) — "Il tuo cammino" (N vette/rifugi/badge +
    progressione per ZONA: barra + "Ti mancano N vette per completare X")
    SOSTITUISCE il Passaporto. "Ultimi traguardi" (ultimi N badge per data)
    al posto dell'anteprima. "Il tuo <anno>" (escursioni/km/D+/vette
    dell'anno) SOSTITUISCE "Quanto hai camminato". "Passo & Fatica" resta,
    esce dal grid a piena larghezza. Nuove progressoZone()/ultimiTraguardi()
    in badges.js su statoBadge() - NON il sistema LIVELLI_ASCESA (punti
    86/87). UNICO tocco backend della revisione: GET /api/tracking/totals
    accetta ?anno=YYYY opzionale (senza -> risposta identica). map.js
    aggiornato (chiamava renderDashboardStamps, rimossa).
  - `309ab92` PASSO 5 (Fase 5) — rifinitura CSS: grafico Passo & Fatica
    non piu' stirato (max-width 640px); "4/7" accanto al nome zona;
    .dashboard-grid align-items:start; 3 fix @media 900 (3 badge in fila,
    "Vedi tutti i badge" fuori dall'angolo del FAB, stacco 16px).
  - `7f8b697` PASSO 6 (19a sessione) — titoli di sezione dell'header =
    voce di menu ("corto ovunque", AskUserQuestion). 5 valori prettyNames
    (app.js) + 5 EN sectionTitle.* (i18n.js) allineati a nav.*, "Badge" /
    "Carpooling" / "Zaino Intelligente" / "Sicurezza & Mesh" / "Tribu' &
    Squadre" (prima erano i nomi lunghi che su telefono facevano "..."); 3
    commenti riscritti. ZERO CSS, ZERO logica. NB: il piano del riordino
    menu (sotto) rilavora comunque tutta la mappa nav.*/sectionTitle.*/
    prettyNames alle rinomine - il principio "header = menu" resta.
  - `e33be1b` PASSO 7 (20a sessione) — sidebar a GRUPPI. <nav> in 4
    blocchi (NAVIGAZIONE / AZIONE / AREA PERSONALE / STRUMENTI); gruppo
    "Esplora" a fisarmonica (Escursioni/Mappa/Ispirazioni); voce "+ Crea"
    (apre create-hike-modal, corpo estratto in
    window.apriModaleNuovaEscursione); blocco PONTE "Strumenti" =
    Carpooling/Zaino/Sicurezza finche' PASSO 14+ non li sposta in
    hike-page. Rinomine (SOLO etichette, data-target invariati):
    Dashboard->Home, Feed->Ispirazioni (anche il titolo interno della
    pagina Feed + icona sparkles), Mappa & Sentieri->Mappa, Tribu' &
    Squadre->Community. app.js: accordion UNO alla volta + stato in
    localStorage; navigateTo apre il gruppo della foglia attiva e ne
    segna la testa "is-current". CSS: .nav-group/.nav-crea + regola-PONTE
    @media 900 = rail a icone di oggi (drawer vero = PASSO 8). Decisioni
    §15: accordion singolo, #progress+alias (PASSO 11), + Crea voce non
    FAB, #people-search redirect (PASSO 10), Q8/Q9 chiuse; Q4->PASSO 12,
    Q6/Q7->PASSO 14+.

  Dashboard: verificato in locale sull'account REALE di Denis (badge veri,
  sola lettura; escursioni finte solo in memoria del browser): tutti gli
  stati, Gran Sasso 4/7, IT<->EN, console pulita. Responsive <900px solo
  in simulazione (resize_window inaffidabile) - da guardare al deploy.
  PASSO 6: verificato in locale (demo Marco Alpinista, 11 sezioni x IT/EN,
  header = menu su tutte, console pulita).
  PASSO 7: verificato in locale (account reale, sola lettura, zero
  scritture DB) - 11 destinazioni IT+EN, fisarmonica, stato annidato,
  deep-link Dashboard, + Crea, localStorage. Console NON leggibile
  (l'estensione Chrome blocca javascript/console su questo localhost);
  nessun errore visibile in ~25 interazioni. <900px solo simulato.
  Bug trovato e corretto in verifica: prettyNames["social"] era rimasto
  "Tribu' & Squadre" (una rinomina = 2 modifiche: prettyNames IT +
  sectionTitle EN).

>>> DA DOVE SI RIPARTE: CODICE, si parte da PASSO 9. I PIANI CI SONO GIA'.

  Piano ESECUTIVO di tutti i blocchi (file-per-file, meccanismi condivisi
  di PASSO 7 in testa, verifica): quello e' il documento da seguire ->
    C:\Users\lenovo\.claude\plans\delegated-gathering-forest.md
  Decisioni + mappa sezioni + §15 (tutte chiuse):
    C:\Users\lenovo\.claude\plans\camoscio-v2-menu-restructure.md

  Struttura bersaglio: Home / Esplora v (Escursioni, Mappa, Ispirazioni) /
  Le mie escursioni v (Tutte, In programma, Completate) / Community v
  (Amici) / + Crea (azione) / area personale: Progressi, Profilo,
  Impostazioni, Sicurezza.

  ORDINE: 7 (fatto, `e33be1b`) -> 9 -> 10 -> 11 -> 11b -> 12 -> 13 ->
  8 (drawer mobile) -> 14a -> 14b -> 14c.
  Il DRAWER MOBILE (PASSO 8) e' in FONDO: non verificabile in locale ora
  (resize_window inaffidabile, v2 non deployata) e i blocchi 9-13 cambiano
  ancora le voci -> fatto per ultimo, sul menu definitivo, provato sul
  telefono al deploy. La regola-PONTE in @media 900 tiene il rail nel
  frattempo.

  PROSSIMO - PASSO 9 (CODICE): #hikes (Esplora > Escursioni) resta SOLO
  scoperta ("A cui puoi partecipare" + ricerca + filtri); #my-hikes
  guadagna 3 viste-filtro nel menu (Tutte / In programma / Completate) che
  mostrano/nascondono i .my-hikes-group. classificaMieEscursioni()
  (social.js) NON si tocca. "In programma" = non completate, niente
  confronto date. Testa "Le mie escursioni" naviga (vista Tutte) + espande.

  DOMANDE §15: TUTTE CHIUSE. Le decisive:
   - Q4 esperto locale -> Profilo MOSTRA, Impostazioni MODIFICA.
   - Q5 + Crea -> popover 2 voci: nuova escursione + nuovo percorso
     (NO "nuova squadra": resta in Community).
   - Q6 zaino personale slegato da un'uscita -> NON serve. Tutto in
     hike-page, #backpack si ritira per intero.
   - Q7 #safety -> resta a se', foglia "Sicurezza" in AREA PERSONALE
     (mesh sim + registro + rimando timer). SOLO Carpooling e Zaino
     vanno in hike-page (14b/14c). Il timer e' gia' in Mappa.
   - blocco "STRUMENTI" (ponte) sparisce a PASSO 14a.

  Punto 3 (responsive Dashboard <900px): si accorpa alla verifica di fine
  riordino, dopo il drawer (PASSO 8).

  FILONE B (indipendente) - COLLEZIONE "I 2000 DELL'APPENNINO", dentro la
  pagina Badge (poi Progressi), NON pagina a se'. Piano:
    C:\Users\lenovo\.claude\plans\camoscio-2000-appennino-collezione.md
  Si innesta su badge-points.js con un campo collezioni:[]; il timbro si
  assegna da solo. LA LISTA UFFICIALE DELLE VETTE LA FORNISCE DENIS.
  PUNTO APERTO n.1: icone e peso del repo (~100 vette). Raccomandazione:
  1 grafica "2000+" unica col nome a testo, o template SVG. Puo'
  aspettare la lista.

  Merge v2-ux -> master + push: lo decide Denis a fine v2. origin/master
  = master = efd1290, 0 divergenza.

>>> DOVE STA IL DETTAGLIO: i PIANI in C:\Users\lenovo\.claude\plans\ -
    camoscio-v2-menu-restructure.md (il riordino, §15 con lo stato delle
    domande), camoscio-2000-appennino-collezione.md (filone B),
    delegated-gathering-forest.md (il piano di PASSO 7, gia' eseguito).
    I piani delle Fasi 3/4/5 in jiggly-munching-brooks.md. Nel vault:
    04-Da-Fare ("PROSSIMO GRANDE LAVORO" + "Da dove si riparte"); decisioni
    in 03 ("Camoscio v2 — riordino menu e navigazione": PASSO 6-7 + le 4
    decisioni + la collezione 2000); parole di Denis in cose_da_fare.txt
    ("VENTESIMA SESSIONE"); racconto nel 06 ("Ventesima sessione"). In 07
    la trappola "[hidden] battuto da una regola d'autore su .nav-group-body".

>>> GIA' IN PRODUZIONE (fino alla 15a sessione, su `master`,
    8d6990c): traduzione IT/EN (punto 102), timbri con traccia reale
    (108), moderazione segnalazioni sentiero (111), parte social —
    Segui/Feed/"mi piace"/"crea percorso" (113), import .fit (114), nome
    uscita editabile (115), + 4 rifiniture del 31/08. Dettaglio:
    cose_da_fare.txt, vault 03/06/07.
    >>> LIMITE .fit/.gpx, e' il FILE non il codice: un file "smart" (o da
        Garmin Connect) ha buchi -> niente split cammino/pause. Serve 1 Hz.
    >>> DENIS DEVE RICARICARE il .gpx/.fit col ⬆ per le escursioni gia'
        fatte che vuole pubblicare (l'originale non era stato conservato).
    >>> Smartwatch (Garmin/Suunto/Amazfit): valutato e SCARTATO.

>>> ALTRE APERTE, non urgenti (dettaglio in vault 04-Da-Fare.md):
    livelli ORIGINALI del punto 63b (gradini per la ZONA dichiarata a
    mano, DIVERSI dai livelli gia' fatti del punto 86) + calcolo
    automatico del raggio (punto 42a) - Denis deve ancora pensarci, non
    richiederlo; dominio email proprio (rimandato da lui, "non ora").

>>> RESTA VALIDO:
  - Hook "Stop" di auto-save: commit locale (MAI push) a fine di ogni
    risposta se ci sono modifiche non salvate in questo repo. Va rifuso
    in un commit vero prima di ogni push/tappa (git reset --soft
    sull'ultimo commit vero se gli auto-save sono piu' di uno).
  - Traduzione IT/EN (punto 102, CHIUSO): resta ITALIANO di proposito
    tutto il testo scritto dagli utenti (didascalie del feed comprese),
    i nomi propri di luogo, i nomi dei percorsi salvati e l'etichetta
    "da <autore>", titolo/descrizione dei badge personali, le schede "i"
    dei badge (punto 112), il testo delle notifiche (lo genera il
    server), il segnaposto nativo di <input type=date>/<input type=file>,
    e componiRapporto() dentro diagnostica-gps.html. Le chiavi EN dei
    blocchi v2 UX si aggiungono mano a mano (scelta di Denis).

===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo,
    vedi 04-Da-Fare.md del vault).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — congelato dal 6/08/2026: da quella data il racconto
    disteso di ogni sessione vive solo in ../camoscio memoria/06-Cronologia-Sessioni.md,
    non qui. Utile solo per il dettaglio di lavoro PRIMA di quella data.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
MAI piu' (congelato); cose_da_fare.txt quando un punto si chiude o ne arriva uno nuovo.

>>> I PUNTI CHIUSI FINO AL 103 (piu' 94 e 98) non sono piu' elencati qui: erano
    diventati un appello di 190 righe, cioe' esattamente "qualcosa che appartiene a
    un altro file". Tutti chiusi, committati e pushati, molti confermati dal vivo da
    Denis. Il dettaglio con le sue parole e' in cose_da_fare.txt (cercare il numero);
    il racconto per sessione in ../camoscio memoria/06-Cronologia-Sessioni.md.
    Compressione fatta il 28/08/2026, stesso criterio dello sfoltimento del 18/08.

>>> LEZIONI OPERATIVE (git dopo un'interruzione, moduli che si collegano
    tardi, componenti condivisi fra piu' pagine, trappole Android/Gradle):
    tutte in ../camoscio memoria/07-Trappole-Tecniche.md, non ripetute qui -
    quella e' la sede dove si guarda prima di toccare codice a rischio.
===================================================================
