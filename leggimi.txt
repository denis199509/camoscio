===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-09-12 (QUARANTATREESIMA sessione. Denis: "possiamo partire
con la modifica delle foto profilo" -> verificata dal vivo e committata a
parte (vedi sotto). STESSA SESSIONE, poi: "come faccio la prova per il
2FA?" -> GIRO COMPLETO SUL TELEFONO DI DENIS, CON GOOGLE AUTHENTICATOR,
CHIUSO E VERIFICATO IN OGNI PASSO: accensione (QR + codici di recupero
salvati) -> login a due passi -> disattivazione -> riattivazione -> reset
password con 2FA attivo (link via email, campo codice) -> recupero
ritardato (opzione C) avviato -> banner "recupero in corso" visto e
annullato -> un SECONDO recupero avviato e completato per davvero. Il
completamento richiede aspettare 14 giorni per progetto (`maturaIl`
immutabile): per non farlo aspettare, forzato `maturaIl`/`expiresAt` del
SOLO documento AccountRecovery vivo del suo account (userId filtrato,
letto PRIMA di scrivere, null altro campo toccato) a 5 minuti nel
passato con uno script usa-e-getta fuori dal repo (stessa forzatura di
`prova-recupero-ritardato.js` sez.7, ma su dati veri non di test).
Riaperto lo stesso link e-mail -> modulo password senza piu' chiedere il
codice -> password cambiata -> VERIFICATO ANCHE DAL DATABASE (lettura
sola): `completatoIl` scritto sul recupero, `twoFactorEnabledAt` tornato
`undefined` sull'utente (il completamento spegne il 2FA per progetto -
il suo account vero e' quindi ORA SENZA 2FA, segnalato a Denis, riaccende
lui quando vuole). NESSUNA prova automatica sostituiva questo giro:
era l'ultimo "davvero aperto" del 2FA prima del BLOCCO 9. Nessun bug
trovato lungo il percorso (un solo intoppo: Denis non aveva notato i due
link piccoli sotto il campo codice della pagina di reset - "Non ho il
telefono: usa un codice di recupero" / "Non ho ne' il telefono ne' i
codici di recupero" - risolto guardando meglio, non era un bug).
——
2026-09-11 (QUARANTADUESIMA sessione. Denis: "possiamo mandare gli
agenti per il controllo dell'ultima sessione", poi "vai a gruppi, parti dai 3
alto" -> "vai col medio" -> "vai con il gruppo basso" -> "commit e push su
tutto".

2FA TOTP — BLOCCHI 2-8 + REVISIONE DEL CUMULATIVO: FATTO E PUSHATO.
`code-reviewer` + `security-privacy` su `git diff d0b8b35` (i blocchi 2-8
della 41a): 0 CRITICI in entrambi i giri. Applicati TUTTI i rilievi in tre
gruppi, con regressione verde dopo ognuno:

  - ALTO (3): secondoFattoreLimiter/duefattoriLimiter erano per IP invece
    che per persona - un secchio condiviso permetteva di bloccare il login
    a due passi di un altro utente sullo stesso NAT, e (piu' grave) lasciava
    di fatto SENZA TETTO i tentativi di indovinare un TOTP (POST /login
    riscrive pending2fa gratis a ogni chiamata riuscita). Corretti con
    keyGenerator per persona; tolto da /reset-password (il freno vero li'
    e' gia' tentativi2fa). La corsa nota in avviaOTrovaRecupero lasciava
    annullare un solo recupero su due: /recovery/cancel e /2fa/disable ora
    fanno updateMany, non updateOne.
  - MEDIO (8): sessione pending2fa superstite a un cambio password via
    recupero (ora riparte da capo); password richiesta anche su
    /2fa/enable (prima solo su /disable); AccountRecovery nell'export
    GDPR; token di recupero da query string a POST; avviso "codici di
    recupero rimasti" mai arrivato al client (ora su /me + toast + badge);
    finestra di recuperoVivoDi ricontrollata anche prima della spazzata
    TTL; cluster i18n (markup che usciva come testo, banner non tradotto,
    tre formattatori di data ora tutti Europe/Rome); test mancante su
    /2fa/recovery-codes (nuova sez. 6b in prova-2fa.js).
  - BASSO (6): segreto 2FA provvisorio ripulito alla scadenza; due chiavi
    i18n morte; session.userId azzerato nel ramo 2FA di /login;
    bcrypt.hash spostato prima del CAS in /recovery/complete; codici di
    recupero svuotati dal DOM lasciando la sezione o il focus; niente piu'
    frammenti del segreto TOTP nei log.

REGRESSIONE a ogni gruppo, sempre verde: prova-2fa 150/150 (144->150, +sez.
6b), prova-recupero-ritardato 54/54, prova-browser-2fa 19/19,
prova-squadre-consenso 71/71 (tocca l'export). npm audit non toccato (zero
dipendenze nuove in questo giro).

GIT: i 10 commit sparsi (8 auto-save originali dei blocchi 2-8 + 2 di
revisione di questa sessione) rifusi in DUE commit puliti, tenuti separati
dai 5 commit "41a: leggimi + cose_da_fare" che stavano in mezzo (lasciati
intatti): `2abd804` (note di sessione) + `2f13451` (tutto il codice).
PUSHATI: `d0b8b35..2f13451 master -> master`. `origin/master` = `2f13451`.

Il `security-privacy` ha di nuovo affermato di aver aggiornato la mia
memoria con note nuove — controllato, false, stessa fabbricazione gia'
vista alla 31a sessione (6 giorni prima, pattern ormai confermato
ricorrente). Non tocca l'affidabilita' dei suoi rilievi tecnici.

FUORI DAL 2FA, nella 42a sessione: controllo utenti registrati su
richiesta di Denis (nuovo iscritto dell'11/09, "Solly99"). Poi Denis ha
chiesto di poter ingrandire la foto profilo al click ("se entro sul
profilo di qualcuno non riesco a vedere bene la foto") — implementato
dall'agente `designer`, che ha riusato il lightbox gia' esistente (nato
per le icone dei badge) invece di farne uno nuovo: `userprofile.js`
(`renderProfileIdentity`, condivisa col proprio profilo) + `styles.css` +
`i18n.js`.

FOTO PROFILO — VERIFICATA DAL VIVO E CHIUSA (43a sessione, 12/09): Denis
"possiamo partire con la modifica delle foto profilo". Riconnessa
l'estensione Chrome (era il blocco della 42a), server locale acceso,
login demo (Marco Alpinista) → profilo di un utente reale con foto vera
(Denis; i 4 account demo hanno solo l'emoji) → click apre il lightbox,
Esc chiude. Committata a parte dall'auto-save della 42a (che la teneva
insieme a nient'altro): `1af4a87` (codice) + `930c2fd` (le note di
chiusura 42a, message invariato). NON ANCORA PUSHATO — via di Denis in
sospeso, resta 2 commit avanti a `origin/master` (= `2f13451`).

  >>> DA DOVE SI RIPARTE (44a sessione):
   1. BLOCCO 9 del piano 2FA = prove complete + docs + vault. La prova al
      telefono (l'unico pezzo che nessuna prova automatica poteva fare) e'
      CHIUSA (vedi sopra, 43a) - resta la parte scritta: rivedere
      `prove/LEGGIMI-PROVE.txt`, i due buchi di test noti dalla 42a
      (`/2fa/recovery-codes` senza sezione dedicata; ramo `scartoMinuti`
      di `/2fa/enable` non asserito), e i18n EN solo sul nucleo.
   2. Il 2FA sull'account vero di Denis e' rimasto SPENTO dalla prova di
      completamento recupero (per progetto) - da riaccendere quando vuole.
   Piano: C:\Users\lenovo\.claude\plans\camoscio-2fa-totp.md, sez. 11.

>>> APERTI (invariati):
  - PERCORSO DA SEGUIRE anche da un RouteDraft PROPRIO: la tendina
    "Percorso da seguire" della registrazione (linea di riferimento, no
    avvisi di fuori-percorso) oggi elenca SOLO i SavedRoute
    (kind:'saved', copiati da una traccia altrui), non i percorsi che
    l'utente ha disegnato da se' col route planner. Denis (10/09, dal
    telefono): "vorrei poter seguire la linea pure da un progetto creato
    da me". Da guardare: public/js/tracking.js, pannello "Opzioni
    escursione". Dettaglio in cose_da_fare.txt.
  - PROMEMORIA "sto registrando" solo se la notifica persistente e'
    stata scartata (Denis, "se fattibile"): il promemoria periodico "il
    tracciamento e' ancora attivo" (~1 h) doveva scattare SOLO se
    l'utente aveva gia' rimosso la notifica persistente "Registrazione
    GPS in corso". Se e' il service worker a rifarla, si puo' gate con
    self.registration.getNotifications({tag}); se e' una push dal server,
    non fattibile e Denis dice "lasciamo cosi". Priorita' bassa.
    Dettaglio in cose_da_fare.txt.
  - RIFUGI SULLA MAPPA con un'emoji da rifugio, non da montagna (Denis
    10/09): public/js/map.js, il forEach su puntiTimbrabili (~1165-1178)
    disegna cime E rifugi con lo stesso peakIcon hardcodato a 🏔️ ->
    ramo su peak.tipo, 'rifugio' -> 🛖 / 🏠. Piccolo, un punto solo.
  - DATA E ORA sotto ogni messaggio di chat, stile WhatsApp (Denis
    10/09): il createdAt c'e' gia' (HikeMessage/SquadMessage timestamps,
    arriva in m.createdAt) ma chatpanel.js renderChatMessages (~77-91)
    non lo mostra. Aggiungere uno <span> con l'ora ITALIANA
    (toLocaleString timeZone Europe/Rome - non UTC, vedi la trappola in
    ../camoscio memoria/07-Trappole-Tecniche.md). Separatori di giorno
    ("Oggi/Ieri/15 settembre") confermati da Denis il 10/09. Un file solo
    (chatpanel.js e' condiviso escursione+squadra). Dettaglio in
    cose_da_fare.txt.
  - CONFORMITA' GDPR + NOTA LEGALE: index.html fa accettare "Termini +
    Informativa privacy" senza documento dietro. Serve un consulente privacy/
    avvocato GDPR (non un DPO) per l'Informativa vera + il testo di consenso
    prima di accettare un invito. security-privacy 35a lo segna ALTO-2. Nel
    frattempo si sono accumulati altri trattamenti da far validare insieme:
    (a) l'automatismo D5 pubblica una traccia GPS al gruppo senza opt-in
    esplicito; (b) deadManLastFired conserva i nomi di contatti di emergenza
    non-utenti; (c) AccountRecovery (2FA) conserva per ~21 giorni la traccia
    di un tentativo di presa dell'account - serve una base giuridica anche
    per questo, e va nominato nell'informativa.
  - VERIFICA DAL VIVO di Denis su tutto l'invito squadra direzionale (entrare
    in una squadra; invito squadra a un'escursione; uscita e successione del creatore).
  - VERIFICHE DAL VIVO 40a: correttezza #9 e #11 in tracking.js; il riquadro
    "ultimo allarme fallito" nel pannello Sicurezza; il comando ⋮ "Togli la
    mia traccia" + la nota nel modale "Completa"; la prova al telefono del
    Punto 1 (+ RICHIESTA 2, un partecipante che dopo carica la sua traccia).
  - MEDIO-5 (security-privacy 35a): i tempi misurati dal creatore si applicano
    identici a ogni confermato e finiscono in "Velocita' Ascesa X m/h" sulla
    Dashboard. GDPR art. 5(1)(d). MA E' LA DECISIONE CHE DENIS HA GIA' PRESO
    APPOSTA (33a/35a: tempo di cammino uniforme, il singolo lo sostituisce
    col ⬆).
  - #9CA3AF degli assi Chart.js -> --color-text-secondary (piccolo cambio
    visivo, punto a se' fuori audit).

>>> LAVORO FUTURO PREPARATO, agli atti (NIENTE codice):
    FILONE B: collezione "I 2000 dell'Appennino" - bloccato, LA LISTA
    UFFICIALE DELLE VETTE LA FORNISCE DENIS. Piano:
      C:\Users\lenovo\.claude\plans\camoscio-2000-appennino-collezione.md

>>> AZIONI MANUALI DI DENIS, aperte:
  1. ACCOUNT_SCRUB_SECRET su Render (stringa a scelta, DIVERSA da
     SAFETY_CRON_SECRET) + un job giornaliero su cron-job.org verso:
       https://camoscio.onrender.com/api/users/scrub-eliminati?chiave=<segreto>
     Senza, gli account eliminati restano "Account eliminato" PER SEMPRE.
     NB: il job SAFETY_CRON_SECRET (scadenze DMS) ora fa ANCHE la spazzata
     dei deadManLastFired oltre 180 giorni - se non esiste ancora, va creato.
  2. Ricaricare il .gpx/.fit col ⬆ per le vecchie escursioni da pubblicare
     nel feed (l'originale non era stato conservato).

>>> ALTRE APERTE, non urgenti:
    dominio email proprio (rimandato) - causa vera delle email in spam.

>>> RESTA VALIDO:
  - Hook "Stop" di auto-save: commit locale (MAI push) a fine di ogni
    risposta se ci sono modifiche non salvate. Va rifuso in un commit vero
    prima di ogni push.
  - Traduzione IT/EN (punto 102, CHIUSO): resta ITALIANO di proposito tutto
    il testo scritto dagli utenti, i nomi di luogo, i percorsi salvati,
    titolo/descrizione dei badge, le schede "i", il testo delle notifiche
    (lo genera il server), il segnaposto nativo di <input type=date/file>,
    e componiRapporto() dentro diagnostica-gps. Le chiavi EN dei blocchi
    nuovi si aggiungono mano a mano.
  - "Account eliminato" (punto A-3.4) SI traduce: common.accountEliminato ->
    "Deleted account".

--- LAVORI PRECEDENTI FATTI E PUSHATI (per contesto) ---
  TASTO INDIETRO / History API (33a): riscritto il nucleo di navigazione in
    public/js/app.js. Pushato (7793426).
  RITOCCHI VISIVI FASCIA A/B/C (33a-37a): audit dell'agente designer.
    Pushati (fino a 6368ad3). Non fatte: B1 (empty-state), B5 (scala testo).
  PUNTO 1 (35a): completamento di gruppo da tracciamento dal vivo. Pushato.
  TRE PICCOLI PUNTI (35a): cestino card completate; M-5 rotte atomiche
    contatti emergenza; TTL 90gg Notification. Pushati.
  2FA COMPLETO, blocchi 1-8 + revisione (35a-42a): blocco 1 in ab06f07
    (con backlog 35a + decisioni/consolidamento 40a); blocchi 2-8 +
    revisione del cumulativo (ALTO/MEDIO/BASSO, 42a) in 2f13451. TUTTO
    PUSHATO. Resta solo il BLOCCO 9 (prove complete + docs) e la prova al
    telefono di Denis — vedi "DA DOVE SI RIPARTE" in cima al file.

===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — congelato dal 6/08/2026: da quella data il racconto
    disteso di ogni sessione vive solo in ../camoscio memoria/06-Cronologia-Sessioni.md,
    non qui. Utile solo per il dettaglio di lavoro PRIMA di quella data.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
MAI piu' (congelato); cose_da_fare.txt quando un punto si chiude o ne arriva uno nuovo.

>>> LEZIONI OPERATIVE: tutte in ../camoscio memoria/07-Trappole-Tecniche.md — quella
    e' la sede dove si guarda prima di toccare codice a rischio. In breve, le piu'
    ricorrenti: git dopo un'interruzione; moduli che si collegano tardi; componenti
    condivisi fra piu' pagine; .toObject vs .toJSON; $unset di campi required;
    select('username') che scavalca la pseudonimizzazione; recupero password vs grazia
    dell'eliminazione; npm audit fix vs overrides; serializzatore aggiunto a una rotta
    senza try/catch; test verde su una premessa falsa; select:false non vale su un campo
    assegnato in memoria; un gate privacy condiviso va dichiarato fail-closed; un
    subagent puo' dichiarare falsamente di aver aggiornato la memoria (successo di nuovo,
    42a sessione: e' un tratto ricorrente di security-privacy, non un caso isolato); due
    funzioni di render sullo stesso dato ma DOM diversi non danno errore se chiami quella
    sbagliata; una navigazione che cambia solo l'hash non ricarica la SPA (serve
    navigateTo); il service worker MV3 dell'estensione Chrome si sospende; Math.max(...
    array) va in RangeError oltre ~1,2e5 elementi (usare .reduce); routeSource.kind:
    'saved' e' SEMPRE una traccia GPS registrata; le prove di prove/ NON si lanciano in
    parallelo (DB Atlas condiviso); crypto.createHmac con chiave vuota NON lancia su
    Node 24; le settings-card si nascondono con la CLASSE .hidden non con l'attributo;
    page.click di Puppeteer fallisce su un bottone in un pannello scrollabile (usare
    evaluate); UN RATE-LIMITER PER IP SU UNA ROTTA FRA ESTRANEI diventa un modo per
    bloccarsi a vicenda (tre occorrenze ormai: DMS/deactivate 40a, 2FA/recovery 42a —
    chiedersi sempre "puo' chiamarla un account estraneo?" prima di lasciare il
    keyGenerator di default). NB dal 06/09: il dislivello ha DUE soglie - 0 per le
    tracce importate da un file di dispositivo (grezzo), 10 per le quote stimate
    (percorso progettato, GPS dal vivo). Vedi lib/gpx.js.
===================================================================
