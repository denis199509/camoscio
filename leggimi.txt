===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-09-04 (TRENTESIMA sessione — CHIUSO PER INTERO il
blocco dei follow-up rimasti aperti dalla revisione sicurezza (29a):
ALTO-1, MEDIO-1b/1c, MEDIO-2, MEDIO-3 residuo, MEDIO-4. Push
`884ea32` su `master`, Render redeploy automatico. Due giri
`security-privacy` (uno su ALTO-1 da solo, uno sul cumulativo dei
5 commit): GO.

--- COSA E' STATO CHIUSO ---
Denis: "procedi" -> l'elenco di sicurezza lavorato in ordine, un
punto alla volta, ognuno col suo commit (6 in tutto, tutti pushati).

 ALTO-1   POST /api/notifications era requireAuth e basta: chiunque
          loggato forgiava una notifica per QUALUNQUE userId/testo
          (fino a 600 caratteri), incluso un falso esito del Dead
          Man's Switch. Rotta rimossa del tutto: l'unico uso
          legittimo (esito accettato/rifiutato di una richiesta di
          iscrizione) ora lo genera il server dentro PUT
          /api/hikes/:id stessa, testo fisso mai dal client.
 MEDIO-1b/1c  GET /api/squads/:id/photo (fino a 2 MB) non aveva un
          limiter di lettura ne' un Cache-Control. Nuovo
          fotoLetturaLimiter (100/ora).
 MEDIO-2  PUT /api/squads/:id/photo validava solo la LUNGHEZZA della
          stringa, mai i byte veri: qualunque payload con
          l'etichetta "data:image/xxx" passava. Ora si valida il
          magic byte reale + la dimensione decodificata.
 MEDIO-3 residuo  20/ora x foto fino a 2 MB = un IP riempiva Atlas
          in ~8h nel caso peggiore. squadpage.js ora comprime
          SEMPRE lato client (come le foto delle segnalazioni,
          stesso modulo imagecompress.js), sempre JPEG; il server ha
          abbassato il tetto a 600 KB e ristretto la validazione al
          solo JPEG.
 MEDIO-4  L'export GDPR (GET /api/users/me/export) aveva gli inviti
          RICEVUTI (B-5, 28a) ma non le richieste INVIATE
          (pendingRequests di una squadra / pendingApproval di
          un'escursione). Aggiunte, stessa minimizzazione di B-5.

 2° GIRO `security-privacy` sul cumulativo dei 5 commit: GO, con 2
 rilievi MEDIO sul codice appena scritto (non sul preesistente),
 chiusi nello stesso blocco (commit `884ea32`):
   M-1  il filtro anti-spam di ALTO-1 (giaPartecipante) chiudeva il
        riciclo pendingApproval SENZA una rimozione vera da
        participants, non quello CON rimozione (due PUT separate
        potevano ancora duplicare la notifica di esito). Ora
        idempotente: stesso testo/destinatario/escursione non si
        riscrive.
   M-2  Cache-Control: private, max-age=86400 su una risorsa
        MUTABILE (Squad.photo cambia a ogni PUT di un admin): gli
        altri membri potevano vedere una foto vecchia o gia' tolta
        fino a 24h. Ora private, no-cache (sempre rivalidata via
        ETag automatico di Express, stesso risparmio di banda sul
        contenuto invariato).

>>> STATO A FINE 30a SESSIONE:
      - camoscio: ultimo commit = `884ea32`, PUSHATO. Working tree
        pulito, niente auto-save in sospeso.
      - Prove verdi (rilanciate una alla volta - le suite condividono
        il DB demo, si "pestano i piedi" in parallelo):
        prova-partecipanti-forzati 43/43, prova-squadre-consenso
        71/71, prova-invito-squadra 54/54, prova-eliminazione-account
        57/57.
      - npm audit --omit=dev = 0.

>>> APERTI (follow-up sicurezza, NON bloccano):
  - ALTO NUOVO (trovato dal 2° giro agente, PREESISTENTE, FUORI
    SCOPE del blocco appena chiuso - quello copriva solo la foto
    squadra): User.profilePhoto (routes/users.js + routes/auth.js)
    ha lo STESSO pattern non validato/non compresso appena chiuso su
    Squad.photo, ma su una superficie piu' esposta: PUT
    /api/users/:id senza limiter dedicato, e POST /api/auth/register
    dove authLimiter ha skipSuccessfulRequests:true -> una
    REGISTRAZIONE RIUSCITA non consuma quota, il tetto vero e' il
    solo apiLimiter (3000/5min). Caso peggiore: 3000 account nuovi x
    foto fino a ~1,5 MB decodificati riempirebbero i 512 MB di Atlas
    in meno di un minuto. Fix proposto dall'agente: stesso blocco di
    validazione byte gia' scritto in routes/squads.js + far
    comprimere public/js/profile.js con lo stesso
    window.CamoscioImageCompress + un limiter NUOVO dedicato sulla
    registrazione (mai riusare authLimiter, condiviso col login).
    E' il prossimo punto, se si continua sul filone sicurezza.
  - CONFORMITA' GDPR (serve un professionista): il testo dell'invito
    dovrebbe dire, PRIMA dell'accettazione, che accettare = la
    propria posizione potra' essere condivisa coi partecipanti in
    caso di mesh/SOS; e l'uscita da participants come revoca del
    consenso.
  - VERIFICA DAL VIVO di Denis su tutto l'invito squadra (card
    Accetta/Rifiuta, gruppo "Inviti", creazione squadra -> inviti,
    "Lascia la squadra") - MAI provato su un telefono vero. E il
    punto 116 (linea su Esplora + card "Percorso previsto").

>>> LAVORO FUTURO PREPARATO IN QUESTA SESSIONE (progetti agli atti,
    NIENTE codice scritto - si riparte da questi):

  A) TASTO INDIETRO / HISTORY API. Denis: "il tasto indietro sia
     del telefono che pc buttano fuori dal sito... e' molto
     fastidioso". Camoscio e' una SPA senza integrazione con la
     cronologia del browser -> Indietro esce dal sito.
     DECISIONI PRESE: ambito PIENO (sezioni + pagine con id +
     MODALI, Indietro con un modale aperto lo chiude); Indietro
     sulla home ESCE (come Gmail/Instagram).
     Progetto completo (analisi navigateTo + censimento 8 modali +
     modello di stato + 4 sotto-decisioni da chiudere):
       C:\Users\lenovo\.claude\plans\camoscio-tasto-indietro-history-api.md
     ~130-180 righe, quasi tutte in public/js/app.js. Verifica
     solo dal vivo nel browser. Si puo' fare inline (no architect).

  B) RITOCCHI VISIVI (audit dell'agente designer, 29a). Il sito
     non e' amatoriale; il difetto e' che le regole imparate non
     sono mai state propagate. La piu' grossa: 19 regole CSS
     stilano <i> senza la gemella su svg -> morte dopo Lucide ->
     icone a 24px colore testo.
     Pronta una "Fascia A" di ~11 interventi CSS a costo quasi
     zero (< 80 righe, un solo file) che sistema le icone di tutto
     il sito, tutti gli stati vuoti (oggi storti: usano classi
     utility inesistenti), 2 overflow su telefono, la lista
     contatti del Dead Man's Switch (unico componente non stilato),
     la gerarchia dei titoli di scheda. NON applicata.
     Rapporto completo (Fascia A/B/C + le 2 domande sotto):
       C:\Users\lenovo\.claude\plans\camoscio-audit-visivo-29a.md
     Se si riprende: il lavoro va all'agente `designer`.
     DUE DECISIONI CHE ASPETTANO DENIS:
      1. "Passo & Fatica" (aperta dal 10/08): i numeri del passo
         sono blu, gli altri numeri della Dashboard arancioni, e
         nel grafico sotto il passo utente e' arancione. -> (a)
         tutti arancioni, (b) grafico ribaltato (passo utente blu
         ovunque), (c) lasciare + legenda "blu = stimato".
      2. Bianco su arancione = 4,03:1 (appena sotto la soglia AA)
         sul CTA principale. Scurire l'arancione del ~6% lo
         sistema, ma e' il colore del marchio.

>>> SI LAVORA SU `master`, hotfix normali (commit vero + push -> Render
    ridistribuisce). L'hook di auto-save committa in locale (mai push):
    va rifuso in UN commit vero prima di ogni push
    (`git reset --soft origin/master`).

>>> AZIONI MANUALI DI DENIS, aperte:
  1. ACCOUNT_SCRUB_SECRET su Render (stringa a scelta, DIVERSA da
     SAFETY_CRON_SECRET) + un job giornaliero su cron-job.org verso:
       https://camoscio.onrender.com/api/users/scrub-eliminati?chiave=<segreto>
     Senza, gli account eliminati restano "Account eliminato" PER
     SEMPRE (nessuno scrub al giorno 30). Il server lo urla nei log
     all'avvio in produzione. Fail-safe, ma da fare.
  2. Ricaricare il .gpx/.fit col ⬆ per le vecchie escursioni da
     pubblicare nel feed (l'originale non era stato conservato). E
     per farci comparire la LINEA sulla mappa (punto 116, 27a).
  3. Camilla: `camilla2026` e' TEMPORANEA. Deve cambiarla da
     Impostazioni > Cambia password. Accede con EMAIL
     camilla.orsini@gmail.com (il login e' per email, non username).

>>> APERTO, non bloccante (da prima della 28a):
  - PROVA MOBILE v2 sul telefono, SUL SITO LIVE: drawer/hamburger, i
    4 tab di hike-page, la nuova #settings, il layout stretto.
  - RESPONSIVE Dashboard <900px (regole @media viste solo in
    simulazione).
  - NOTA LEGALE: index.html fa accettare "Termini + Informativa
    privacy" senza documento dietro. Serve un professionista.
  - M-5 (il vecchio, diverso da M-5 della revisione): race del
    cancella-contatto emergenza con 2 schede aperte (serve una
    DELETE per contenuto/id, non per indice posizionale).
  - TTL sulle Notification che nominano un utente: un "Mario Rossi
    ha chiesto di..." resta per sempre nelle notifiche altrui col
    nome vero anche dopo l'eliminazione di Mario. Cambio schema
    app-wide + migrazione -> a se'.
  - nascondere il cestino sulle card "completate in gruppo".

>>> GIA' IN PRODUZIONE prima di oggi (origin/master era `20e195f`
    prima della 30a, `884ea32` dopo):
    29a sessione — 5 follow-up della revisione sicurezza (28a):
    MEDIO-1 sicurezzaLimiter dedicato per l'attivazione del Dead
    Man's Switch; MEDIO-3 Squad.photo fuori da GET /api/squads
    (select:false, muro RAM su Render) + nuova GET /:id/photo; B-4/
    B-5/B-6 (notifiche espulsione/eredita' squadra, export inviti
    ricevuti, pendingInvites visibile solo al creatore). Push
    `99c5ddd`.
    28a sessione — HOTFIX revisione sicurezza "Invito squadra
    direzionale": C-1 (chiusa la corsia "proposta", rovescia la
    scelta della 27a) + 11 voci + 2 rilievi del 2o giro agente.
    27a — punto 116 (traccia importata -> linea sulla mappa,
    Hike.routePath) + INVITO SQUADRA DIREZIONALE chiuso per intero
    (4 tappe + consenso squadra).
    26a — DELETE /api/hikes/:id + due vulnerabilita' preesistenti
    (creatore aggiungeva chiunque; complete-group senza tetto).
    25a — tre rifiniture v2. 24a — menu "Squadre"; eliminazione
    account (A-3.4); password temporanea Camilla. Prima: TUTTA la
    revisione UX "Camoscio v2" (merge 02/09); traduzione IT/EN
    (102); timbri con traccia reale (108); moderazione segnalazioni
    (111); parte social (113); import .fit (114); nome uscita
    editabile (115).
    >>> LIMITE .fit/.gpx, e' il FILE non il codice: un file "smart"
        (o da Garmin Connect) ha buchi -> niente split cammino/pause.
    >>> Smartwatch (Garmin/Suunto/Amazfit): valutato e SCARTATO.

>>> ALTRE APERTE, non urgenti (dettaglio in vault 04-Da-Fare.md):
    livelli ORIGINALI del punto 63b + calcolo automatico del raggio
    (punto 42a) — Denis deve ancora pensarci, non richiederlo;
    dominio email proprio (rimandato da lui) — e' anche la causa
    vera delle email che non arrivano (spam).
    FILONE B: collezione "I 2000 dell'Appennino" — bloccato, LA
    LISTA UFFICIALE DELLE VETTE LA FORNISCE DENIS.
    Piano: C:\Users\lenovo\.claude\plans\camoscio-2000-appennino-collezione.md

>>> RESTA VALIDO:
  - Hook "Stop" di auto-save: commit locale (MAI push) a fine di
    ogni risposta se ci sono modifiche non salvate. Va rifuso in un
    commit vero prima di ogni push.
  - Traduzione IT/EN (punto 102, CHIUSO): resta ITALIANO di
    proposito tutto il testo scritto dagli utenti, i nomi propri di
    luogo, i nomi dei percorsi salvati, titolo/descrizione dei badge
    personali, le schede "i" dei badge, il testo delle notifiche (lo
    genera il server), il segnaposto nativo di <input type=date>/
    <input type=file>, e componiRapporto() dentro diagnostica-gps.
    Le chiavi EN dei blocchi nuovi si aggiungono mano a mano.
  - "Account eliminato" (punto A-3.4) SI traduce:
    common.accountEliminato -> "Deleted account".

===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — congelato dal 6/08/2026: da quella data il racconto
    disteso di ogni sessione vive solo in ../camoscio memoria/06-Cronologia-Sessioni.md,
    non qui. Utile solo per il dettaglio di lavoro PRIMA di quella data.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
MAI piu' (congelato); cose_da_fare.txt quando un punto si chiude o ne arriva uno nuovo.

>>> LEZIONI OPERATIVE (git dopo un'interruzione, moduli che si collegano tardi,
    componenti condivisi fra piu' pagine, .toObject vs .toJSON, $unset di campi
    required, select('username') che scavalca la pseudonimizzazione, recupero
    password vs grazia dell'eliminazione, npm audit fix vs overrides, serializzatore
    aggiunto a una rotta senza try/catch, test verde su una premessa falsa,
    select:false non vale su un campo assegnato in memoria): tutte in
    ../camoscio memoria/07-Trappole-Tecniche.md — quella e' la sede dove si guarda
    prima di toccare codice a rischio.
===================================================================
