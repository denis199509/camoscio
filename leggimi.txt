===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-07-30
===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DECISO DA DENIS IL 2026-07-29, ed e' il motivo per cui questo file e' nato:
prima "leggimi.txt" pesava 307 KB, ripeteva quello che c'era gia' negli altri due, ed
e' l'unico dei tre che ha detto il falso - due volte nella stessa giornata. Un file
lungo non si rilegge tutto quando lo si aggiorna, quindi invecchia senza che nessuno
se ne accorga. Questo si rilegge.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub, quindi non e' pubblico.
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — l'ex leggimi. Il racconto disteso di ogni lavoro fatto,
    con le misure. Ci si va quando serve il dettaglio di qualcosa di gia' fatto.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
solo se c'e' un lavoro nuovo da raccontare; cose_da_fare.txt quando un punto si chiude
o ne arriva uno nuovo.



>>> CHIUSO IL 2026-07-30, NON VA RIAPERTO: la persona del secondo account reale HA
    CONFERMATO il suo indirizzo, entro un'ora dall'invio. Sul database risulta
    emailVerified true e il token e' stato CONSUMATO, non e' scaduto.
    ORA TUTTI E TRE GLI ACCOUNT REALI SONO CONFERMATI, quindi tutti possono usare il
    recupero password - che era l'unico motivo per cui la conferma esiste.

>>> PRIMA COSA DA FARE, decisa da Denis il 2026-07-29 e ANCORA DA FARE:
    METTERE IL VAULT SU GITHUB, in un repository PRIVATO e separato. "Privato" e' gia'
    stato scelto da lui: non e' una domanda da rifargli.
    Perche': il vault e' la fonte principale del progetto ma sta solo su disco dentro
    OneDrive, ed e' l'unica cosa senza storia delle versioni - un errore di scrittura li'
    non si annulla con un commit.
    Perche' NON dentro camoscio: questo repository e' pubblico, e una cartella dentro un
    repo pubblico e' pubblica. Il controllo del contenuto e' gia' stato fatto (nessuna
    credenziale, nessun numero, nessuna email; ci sono pero' il nome del secondo account
    reale e i nomi di cluster e utente del database). I dettagli stanno in
    ../camoscio memoria/04-Da-Fare.md.

>>> COSA E' SUCCESSO IL 30/07, in breve (per esteso: punto 49 di cose_da_fare.txt).
    Segnalazione arrivata da una SECONDA PERSONA che usa il sito: "il tasto della
    conferma email non funziona, non arriva niente". IL TASTO NON ERA ROTTO.
    LE DUE COSE DA PORTARSI VIA:
      - il tasto NON SCADE. Le 24 ore sono la durata del link dentro l'email. E' un
        equivoco che tornera' fuori: non c'e' niente da "riattivare";
      - IL MODO PER SAPERLO SENZA INDOVINARE: ogni pressione crea un token sul
        database, che dura 24 ore. Non ce n'era nessuno, e la sua sessione era aperta
        da 11 ore. Quindi la richiesta al server non era mai partita. Il database sa
        rispondere a "questo tasto e' stato premuto davvero?" - conviene chiederglielo
        prima di leggere codice per mezz'ora.
    L'email gli e' stata poi fatta partire da produzione, col via libera di Denis,
    senza passare dalla fascia: "password dimenticata" su un indirizzo non confermato
    manda l'email di CONFERMA. Strada che esisteva gia' e non era mai stata usata.

>>> DUE COSE DA NON RIPETERE, imparate il 30/07:
    - AVEVO DETTO DI AVER RIPRODOTTO IL GUASTO, e non era vero: leggevo lo stato della
      pagina troppo presto, quando la fascia non era ancora comparsa. La regola delle
      prove ("si aspetta una condizione vera, mai un tempo fisso") vale anche quando si
      indaga a mano, non solo quando si scrive una prova;
    - la prima misura del miglioramento era rumorosa perche' cronometrava anche il
      caricamento della pagina. Il numero giusto era lo SCARTO fra "sito visibile" e
      "fascia comparsa": 1232 ms col codice vecchio, 0 ms con quello nuovo.

>>> DA DOVE SI CONTINUA COL LAVORO VERO. L'ordine consigliato in fondo a
    cose_da_fare.txt vale ancora: PUNTO 44, poi 41, 40, 46, 47 e 43. Il 43 e il 44
    vanno fatti vicini: sono due meta' dello stesso problema.
    CI SONO ANCHE il PUNTO 39 (mappa topografica: e' un cambio di MAPPA DI BASE, non
    un ritocco, e vuole due correzioni obbligatorie alla mappa offline) e il PUNTO 42b
    (quante volte su una vetta: non serve piu' nessun raggio), che dal 29/07 non sono
    piu' bloccati. Denis non ha ancora scelto da quale ripartire: GLIELO SI CHIEDE,
    non si decide per lui.

>>> STATO VERIFICATO A FINE SESSIONE (2026-07-30):
      - albero di lavoro pulito, master allineato a origin/master; la correzione della
        fascia e' stata COMMESSA E PUBBLICATA su richiesta di Denis ("manda online"),
        e il deploy su Render e' stato verificato sul file davvero servito, non solo
        sulla home;
      - sito online: home 200, e l'invio email risulta configurato e sano;
      - database: 7 utenti (4 demo + 3 reali), TUTTI E TRE I REALI CONFERMATI, nessun
        residuo di prova, nessun token in giro;
      - 182 controlli automatici rilanciati dopo la modifica, tutti passati:
        38 (browser verifica) + 34 (API verifica) + 53 (punto 7) + 57 (browser punto 7);
      - nessun server locale acceso, nessun processo node rimasto.
