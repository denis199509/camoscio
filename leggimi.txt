===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-09-14 (47a sessione, CHIUSA. Denis: "Possiamo
ripartire, manda l'architetto" -> PROGETTATO lo spacchettamento di
initApp() (piano: C:\Users\lenovo\.claude\plans\camoscio-initapp-refactor.md,
5 decisioni D-1...D-5) -> riportato in sintesi -> "Procedi con le
raccomandazioni" -> IMPLEMENTATO su 11 file e VERIFICATO DAL VIVO nel
browser -> VERIFICA GENERALE DEL CODICE ORA CHIUSA PER INTERO (3/3
CRITICI, 18/18 ALTO). Dettaglio completo: vault,
06-Cronologia-Sessioni.md, 47a sessione.)

FATTO in questa sessione:
 - PROGETTATO (agente architect) e POI IMPLEMENTATO l'ultimo ALTO della
   verifica generale: gli 8 moduli (safety, tracking, map, social,
   weather, storico, peoplesearch, trailhead-picker) che si
   agganciavano tardi dentro initApp() ora si dividono in
   setupXEvents() (subito) + renderX() (dati, dentro initApp()) -
   stessa forma dei 3 fix gia' fatti (menu 26/07, fascia email 30/07,
   profilo 06/08). Sistemati nello stesso giro anche carpool.js/
   backpack.js (D-3, avevano gia' lo split ma restavano chiamati tardi).
 - DUE DIFETTI PREESISTENTI trovati progettando e corretti nello
   stesso giro: (1) corsa fra il caricamento della mappa e la ripresa
   di una registrazione dopo un ricaricamento pagina (scriveva su una
   mappa non ancora creata, errore silenzioso - imparentato con gli
   avvii-fantasma gia' visti al punto 94); (2) il form "Nuova
   escursione" senza action, che ricaricava la pagina perdendo tutto
   il modulo compilato se si premeva Invio prima che il submit fosse
   collegato.
 - 5 DECISIONI (D-1...D-5) proposte dall'architetto, TUTTE approvate
   da Denis in blocco: spostare anche il ripristino del Timer di
   Sicurezza/contatti prima di initApp() (D-1); creare la mappa
   subito col rettangolo di riserva invece di aspettare i confini
   veri, opzione B (D-2, chiude anche il difetto 1 sopra); sistemare
   carpool/backpack nello stesso giro (D-3); guardia anti-doppio-
   aggancio su tutti gli 8 (D-4); chiudere il difetto (1) in questo
   giro (D-5).
 - VERIFICATO DAL VIVO nel browser vero (server locale, account demo
   Marco Alpinista, estensione Chrome): Dashboard, Mappa (contatti/
   Timer sicurezza popolati subito, meteo calcolato una volta sola,
   tasti registrazione abilitati dopo il caricamento), Sicurezza (chat
   mesh "Attivo, connesso" - prova diretta che D-1 funziona), Social
   (modale creazione aperto dal menu e richiuso dalla X senza
   ricaricare), Storico/Completate - console del browser sempre
   pulita. NON fatta la controprova rigorosa (riprodurre il guasto (1)
   PRIMA della correzione con rete rallentata ad arte, verificare che
   sparisca DOPO) - resta aperta, vedi sotto.
 - VERIFICA GENERALE DEL CODICE (iniziata 44a sessione): ORA CHIUSA
   PER INTERO. 3/3 CRITICI, 18/18 ALTO, tutti corretti e verificati.
 - Nessun file server toccato in questa sessione (solo public/js/):
   non applicabile la regola sul rilancio delle prove accessi/
   password/email. CONTROLLO FINALE: node --check pulito sugli 11
   file toccati; server di sviluppo locale fermato (PID specifico);
   git status pulito (solo gli 11 file attesi).

  >>> DA DOVE SI RIPARTE (48a sessione):
   1. BLOCCO 9 del piano 2FA = prove automatiche complete + docs (la
      prova dal vivo e' gia' chiusa, 43a sessione). Due buchi di test
      noti dalla 42a: `/2fa/recovery-codes` senza sezione dedicata;
      ramo `scartoMinuti` di `/2fa/enable` non asserito. i18n EN solo
      sul nucleo delle viste 2FA.
      Piano: C:\Users\lenovo\.claude\plans\camoscio-2fa-totp.md, sez. 11.
   2. Poi ~50+ MEDIO/BASSO dei tre blocchi della verifica generale
      (nessuno deciso ne' applicato) - vedi 04-Da-Fare.md.
   3. Se si torna a toccare map.js/tracking.js: prima la controprova
      rigorosa rimasta aperta (vedi sopra).
   4. Nessun'altra richiesta di Denis in sospeso al momento.

>>> APERTI (invariati salvo dove segnato 45a):
  - CONFORMITA' GDPR + NOTA LEGALE: index.html fa accettare "Termini +
    Informativa privacy" senza documento dietro. Serve un consulente privacy/
    avvocato GDPR (non un DPO) per l'Informativa vera + il testo di consenso
    prima di accettare un invito. security-privacy 35a lo segna ALTO-2. Nel
    frattempo si sono accumulati altri trattamenti da far validare insieme:
    (a) l'automatismo D5 pubblica una traccia GPS al gruppo senza opt-in
    esplicito; (b) deadManLastFired conserva i nomi di contatti di emergenza
    non-utenti; (c) AccountRecovery (2FA) conserva per ~21 giorni la traccia
    di un tentativo di presa dell'account; (d) selezionare un RouteDraft
    proprio come "percorso da seguire" puo' ancora mandare la geometria a
    open-meteo per il route planner; (e) (44a) trasferimenti a terzi mai
    dichiarati: opentopomap.org, nominatim/overpass (ricerca luoghi),
    open-meteo (quote); (f) (44a) import-gpx e completions/:id/gpx creano
    una sessione di posizione fuori dal gate di consenso geolocalizzazione;
    (g) (44a) nessun TTL sulle tracce GPS, restano per sempre anche dopo
    l'eliminazione dell'account; (h) (45a) open-meteo riceve dal BROWSER
    posizione esatta (backpack.js manda anche ritrovo+data futura) - qui il
    terzo vede l'utente direttamente, non passa dal server; (i) (45a) il
    consenso geo resta valido anche se la scrittura che lo registra fallisce,
    e non ha una data; (j) (45a) il logout non cancella i dati locali sul
    dispositivo (posizione reale, zona di casa, stato DMS, coda GPS); (k)
    (45a) la posizione viaggia su ogni messaggio della chat mesh senza
    informativa ne' interruttore; (l) (45a) GET /api/reports espone a tutti
    chi ha segnalato, dove e quando; (m) (45a) nessuna scadenza sui dati
    locali (cache tile mappa, coda GPS orfana); (n) (45a, CHIUSO - verificato
    conforme) le foto NON portano EXIF/GPS, tutti gli input passano da un
    ricompressore canvas che li distrugge - da nominare comunque come
    rassicurazione quando l'informativa esiste.
  - VERIFICHE DAL VIVO: correttezza #9 e #11 in tracking.js; il riquadro
    "ultimo allarme fallito" nel pannello Sicurezza; il comando ⋮ "Togli la
    mia traccia" + la nota nel modale "Completa"; la prova al telefono del
    Punto 1 (+ RICHIESTA 2, un partecipante che dopo carica la sua traccia).
    NUOVA (47a): la controprova rigorosa della corsa mappa/tracciamento -
    riprodurre il guasto PRIMA della correzione (rete rallentata ad arte
    su /api/regions/boundaries durante un ricaricamento a meta'
    registrazione) e verificare che sparisca DOPO. Fatta solo la verifica
    "a caldo" nel browser vero, non questa controprova specifica.
  - M-5 (44a, RIEMERSO): race sulla cancellazione di un contatto d'emergenza
    per indice posizionale invece che per id - due schede aperte in corsa
    possono cancellare il contatto sbagliato. Dalla 22a/24a sessione, mai
    piu' comparso nelle liste "aperti" successive per dimenticanza.
  - MEDIO-5 (security-privacy 35a): i tempi misurati dal creatore si applicano
    identici a ogni confermato e finiscono in "Velocita' Ascesa X m/h" sulla
    Dashboard. GDPR art. 5(1)(d). MA E' LA DECISIONE CHE DENIS HA GIA' PRESO
    APPOSTA (33a/35a: tempo di cammino uniforme, il singolo lo sostituisce
    col ⬆).

>>> LAVORO FUTURO PREPARATO, agli atti (NIENTE codice):
    FILONE B: collezione "I 2000 dell'Appennino" - bloccato, LA LISTA
    UFFICIALE DELLE VETTE LA FORNISCE DENIS. Piano:
      C:\Users\lenovo\.claude\plans\camoscio-2000-appennino-collezione.md

>>> AZIONI MANUALI DI DENIS, aperte:
  1. ACCOUNT_SCRUB_SECRET su Render (stringa a scelta, DIVERSA da
     SAFETY_CRON_SECRET) + un job giornaliero su cron-job.org verso:
       https://camoscio.onrender.com/api/users/scrub-eliminati?chiave=<segreto>
     Senza, gli account eliminati restano "Account eliminato" PER SEMPRE.
     NB: il job SAFETY_CRON_SECRET (scadenze DMS) ora fa ANCHE la spazzata
     dei deadManLastFired oltre 180 giorni - se non esiste ancora, va creato.
  2. Ricaricare il .gpx/.fit col ⬆ per le vecchie escursioni da pubblicare
     nel feed (l'originale non era stato conservato).

>>> ALTRE APERTE, non urgenti:
    dominio email proprio (rimandato) - causa vera delle email in spam.
    Punto 63b - livelli intermedi zona dichiarata a mano (Denis deve pensarci).

>>> RESTA VALIDO:
  - Hook "Stop" di auto-save: commit locale (MAI push) a fine di ogni
    risposta se ci sono modifiche non salvate. Va rifuso in un commit vero
    prima di ogni push. Commit di auto-save accumulati dalla 45a e dalla
    46a ancora da rifondere, MAI pushati (git status per il conteggio
    esatto: crescono a ogni risposta con modifiche).
  - Traduzione IT/EN (punto 102, CHIUSO): resta ITALIANO di proposito tutto
    il testo scritto dagli utenti, i nomi di luogo, i percorsi salvati,
    titolo/descrizione dei badge, le schede "i", il testo delle notifiche
    (lo genera il server), il segnaposto nativo di <input type=date/file>,
    e componiRapporto() dentro diagnostica-gps. Le chiavi EN dei blocchi
    nuovi si aggiungono mano a mano.
  - "Account eliminato" (punto A-3.4) SI traduce: common.accountEliminato ->
    "Deleted account".

--- LAVORI PRECEDENTI FATTI E PUSHATI (per contesto, dal piu' vecchio) ---
  TASTO INDIETRO / History API (33a): riscritto il nucleo di navigazione in
    public/js/app.js. Pushato (7793426).
  RITOCCHI VISIVI FASCIA A/B/C (33a-37a): audit dell'agente designer.
    Pushati (fino a 6368ad3). Non fatte: B1 (empty-state), B5 (scala testo).
  PUNTO 1 (35a): completamento di gruppo da tracciamento dal vivo. Pushato.
  TRE PICCOLI PUNTI (35a): cestino card completate; M-5 rotte atomiche
    contatti emergenza; TTL 90gg Notification. Pushati.
  2FA COMPLETO, blocchi 1-8 + revisione (35a-42a): TUTTO PUSHATO
    (ab06f07, 2f13451). Resta solo il BLOCCO 9 - vedi sopra.
  43a SESSIONE (12/09): foto profilo, prova 2FA telefono, invito squadra,
    rifugi/assi/chat/percorso da seguire + revisione cumulativo - vedi
    dettaglio in cose_da_fare.txt.
  44a SESSIONE (12/09): NESSUN CODICE PUSHATO. Sfoltimento vault +
    verifica generale del codice, blocchi 1-2 (routes/, lib/) - vedi
    dettaglio in 06-Cronologia-Sessioni.md del vault.
  45a SESSIONE (13/09): NESSUN CODICE PUSHATO (3 commit di auto-save
    locali, da rifondere). Blocco 3 (public/js/) della verifica generale
    + CORRETTI 2 CRITICI (lib/gpx.js, public/js/map.js), misurati e
    verificati dal vivo nel browser - vedi dettaglio in
    06-Cronologia-Sessioni.md del vault, sezione "45a" (due paragrafi).
  46a SESSIONE (14/09): NESSUN CODICE PUSHATO (commit di auto-save
    locali, da rifondere - un commit per lettera A-H sarebbe naturale).
    TERZO E ULTIMO CRITICO corretto (tracking.js+map.js, replay
    quadratico) + 17 DEI 18 ALTO corretti in blocco (sessione/dati
    esposti/lib/privacy client/DMS/mappa offline/refreshState/pannello
    mesh) - ognuno misurato e/o verificato dal vivo, 4 prove di
    regressione rilanciate (176/176). Resta solo il punto 15
    (strutturale, 8 file, rimandato apposta con l'architect) - vedi
    dettaglio in 06-Cronologia-Sessioni.md del vault, sezione "46a".
  47a SESSIONE (14/09): NESSUN CODICE PUSHATO (commit di auto-save
    locali, da rifondere). PROGETTATO con l'agente architect (piano
    dedicato, 5 decisioni D-1...D-5 tutte approvate) e POI IMPLEMENTATO
    nella stessa sessione l'ultimo ALTO della verifica generale (punto
    15, spacchettamento di initApp() su 11 file) - VERIFICA GENERALE
    ORA CHIUSA PER INTERO (3/3 CRITICI, 18/18 ALTO). Due difetti
    preesistenti trovati progettando e corretti nello stesso giro.
    Verificato dal vivo nel browser vero (console sempre pulita); non
    ancora fatta la controprova rigorosa con rete rallentata ad arte -
    vedi dettaglio in 06-Cronologia-Sessioni.md del vault, sezione "47a".

===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — congelato dal 6/08/2026: da quella data il racconto
    disteso di ogni sessione vive solo in ../camoscio memoria/06-Cronologia-Sessioni.md,
    non qui. Utile solo per il dettaglio di lavoro PRIMA di quella data.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
MAI piu' (congelato); cose_da_fare.txt quando un punto si chiude o ne arriva uno nuovo.

>>> LEZIONI OPERATIVE: tutte in ../camoscio memoria/07-Trappole-Tecniche.md — quella
    e' la sede dove si guarda prima di toccare codice a rischio. In breve, le piu'
    ricorrenti: git dopo un'interruzione; moduli che si collegano tardi; componenti
    condivisi fra piu' pagine; .toObject vs .toJSON; $unset di campi required;
    select('username') che scavalca la pseudonimizzazione; recupero password vs grazia
    dell'eliminazione; npm audit fix vs overrides; serializzatore aggiunto a una rotta
    senza try/catch; test verde su una premessa falsa; select:false non vale su un campo
    assegnato in memoria; un gate privacy condiviso va dichiarato fail-closed; un
    subagent puo' dichiarare falsamente di aver aggiornato la memoria (tratto ricorrente
    di security-privacy, non un caso isolato); due funzioni di render sullo stesso dato
    ma DOM diversi non danno errore se chiami quella sbagliata; una navigazione che
    cambia solo l'hash non ricarica la SPA (serve navigateTo, e ATTENZIONE dal 13/09:
    nemmeno chiamare navigateTo dalla console basta se non si passa dal punto giusto -
    verificare che la sezione prenda davvero la classe .active, non solo l'hash); il
    service worker MV3 dell'estensione Chrome si sospende; Math.max(...array) va in
    RangeError oltre ~1,2e5 elementi (usare .reduce, e ATTENZIONE: douglasPeucker in
    lib/geometry.js e' la stessa famiglia di bug, ricorsiva, RangeError misurato da
    ~4.000 punti - trovato 44a, CORRETTO IL 14/09 (46a): stack esplicito PIU' un tetto
    sul lavoro totale, perche' la sola conversione a iterativo toglie il RangeError ma
    NON il costo quadratico sull'input avversario, misurato apposta dopo - vedi sotto);
    routeSource.kind:'saved' e'
    SEMPRE una traccia GPS registrata; le prove di prove/ NON si lanciano in parallelo
    (DB Atlas condiviso); crypto.createHmac con chiave vuota NON lancia su Node 24; le
    settings-card si nascondono con la CLASSE .hidden non con l'attributo; page.click
    di Puppeteer fallisce su un bottone in un pannello scrollabile (usare evaluate); UN
    RATE-LIMITER PER IP SU UNA ROTTA FRA ESTRANEI diventa un modo per bloccarsi a
    vicenda (quattro occorrenze ormai: DMS/deactivate 40a, 2FA/recovery 42a,
    route-planning 43a); un RouteDraft salva solo i punti scelti, MAI la linea
    calcolata (si ricalcola con /api/routing/plan); i tratti 'retta' di un percorso
    calcolato vanno SEMPRE distinti visivamente dai tratti 'sentiero' (vincolo 7); uno
    script Node con backtick dentro va scritto su file ed eseguito, MAI passato inline
    a bash -c/node -e dentro doppi apici (la shell li mangia come command
    substitution). NB dal 06/09: il dislivello ha DUE soglie - 0 per le tracce
    importate da un file di dispositivo (grezzo), 10 per le quote stimate (percorso
    progettato, GPS dal vivo). Vedi lib/gpx.js. NB dal 12/09 (44a): una regex "greedy"
    con una classe di caratteri troppo larga (es. [^>] invece di [^<>]) puo' andare in
    backtracking quadratico su input malformati - trovato in lib/gpx.js, CORRETTO IL
    13/09 (45a): misurato prima/dopo sullo stesso identico file patologico, da
    quadratico (40.000 occ. = 5s) a piatto (200.000 = 9ms). NB dal 13/09 (45a): stesso
    principio per un ciclo che ridisegna una struttura a ogni iterazione - addLatLng
    uno per uno su una polyline Leaflet e' quadratico, misurato 42s/16.000 punti in
    tracking.js, CORRETTO IL 14/09 (46a): un solo setLatLngs, 8,9ms/16.000 punti. E: quando la stessa
    formula/convenzione e' reimplementata a mano in piu' file invece di stare in un
    unico posto, prima o poi le copie divergono in silenzio - confermato una TERZA
    volta (escapeHtml mancante in una sola delle due copie del popup vetta in map.js,
    XSS reale, CORRETTO IL 13/09) e allargato alle formule: quella ore-CAI esiste in 3
    copie di cui 2 gia' NaN, ancora aperta. NB dal 13/09 (45a): escapeHtml() dentro un
    onclick="f('...')" inline NON basta - il browser decodifica le entita' PRIMA che
    il motore JS legga l'attributo, quindi &#39; ridiventa ' e si esce comunque dalla
    stringa. Corretto in map.js (tolto l'onclick inline, sostituito con data-* +
    addEventListener); la stessa forma resta latente in ~20 altri punti del progetto,
    oggi innocua solo perche' interpolano sempre un ObjectId. Prima di correggere un
    punto trovato in revisione, grep il resto del file/blocco per lo stesso dato o la
    stessa formula. NB dal 14/09 (46a): un cancello unico che unisce piu' requisiti
    indipendenti blocca anche chi non ha bisogno di tutti - configurato() di
    lib/mailer.js univa "credenziali presenti" (serve sempre) e "APP_BASE_URL
    utilizzabile" (serve solo alle email coi link), e l'allarme DMS - che non ha
    nessun link - passava dallo stesso cancello: un APP_BASE_URL rotto in produzione
    faceva tacere l'unico avviso di sicurezza del sito. Corretto scomponendo il
    cancello (credenzialiPresenti() separata) con un parametro a default invariato
    (richiedeLinkFunzionante) sui chiamanti esistenti.
===================================================================
