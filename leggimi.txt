===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-08-30 (quattordicesima sessione).

>>> NIENTE IN CORSO. Tutto pushato e in produzione (Render redeploy
    automatico ai push).

  ULTIMO LAVORO — PUNTI 114 e 115, CHIUSI E IN PRODUZIONE il 30/08
  (push a2a53a5..f3dfeef su camoscio/master), confermati da Denis dal
  vivo.

  PUNTO 114 — IMPORT FILE .FIT (formato nativo Garmin) nello storico.
  lib/fit.js NUOVO: parseFit(buffer) restituisce la STESSA forma di
  parseGpx -> tutto il codice a valle invariato. Dipendenza nuova
  fit-file-parser@5.0.2 (MIT, +buffer, 0 vulnerabilita' nuove): il FIT
  e' binario, il divieto di librerie sui .gpx era anti-XXE e non vale.
  Ambito: i due punti dello storico (/import-gpx e il ⬆ di
  completions.js). importedFrom resta 'gpx' (= "importato da file", non
  il formato). Il .fit viaggia in base64 (window.fileToBase64 in app.js).
  prove/prova-fit.js NUOVO (33, senza server). Commit 0837f42.
  >>> LIMITE, e' il FILE non il codice: un .fit "smart" (o da Garmin
      Connect) ha gli stessi buchi di un GPX degradato -> niente split
      cammino/pause. Serve un .fit a 1 Hz. Distanza/dislivello/durata
      totale/badge: sempre giusti.

  PUNTO 115 — NOME DELL'USCITA, MODIFICABILE. ActiveHikeSession
  .importedName diventa il nome editabile (non solo il <name> del file),
  max 120, per QUALSIASI uscita; assente -> data. PATCH
  /api/tracking/sessions/:id/name NUOVA (owner, vuoto -> $unset);
  publish accetta anche {name}. Frontend: MATITA in "Le mie escursioni"
  su ogni riga uscita (anche le "escursione completata" con traccia dal
  ⬆ - aggiunto dopo feedback di Denis); tasti pubblica/rinomina delegati
  (data-*, non onclick inline); titolo dell'uscita in cima alla pagina
  di dettaglio (NUOVO). completions.js: nome di default della traccia
  collegata = <name> del file o il titolo dell'escursione. prove/
  prova-punto115.js NUOVO (28) + sez.6 in prova-punto113-gpx.js (27->35).
  Commit e13ca9a + f3dfeef.
  Dettaglio: cose_da_fare.txt "Quattordicesima sessione"; decisioni nel
  vault 03 "Punto 114"/"Punto 115"; racconto nel vault 06.

  CONTROLLO FATTIBILITA' SMARTWATCH (Garmin/Suunto/Amazfit) fatto e
  SCARTATO: Garmin blocca il programma developer (solo persone
  giuridiche + sospeso), Amazfit non ha API, Suunto e' possibile ma
  marginale, Strava vieta di mostrare i dati altrui. Non riproporre
  "colleghiamo Garmin". Dettaglio in vault 05-Idee-Facoltative.

  COSMETICO noto sul 115 (non deciso di sistemarlo): su una riga
  "escursione completata" con traccia collegata, la card in "Le mie
  escursioni" mostra il TITOLO dell'escursione; la matita rinomina il
  nome nel FEED / pagina di dettaglio. Due nomi se li fai diversi (il
  default li tiene allineati).

  >>> DENIS DEVE RICARICARE il .gpx/.fit col ⬆ per le escursioni gia'
      fatte che vuole pubblicare (l'originale non era stato conservato).

  CODA: controllo dimensioni file. Le 33 icone badge (public/img/) erano
  46 MB, l'88% del repo (salvate a 1024-1254 px, mostrate a 80-90 px).
  Ridimensionate a 768 px PNG senza perdita con scripts/comprimi-badge.js
  (NUOVO, sharp): 46 -> 8,8 MB, zero codice toccato, originali in
  ../camoscio-badge-originali/ (fuori dal repo). Commit 084f992.
  NON fatto: sgonfiare il .git da 89 MB (git filter-repo, delicato su
  repo pubblico); WebP; alleggerire lucide.js. Non stanno su MongoDB:
  era peso di pagina + repo, non il vincolo 512 MB.

  CODA (commit 0d0ecc3, solo frontend): verificato dal vivo il permesso
  di geolocalizzazione del browser (finestrella nativa - Denis: "provato
  tutto, funzionante"). Provando sono uscite 3 cose, sistemate:
  A) la guida allo sblocco GPS su PC mostrava le istruzioni Android ->
     aggiunta la variante desktop (geolocation.js, geo.guidaDesktop).
  B) la registrazione GPS partiva sempre su "Ascesa Corno Grande":
     app.js metteva activeHikeId su hikes[0] (prima escursione del DB di
     chiunque) -> tolto. Ora parte da "Nessuna - traccia libera".
  C) lo Zaino era bloccato sulla prima/prossima escursione senza scelta:
     aggiunto un <select> ("Automatico" + optgroup "Organizzate da me" /
     "A cui partecipo", solo IN PROGRAMMA + "Zaino personale"). Tolto
     anche il ripiego sull'ultima escursione passata.

>>> GIA' IN PRODUZIONE (sessioni precedenti): Punto 113 (parte social:
    Segui, Feed, uscita apribile, "mi piace", "crea percorso"); Punto 111
    (segnalazioni sentiero: "risolvi" e scadenza a 90gg passano dal
    moderatore); Punto 108 (un timbro richiede una traccia reale entro la
    soglia). Dettaglio: cose_da_fare.txt punti 113/111/108, vault 03/06/07.

>>> ALTRE APERTE, non urgenti (dettaglio in vault 04-Da-Fare.md):
    livelli ORIGINALI del punto 63b (gradini per la ZONA dichiarata a
    mano, DIVERSI dai livelli gia' fatti del punto 86) + calcolo
    automatico del raggio (punto 42a) - Denis deve ancora pensarci, non
    richiederlo; dominio email proprio (rimandato da lui, "non ora").

>>> RESTA VALIDO:
  - Hook "Stop" di auto-save: commit locale (MAI push) a fine di ogni
    risposta se ci sono modifiche non salvate in questo repo. Va rifuso
    in un commit vero prima di ogni push (git reset --soft sull'ultimo
    commit vero se gli auto-save sono piu' di uno) - fatto per i passi
    7-10 (b7afa35, reset --soft 629476f sui 4 auto-save).
  - Traduzione IT/EN (punto 102, CHIUSO): resta ITALIANO di proposito
    tutto il testo scritto dagli utenti (didascalie del feed comprese),
    i nomi propri di luogo, i nomi dei percorsi salvati e l'etichetta
    "da <autore>", titolo/descrizione dei badge personali, le schede "i"
    dei badge (punto 112), il testo delle notifiche (lo genera il
    server), il segnaposto nativo di <input type=date>/<input type=file>,
    e componiRapporto() dentro diagnostica-gps.html. Le chiavi EN dei
    punti 113/114/115 sono state aggiunte tutte. Restano IT anche i NOMI
    delle uscite (punto 115: li scrive l'utente).
  - Punti 102, 106, 108, 111, 112, 113, 114, 115 CHIUSI E IN PRODUZIONE.

===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo,
    vedi 04-Da-Fare.md del vault).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — congelato dal 6/08/2026: da quella data il racconto
    disteso di ogni sessione vive solo in ../camoscio memoria/06-Cronologia-Sessioni.md,
    non qui. Utile solo per il dettaglio di lavoro PRIMA di quella data.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
MAI piu' (congelato); cose_da_fare.txt quando un punto si chiude o ne arriva uno nuovo.

>>> I PUNTI CHIUSI FINO AL 103 (piu' 94 e 98) non sono piu' elencati qui: erano
    diventati un appello di 190 righe, cioe' esattamente "qualcosa che appartiene a
    un altro file". Tutti chiusi, committati e pushati, molti confermati dal vivo da
    Denis. Il dettaglio con le sue parole e' in cose_da_fare.txt (cercare il numero);
    il racconto per sessione in ../camoscio memoria/06-Cronologia-Sessioni.md.
    Compressione fatta il 28/08/2026, stesso criterio dello sfoltimento del 18/08.

>>> LEZIONI OPERATIVE (git dopo un'interruzione, moduli che si collegano
    tardi, componenti condivisi fra piu' pagine, trappole Android/Gradle):
    tutte in ../camoscio memoria/07-Trappole-Tecniche.md, non ripetute qui -
    quella e' la sede dove si guarda prima di toccare codice a rischio.
===================================================================
