===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-09-02 (VENTIDUESIMA sessione — BLOCCO 0 (zaino/carpooling
per-partecipanti) + TUTTO IL LOTTO SICUREZZA (C-1/C-2/C-3/A-1/A-2/A-3.1-3)
+ i FIX della ri-review dell'agente security-privacy (1 critico + 6 alti +
5 medi) CODIFICATI, verificati in locale, committati su `v2-ux` (auto-save),
NIENTE pushato. La ri-review (1o giro) aveva dato "NON pronto": tutti i
blocchi chiusi in sessione, il critico era C-NUOVO-1 (`PUT /api/hikes/:id`
con update vuoto in Mongoose degrada a LETTURA e restituisce il doc intero).
Suite prove/ non-Puppeteer COMPLETA VERDE (~950 controlli). Denis ha fermato
la sessione qui: la 23a PARTE DAL 2o GIRO DELL'AGENTE security-privacy
(verifica dei fix), poi prova mobile + responsive <900px + merge.
Dettaglio completo: vault 04-Da-Fare ("Ri-review 22a"), 06 ("Ventiduesima
sessione"), 03 (nuovi paragrafi), 07 (3 trappole nuove).)

>>> LAVORO IN CORSO: revisione UX del sito ("Camoscio v2"). Si lavora
    ESCLUSIVAMENTE IN LOCALE sul branch `v2-ux`. NIENTE push, NIENTE deploy:
    Denis manda online solo quando l'intera v2 lo convince. `master` (su
    `origin` = `efd1290`) resta intatto = la produzione attuale.
    Metodo: un commit vero `V2 UX PASSO n: ...` per blocco (locale su
    `v2-ux`). L'hook di auto-save committa comunque: si rifonde (rebase
    --onto / reset --soft) in un commit vero (la rifusione dei ~13 auto-save
    di questa sessione la fa Denis). La DASHBOARD (Fasi 1-5), il RIORDINO
    MENU/NAVIGAZIONE (PASSO 6->14c), il BLOCCO 0 (zaino/carpooling
    per-partecipanti) e TUTTO IL LOTTO SICUREZZA sono FATTI. `v2-ux` = 33
    avanti / 1 indietro a `origin/master` (`efd1290` Ju Busciu, il merge lo
    riconcilia).

>>> FATTO E COMMITTATO su `v2-ux` (NON pushato). Struttura finale:
    Home / Esplora v (Escursioni, Mappa, Ispirazioni) / Le mie escursioni v
    (Tutte, In programma, Completate) / Community v (Amici) / + Crea /
    AREA PERSONALE: Progressi, Profilo, Impostazioni, Sicurezza.

  DASHBOARD (Fasi 1-5, `7915762`..`309ab92`) + PASSO 6 (`7f8b697`, header =
  voce di menu) + PASSO 7 (`e33be1b`, sidebar a gruppi a fisarmonica +
  rinomine). Dettaglio nel vault 06 ("Sedicesima"..."Ventesima sessione").

  RIORDINO MENU (21a sessione, 10 commit `4ea24df`..`77fcaf8` sopra
  `a34b5f1`):
  - PASSO 9  — "Le mie escursioni" a 3 viste-filtro (Tutte / In programma /
    Completate, attributo data-view + CSS); #hikes (Esplora>Escursioni) =
    solo scoperta. `classificaMieEscursioni` intatta.
  - PASSO 10 — Community diventa gruppo con sotto-voce "Amici"; la ex
    sezione #people-search RITIRATA, la ricerca per nome utente e' nella
    card "Persone" di #social (id invariati, peoplesearch.js non cambia);
    navigateTo alias people-search -> social + data-scroll generico.
  - PASSO 11 — #badges -> #progress ("Progressi"), TUTTI gli id figli
    invariati (renderBadges non si tocca) + nuovo blocco "Il tuo cammino
    per zona" (progressoZone({tutte:true})). #badges resta alias.
  - PASSO 11b — statistiche personali filtrabili in #progress: intervallo
    date + 3 intervalli rapidi. UNICO tocco backend: `GET
    /api/tracking/totals` ora accetta anche `?from=&to=` (oltre `?anno=`).
    Nessun modello/indice/migrazione.
  - PASSO 12 — #my-profile SDOPPIATA: "Profilo" = sola vista (bio + esperto
    read-only); "Impostazioni" (#settings, NUOVA) = foto/bio/esperto/
    password/lingua/logout + card placeholder inerte. I controlli SPOSTATI,
    id invariati (renderProfileCard/setupProfileCardEvents non cambiano).
  - PASSO 13 — "+ Crea" apre un popover (2 voci: nuova escursione / nuovo
    percorso). Flip verso l'alto se sfora il fondo della .nav-menu.
  - PASSO 8  — DRAWER MOBILE (<=900px): hamburger nell'header + sidebar
    off-canvas + scrim. Riscritta la @media <=900 (via il rail 62px + la
    regola-ponte di PASSO 7). Bandiere lingua -> in fondo al drawer.
    VERIFICATO SOLO IN SIMULAZIONE (resize_window inaffidabile): PROVA VERA
    SUL TELEFONO DI DENIS AL DEPLOY.
  - PASSO 14a — via il blocco "STRUMENTI"; "Sicurezza" (era "& Mesh") =
    foglia dell'AREA PERSONALE. #safety internamente invariato.
  - PASSO 14b — Carpooling = TAB dentro hike-page (4 tab: Dettagli/Chat/
    Carpooling/Zaino). Scelta di Denis = OPZIONE A: il tab e' SOLO
    per-escursione (annuncio della singola uscita, non piu' "tutte";
    "Offri un Passaggio" senza selettore escursione; Split Spese generico
    resta). #carpool .page-section RITIRATA. carpool.js legato all'hikeId.
  - PASSO 14c — Zaino = TAB dentro hike-page. Niente piu' selettore
    "prepara lo zaino per" ne' "zaino personale" (Q6): contesto sempre =
    l'escursione del tab. #backpack .page-section RITIRATA. backpack.js
    legato all'hikeId (escursioneDiRiferimento semplificata).

  Verifica: ogni blocco provato dal vivo (account reale, sola lettura, DB
  demo dove serviva, IT+EN, zero errori JS). Sweep finale nav desktop: ok.

  BLOCCO 0 (zaino/carpooling per-partecipanti) - piano
  camoscio-zaino-carpool-partecipanti.md, ESEGUITO in 2 checkpoint:
  - Hike.multiDay (Boolean, default:undefined) - lo decide il CREATORE
    (spunta in create-hike-modal). Zaino personale SEMPRE in localStorage,
    non passa dal server. "Zaino condivisibile" (hike.backpackTemplate) +
    ripartizione pesi SOLO se hike.multiDay. Via il <select> durata.
  - routes/hikes.js: gate isHikeParticipant sui rami carpool/backpackTemplate
    di PUT /:id (C-2); !hike.multiDay -> 403 sui write zaino; GET / strippa
    carpool/backpackTemplate per i non-partecipanti; sotto-bug passeggeri
    (sulla propria voce driver possono solo calare).
  - hikepage.js+CSS: tab Chat/Carpooling/Zaino nascosti ai non-partecipanti
    (A-1, difesa in profondita').

  LOTTO SICUREZZA (decisioni con AskUserQuestion):
  - C-1: 'homeCity' in ALWAYS_PRIVATE_FIELDS + maxlength 100; GET
    /api/hikes/:id/home-match (solo partecipanti) risponde SOLO {quanti:N}
    - MAI i nomi (coi nomi era un oracolo sulla zona di casa).
  - C-3: mesh WS -> log ridotto a parsed.type; broadcast SOLO ai
    co-partecipanti (Hike non conclusa); mittente/nome dal cookie di
    sessione, non dal pacchetto; lat/lng validati -> null; cap 20 msg/10s.
  - A-2: express-rate-limit@8.7.0 (MIT, DIPENDENZA NUOVA approvata da Denis);
    middleware/rateLimit.js, tutti i limiter GATED su NODE_ENV=='production'
    (skip in dev/test). npm audit --omit=dev = 0 (spostato @capacitor/cli
    in devDependencies).
  - A-3.1: revoca consenso GPS in #settings + ENFORCEMENT server-side
    (middleware/consensoGeo.js su tracking.js /start,/:id/points,/:id/resume).
  - A-3.2: RIMOSSO deadManContactIndex (0 utenti). Alla scadenza l'allarme
    va a TUTTI i contatti con email. safety.js: lista contatti con "Rimuovi"
    per riga. Demo NON armano il DMS. Email HTML escapata + tetto 5 contatti.
    prova-punto37 riscritta (account reale temporaneo, non demo).
  - A-3.3: GET /api/users/me/export -> bundle JSON scaricabile da #settings
    (exportLimiter 3/giorno, .select('-points -offTrailBuffer'), no indent).

  FIX DELLA RI-REVIEW (1o giro = "NON pronto", tutti chiusi):
  - C-NUOVO-1 (CRITICO): PUT /api/hikes/:id con update {} -> Mongoose
    findByIdAndUpdate degrada a _findOne() e RESTITUISCE IL DOC INTERO
    (riapriva C-2 lettura + C-1 + A-1 + punto 77). -> hikeVisibileA()
    unica su GET / E risposta PUT /:id; 400 su update vuoto PRIMA di
    findByIdAndUpdate; 404 sulle concluse ai non-partecipanti nel PUT.
  - A-NUOVO-1: DMS come relay email verso terzi (demo + N contatti-HTML).
    -> esc() HTML + tetto 5 contatti + demo fuori dal DMS + scritturaLimiter.
  - A-NUOVO-2: home-match era un oracolo -> solo {quanti}, no nomi.
  - A-NUOVO-3: export = DoS memoria -> .select senza points + exportLimiter.
  - A-NUOVO-4: runValidators NON valida i sotto-doc degli array su
    findByIdAndUpdate -> lunghezze validate NELLA rotta; maxlength su
    Hike.title/description; scritturaLimiter su POST /api/hikes.
  - medi: M-1 mesh anti-spoofing, M-2 log NODE_ENV all'avvio, M-3 export
    minimizza le proprie hike, M-4 capacitor/cli in devDeps.
  RIMANDATI (non bloccanti, decide Denis): A-3.4 elimina account (blocco a
  se', scelta = chiusura + scrub PII + tombstone); M-5 race cancella-contatto
  con 2 schede; NOTA LEGALE - l'accettazione "Termini + Informativa privacy"
  in index.html non ha un documento dietro (serve un professionista).

>>> DA DOVE SI RIPARTE: la 23a sessione PARTE DA QUI, in ordine (Denis ha
    fermato la 22a prima del punto 1):

  1. 2o GIRO DELL'AGENTE `security-privacy` - verifica che i fix chiudano
     davvero i 6 blocchi + niente di nuovo. Diff da rivedere:
     `git diff 4f39fb1`. Il prompt del 1o giro e' nel vault 06
     ("Ventiduesima sessione"). L'agente ha memoria in
     .claude/agent-memory/security-privacy/.
  2. PROVA MOBILE sul telefono di Denis (drawer, tab di hike-page,
     #settings nuovo, layout stretto). Senza deployare: server sulla LAN
     (cookie secure:false in locale, login regge in HTTP) -> telefono
     stesso wifi http://<ip-pc>:3000. Fallback: tunnel effimero.
  3. Verifica responsive Dashboard <900px (ex "punto 3").
  4. MERGE `v2-ux` -> `master` + push (lo decide Denis). `v2-ux` = 33
     avanti / 1 indietro a `origin/master` (`efd1290` Ju Busciu `sogliaM`)
     - il merge lo riconcilia; v2-ux NON tocca geofenceTimbri.js/
     badge-points.js/map.js/stamps.js quindi atteso conflict-free.
     Backup pre-rifusione: `v2-ux-backup-preref`. La rifusione dei ~13
     auto-save in commit veri `V2 UX ...` la fa Denis.

  FILONE B (indipendente) - COLLEZIONE "I 2000 DELL'APPENNINO", dentro la
  pagina Progressi (ex Badge), NON pagina a se'. Piano:
    C:\Users\lenovo\.claude\plans\camoscio-2000-appennino-collezione.md
  Bloccato: LA LISTA UFFICIALE DELLE VETTE LA FORNISCE DENIS. Punto aperto:
  icone/peso repo (~100 vette) - raccomandazione: 1 grafica "2000+" unica.

>>> DOVE STA IL DETTAGLIO: i PIANI in C:\Users\lenovo\.claude\plans\ -
    camoscio-zaino-carpool-partecipanti.md (IL PROSSIMO BLOCCO, da fare
    per primo nella 22a), delegated-gathering-forest.md (il piano
    esecutivo file-per-file di 9->14c, ESEGUITO), camoscio-v2-menu-
    restructure.md (decisioni + §15), camoscio-2000-appennino-collezione.md
    (filone B). Nel vault: 04-Da-Fare ("PROSSIMO GRANDE LAVORO" + "Da dove
    si riparte" + "Esito agente security-privacy"); decisioni in 03
    ("Camoscio v2 — riordino menu e navigazione", con l'opzione A del 14b e
    il modello zaino/carpool per-partecipanti); parole di Denis in
    cose_da_fare.txt ("VENTUNESIMA SESSIONE"); racconto nel 06
    ("Ventunesima sessione"). In 07 le trappole nuove (scroll "smooth"
    interrotto dai render async; race di risposte in un fetch con debounce).

>>> GIA' IN PRODUZIONE (fino alla 15a sessione, su `master`, ora
    `origin` = `efd1290`): traduzione IT/EN (punto 102), timbri con traccia
    reale (108), moderazione segnalazioni sentiero (111), parte social —
    Segui/Feed/"mi piace"/"crea percorso" (113), import .fit (114), nome
    uscita editabile (115), + 4 rifiniture del 31/08, + hotfix Ju Busciu
    `sogliaM` (17a sessione, `efd1290`). Dettaglio: cose_da_fare.txt, vault.
    >>> LIMITE .fit/.gpx, e' il FILE non il codice: un file "smart" (o da
        Garmin Connect) ha buchi -> niente split cammino/pause. Serve 1 Hz.
    >>> DENIS DEVE RICARICARE il .gpx/.fit col ⬆ per le escursioni gia'
        fatte che vuole pubblicare (l'originale non era stato conservato).
    >>> Smartwatch (Garmin/Suunto/Amazfit): valutato e SCARTATO.

>>> ALTRE APERTE, non urgenti (dettaglio in vault 04-Da-Fare.md):
    livelli ORIGINALI del punto 63b (gradini per la ZONA dichiarata a
    mano, DIVERSI dai livelli gia' fatti del punto 86) + calcolo
    automatico del raggio (punto 42a) - Denis deve ancora pensarci, non
    richiederlo; dominio email proprio (rimandato da lui, "non ora").

>>> RESTA VALIDO:
  - Hook "Stop" di auto-save: commit locale (MAI push) a fine di ogni
    risposta se ci sono modifiche non salvate in questo repo. Va rifuso
    in un commit vero prima di ogni push/tappa.
  - Traduzione IT/EN (punto 102, CHIUSO): resta ITALIANO di proposito
    tutto il testo scritto dagli utenti (didascalie del feed comprese),
    i nomi propri di luogo, i nomi dei percorsi salvati e l'etichetta
    "da <autore>", titolo/descrizione dei badge personali, le schede "i"
    dei badge (punto 112), il testo delle notifiche (lo genera il
    server), il segnaposto nativo di <input type=date>/<input type=file>,
    e componiRapporto() dentro diagnostica-gps.html. Le chiavi EN dei
    blocchi v2 UX si aggiungono mano a mano (scelta di Denis).

===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo,
    vedi 04-Da-Fare.md del vault).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — congelato dal 6/08/2026: da quella data il racconto
    disteso di ogni sessione vive solo in ../camoscio memoria/06-Cronologia-Sessioni.md,
    non qui. Utile solo per il dettaglio di lavoro PRIMA di quella data.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
MAI piu' (congelato); cose_da_fare.txt quando un punto si chiude o ne arriva uno nuovo.

>>> I PUNTI CHIUSI FINO AL 103 (piu' 94 e 98) non sono piu' elencati qui: erano
    diventati un appello di 190 righe, cioe' esattamente "qualcosa che appartiene a
    un altro file". Tutti chiusi, committati e pushati, molti confermati dal vivo da
    Denis. Il dettaglio con le sue parole e' in cose_da_fare.txt (cercare il numero);
    il racconto per sessione in ../camoscio memoria/06-Cronologia-Sessioni.md.
    Compressione fatta il 28/08/2026, stesso criterio dello sfoltimento del 18/08.

>>> LEZIONI OPERATIVE (git dopo un'interruzione, moduli che si collegano
    tardi, componenti condivisi fra piu' pagine, trappole Android/Gradle):
    tutte in ../camoscio memoria/07-Trappole-Tecniche.md, non ripetute qui -
    quella e' la sede dove si guarda prima di toccare codice a rischio.
===================================================================
