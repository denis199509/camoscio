===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-08-07
===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo,
    vedi 04-Da-Fare.md del vault).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — l'ex leggimi. Il racconto disteso di ogni lavoro fatto,
    con le misure. Ci si va quando serve il dettaglio di qualcosa di gia' fatto.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
solo se c'e' un lavoro nuovo da raccontare; cose_da_fare.txt quando un punto si chiude
o ne arriva uno nuovo.



>>> DIECI PUNTI CHIUSI IN QUESTA SESSIONE (iniziata il 06/08, proseguita dopo un
    riavvio imprevisto del PC fino al 07/08 - nessun lavoro perso, ogni volta):
    58 (deploy rimasto indietro), 61, 54, 57, 63a, 59, 7, 42b, 43, 64. Il punto 43 ha
    anche riattivato il 44 (tempo previsto CAI), gia' chiuso a inizio agosto e mai
    toccato di nuovo. TUTTO PUSHATO E VERIFICATO LIVE SU RENDER. Dettaglio di ogni
    punto in cose_da_fare.txt e in ../camoscio memoria/06-Cronologia-Sessioni.md.

>>> PROSSIMA SESSIONE: SI RIPARTE DIRETTAMENTE COL PUNTO 37 (richiesta esplicita di
    Denis). GIA' PREPARATO il 07/08/2026, non ancora scritto codice: PRIMA DI TOCCARE
    NIENTE, fare a Denis le due domande scritte in cose_da_fare.txt punto 37 (e in
    ../camoscio memoria/04-Da-Fare.md) - (1) i contatti di emergenza non hanno un
    campo email, il canale scelto per l'allarme e' l'email; (2) questo progetto non
    ha nessuno scheduler, quindi va deciso insieme come far scattare un controllo
    anche a sito addormentato (Render gratuito). Dopo il 37, ordine gia' concordato:
    48 (ancora vago, serve un esempio da Denis prima di progettare), 45, 55.

>>> PUNTO 52 - CHIUSO IL 07/08/2026, in una sessione successiva a quella dei dieci punti
    sopra: Denis ha fatto la prova dal telefono (tasto di pagina perfetto, tasto/gesto
    DEL TELEFONO usciva dal sito) e ha chiesto di farlo comportare come quello di pagina.
    CORRETTO in public/js/auth.js: prima volta che il sito usa davvero la History API
    (entry-sentinella che intercetta l'indietro vero e sposta la fase, si riarma da
    sola). Provato dal vivo con l'estensione Chrome (dati mai persi, anche mischiando
    tasto di pagina e indietro del telefono), piu' prova-configurazione.js (9/9) e
    prova-punto7.js (53/53) di controllo, nessuna regressione. COMMITTATO E PUSHATO
    (fd5c4cf camoscio, 27c220b vault), verificato live su Render. Dettaglio completo in
    ../camoscio memoria/03-Decisioni-Architetturali.md e
    ../camoscio memoria/06-Cronologia-Sessioni.md.

>>> PUNTO 39 - CHIUSO IL 07/08/2026, stessa sessione del punto 52: mappa base cambiata
    da OSM standard a OpenTopoMap in public/js/map.js (curve di livello, rilievo
    ombreggiato, sentieri con nome e grado CAI - prima quasi invisibili), URL
    condivisa con public/js/trailhead-picker.js invece di ricopiata
    (CAMOSCIO_TILE_URL/OPTIONS). Le due correzioni alla cache offline previste dal
    29/07 fatte insieme, in public/js/offline-map.js: tileKey() ora include lo stile
    (niente piu' collisione tra mappe diverse), estimateOfflineDownloadSize usa 32
    KB/tile invece di 20 (il peso vero misurato). Provato dal vivo con l'estensione
    Chrome su Corno Grande a piu' zoom (incluso il massimo, 17, nessuna tile rotta),
    sulla mini-mappa del ritrovo, e controllando IndexedDB a mano (tile vecchie e
    nuove non si mescolano). COMMITTATO E PUSHATO (65053cf camoscio, d6eb99e vault),
    verificato live su Render. Dettaglio completo in
    ../camoscio memoria/03-Decisioni-Architetturali.md e
    ../camoscio memoria/06-Cronologia-Sessioni.md.

>>> PUNTO 65 - CHIUSO IL 07/08/2026: Denis, dopo aver visto la mappa nuova,
    ha segnalato che le notifiche del campanello "non sono bene leggibili perche' una
    parte a destra viene coperta" (vista dal telefono). RIPRODOTTO NEL BROWSER senza
    bisogno del telefono: non e' un problema di schermo, e' di CONTENUTO. Una notifica
    con una parola sola senza spazi (tipico di uno username, scelto apposta senza
    spazi in registrazione) non andava mai a capo, usciva dal riquadro di 320px e
    veniva tagliata a destra (overflow:hidden del contenitore) - il trigger piu'
    probabile e' "USERNAME ha chiesto di partecipare alla tua escursione TITOLO"
    (routes/hikes.js). CORRETTO in public/css/styles.css: overflow-wrap: anywhere su
    .notification-item - stessa proprieta' gia' in uso altrove nel foglio di stile
    (es. .outing-card-title) per lo stesso identico problema, solo mai applicata alle
    notifiche. Provato dal vivo (testo-parola-sola finto e caso realistico con
    username lungo): entrambi vanno a capo dentro il riquadro dopo la correzione,
    nessuna barra di scorrimento, nessun testo tagliato; la notifica vera di Marco
    resta invariata. COMMITTATO E PUSHATO (ec36887 camoscio, bc91141 vault),
    verificato live su Render. NON BASTAVA DA SOLO: vedi punto 66 subito sotto,
    stessa notifica, difetto diverso. Dettaglio completo in cose_da_fare.txt
    punto 65 e in ../camoscio memoria/06-Cronologia-Sessioni.md.

>>> SESSIONE INTERROTTA QUI per cause sconosciute (non un PC spento: i due
    repository risultavano gia' entrambi puliti e pushati riaprendo dopo). Le
    NOTE pero' erano rimaste ferme a un istante prima del push del 65 - dicevano
    ancora "sessione aperta" e "non committato". LEZIONE: dopo un'interruzione,
    controllare SEMPRE `git log origin/master..HEAD` in ogni repository PRIMA di
    fidarsi dell'ultima riga scritta - una nota puo' invecchiare anche dentro la
    stessa sessione, non solo da una sessione all'altra. Vedi
    ../camoscio memoria/07-Trappole-Tecniche.md.

>>> PUNTO 66 (NUOVO) - CHIUSO IL 07/08/2026, sessione di ripresa: Denis ha
    riaperto il sito e segnalato via screenshot dal telefono che il problema
    delle notifiche sembrava lo stesso di prima - ma stavolta il testo mancava
    dall'INIZIO di ogni riga ("ncora completato" invece di "Non hai ancora
    completato"), non dalla fine: difetto diverso, non risolvibile dal punto 65
    (gia' verificato live, vedi sopra). CAUSA VERA: non il testo, il RIQUADRO
    stesso. .notification-dropdown e' position:absolute, right:0 sotto al
    campanello, largo 320px fissi; su schermo stretto la sidebar si riduce a
    62px di sole icone e il campanello finisce vicino al bordo sinistro - il
    riquadro sfonda oltre il bordo di .main-content (overflow:hidden sulla
    propria colonna, styles.css riga 201), che taglia via la parte in eccesso,
    testo compreso. Misurato dal vivo (estensione Chrome, iframe della stessa
    origine ridimensionato a mano - resize_window non affidabile, confermato di
    nuovo): 17-79px tagliati a seconda della larghezza. Tentativo intermedio
    scartato: solo position:fixed non bastava, perche' .top-header ha
    backdrop-filter e in Chrome questo diventa blocco di contenimento anche per
    i discendenti fixed - il riquadro restava comunque intrappolato nella
    stessa colonna. CORRETTO spostando #notification-dropdown a figlio diretto
    di <body> (public/js/app.js, setupNotificationBell()), fuori da entrambi
    gli antenati: la posizione si ricalcola dal campanello vero a ogni
    apertura, stessa dimensione di sempre (richiesta esplicita di Denis: "la
    grandezza va bene, si deve solo vedere sopra tutto"), con un margine minimo
    di 8px dal bordo invece di uscire dallo schermo nei casi piu' stretti.
    Provato dal vivo con server locale, misurato con getBoundingClientRect()
    reale a 320/360/390/430/700/1536px: nessun taglio in nessun caso, posizione
    su schermo largo identica a prima; confermato anche visivamente con uno
    screenshot a 386px. COMMITTATO E PUSHATO (0dbfac7 camoscio), verificato
    live scaricando app.js vero da camoscio.onrender.com. Dettaglio completo in
    cose_da_fare.txt punto 66.

>>> DUE COSE DA SAPERE PRIMA DI TOCCARE public/index.html (trovate in questa sessione,
    non ovvie a prima vista):
      - create-hike-modal e complete-group-modal vivono FUORI da .page-container
        (vicino a generic-modal), non dentro una .page-section: un modale raggiungibile
        da piu' di una pagina (es. sia "Escursioni" sia "Le mie escursioni") deve stare
        li' con loro, altrimenti resta invisibile aprendolo dalla sezione "sbagliata" -
        bug vero trovato su create-hike-modal, in piedi dal punto 54 senza che nessuno
        se ne accorgesse;
      - un modulo che si collega SOLO da dentro initApp() (dopo await refreshState())
        rischia la stessa finestra "pulsante visibile ma senza listener" gia' trovata
        due volte (navigazione il 26/07, profilo il 06/08) - se un pulsante sembra
        rotto senza nessun errore in console, controllare prima QUANDO si e' agganciato
        il suo ascoltatore, non se il codice del gestore e' giusto.



>>> STATO A FINE SESSIONE (2026-08-07, dopo la ripresa dall'interruzione e la
    chiusura del punto 66 - controllo di fine sessione fatto):
      - camoscio: punti 52 (fd5c4cf), 39 (65053cf), 65 (ec36887) e 66 (0dbfac7)
        TUTTI committati e pushati, tutti verificati live su Render (file veri
        scaricati da camoscio.onrender.com, non solo home a 200);
      - camoscio memoria (vault): punti 52 (27c220b), 39 (d6eb99e) e 65 (bc91141)
        committati e pushati. Il punto 66 va ancora scritto qui nel vault (04 e 06)
        e committato - e' l'ultima cosa rimasta di questa sessione;
      - controllo con le prove automatiche esistenti: prova-configurazione.js 9/9
        rilanciata dopo la ripresa - nessuno dei quattro punti di questa sessione
        tocca una rotta server o un modello, solo file statici lato client (auth.js,
        map.js/offline-map.js/trailhead-picker.js, styles.css, app.js);
      - nessun account/dato di prova lasciato nel database: punti 52/65/66 sono solo
        front-end (mai inviata una registrazione, solo DOM temporaneo nel browser -
        per il 66 anche su server locale, mai sul sito vero); il punto 39 ha lasciato
        per sbaglio una sessione di tracciamento GPS vuota (36s, 0 km) - cancellata
        subito con DELETE /api/tracking/sessions/:id, le altre sessioni demo di Marco
        ricontrollate intatte;
      - nessun server locale acceso, porta 3000 verificata libera (stop del task piu'
        taskkill sul PID preso da netstat, non basta lo stop da solo su Windows) -
        avviato e fermato piu' volte in questa sessione (anche nella parte ripresa
        dopo l'interruzione, per il punto 66), stessa procedura ogni volta.
