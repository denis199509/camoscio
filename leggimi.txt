===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-09-06 (TRENTAQUATTRESIMA sessione — DUE blocchi. (1) FASCIA
A DELL'AUDIT VISIVO CHIUSA: A9, A10, "(da B4)" .settings-placeholder, uno
alla volta col via di Denis, verificati dal vivo. (2) Poi il grosso: il
problema "Marco non puo' chiudere/recensire l'escursione di ieri" - scavato
fino alla causa, 4 modifiche al codice + un fix-dati mirato, TUTTO PUSHATO E
IN PRODUZIONE (origin/master = `18af140`). SI RIPARTE DAL PUNTO 1: a fine
tracciamento collegato a un'escursione, aprire la checklist "chi era con
me" (come complete-group) invece di completare solo se stessi in silenzio -
e' la causa vera, quello di stasera ha sistemato il caso singolo. Denis:
"lo affrontiamo in un'altra sessione". Restano anche: Fascia B/C mai
proposte, verifica al telefono di A6/A7/A8/A9/A10 insieme.)

--- TASTO INDIETRO / HISTORY API (33a): FATTO, PUSHATO (`7793426`) ---
Il tasto Indietro non tornava indietro, buttava fuori dal sito. Riscritto
il nucleo di navigazione in public/js/app.js (navigateTo unico a pushare,
un solo gestore popstate, registro modali apri/chiudi/scollegaModaleStorico)
e verificato dal vivo. Tre bug trovati e corretti in corso. Dettaglio:
cose_da_fare.txt "TASTO INDIETRO / HISTORY API". Piano aggiornato:
C:\Users\lenovo\.claude\plans\camoscio-tasto-indietro-history-api.md

--- RITOCCHI VISIVI FASCIA A (33a-34a): CHIUSA ---
Lavoro preparato dall'audit dell'agente designer (rapporto:
C:\Users\lenovo\.claude\plans\camoscio-audit-visivo-29a.md). Fatto UN PUNTO
ALLA VOLTA col via esplicito di Denis a ognuno; ogni punto verificato dal
vivo (misure/colori renderizzati via javascript_tool, non a occhio), CSS
sempre a graffe bilanciate. Solo public/css/styles.css, tranne A5 (anche
social.js/squadpage.js/storico.js) e A2/A8 (anche index.html).

  A1  19 regole icona morte (<i> -> "<sel> i, <sel> svg") + generica
      ".btn i,.btn svg{width:16px;height:16px;flex-shrink:0}".
  A2  6 classi utility fantasma dichiarate (.text-center/.text-secondary/
      .py-2/3/4/.col-span-2), ~55 usi.
  A3  .btn-success:hover da verde neon #0D9F6E a #3D6337 (scurimento in
      tinta). Contrasto bianco 3,39->6,91:1.
  A4  .hike-card-title da #FFF a var(--color-text-primary).
  A5  20 bottoni con style="padding:2px..." inline (~23px di tocco) ->
      classe .btn-xs (~30px misurati), style rimosso da tutti e 20.
  A6  .notification-dropdown / .toast-container con min(Npx,calc(100vw-Mpx))
      per non sporgere sotto ~380px.
  A7  .hikes-header-row -> flex-wrap:wrap; gap:12px.
  A8  .form-row-ratings / .checkbox-group a 1 colonna sotto i 900px (+
      style inline a 3 col tolto da index.html); .rating-group{min-width:0}
      fuori dalla media.
  A9  .safety-contact-row (era SENZA css) -> riga flex con separatore,
      stesso linguaggio di .veto-request-item; .safety-contact-info
      flex:1+min-width:0+overflow-wrap:anywhere (email lunga va a capo
      dentro la card); .btn-rimuovi-contatto flex-shrink:0. Verificato dal
      vivo a 325px e forzato a 250px: niente sfora, console pulita, nessuna
      scrittura DB (contatti di prova solo nel DOM).
  A10 h4 e h5 non avevano NESSUNA regola di dimensione. Base sola: h4
      1.05rem, h5 0.9rem (subito dopo h1..h6). Tolti 5 font-size di
      sedimento (.backpack-add-*/.sms-log-box/.chat-box a 0.85rem;
      .squad-item a 0.9rem). Tenuto .backpack-category h5 a 0.95rem di
      proposito. h3 non toccato. Verificato su Dashboard/Escursioni/modale/
      Impostazioni/Community: 0 clip, 0 a-capo nuovi, 0 inversioni h5>=h4.
  (da B4)  .settings-placeholder opacity .65 -> .85. Migliora la card
      "Altre impostazioni"; la sotto-lista grigia resta sotto AA finche'
      non si alza --color-text-muted stesso (quello e' B4 vero, Fascia B).

  DA VERIFICARE AL TELEFONO (o con un resize che funzioni): A6/A7/A8/A9/A10
  insieme. L'estensione Chrome non cambiava la larghezza effettiva della
  pagina; il comportamento stretto e' su @media/min()/calc() standard gia'
  usati altrove, mai visto renderizzare per davvero < ~380px.

  COMMIT: `7bd9c78` ("Ritocchi visivi Fascia A9-A10 + .settings-placeholder"),
  `d9d6edf` (A1-A8) intatto. Entrambi PUSHATI stasera (sono partiti insieme
  al deploy del blocco GPX qui sotto, su "vai con commit e push" di Denis) -
  origin/master ora e' `18af140`. Il giro `code-reviewer` su tutta la coda
  post-`a886ae4` non e' MAI stato completato (interrotto nella 33a) - se lo
  si vuole va rilanciato ora su `a886ae4..18af140`, il codice e' gia' in
  produzione.

  FASCIA B/C dell'audit: mai proposte. Empty state unificato, descrizione
  escursione tagliata, --color-text-muted sotto soglia, palette fredda
  residua (--bg-panel/--bg-overlay), responsive login/registrazione, colori
  JS duplicati. Restano nel piano, si decide quando/se.

--- COMPLETAMENTO / TRACCIA IMPORTATA (34a): 4 modifiche + fix Marco, TUTTO
    IN PRODUZIONE. RESTA IL PUNTO 1. ---
Il problema di partenza (33a): Denis completa un'escursione col tracciamento
dal vivo; il compagno Marco resta senza tasto di conferma e non si possono
recensire. Causa: `POST /api/hikes/:id/complete` (auto, da completeLinkedHike)
completa SOLO te in silenzio - mai "chi era con te", mai groupCompletedAt -
e poi l'escursione finisce in "Gia' fatte", dove il tasto "Completa
escursione" NON esiste. Un partecipante non-creatore che non ha tracciato
lui stesso non ha OGGI nessuna via verso un Completion.

FATTO STASERA (commit `8b19006` poi `18af140`, pushati e deployati):
  1. `routes/completions.js` POST /:id/gpx (tasto ⬆): se esiste gia' una
     sessione CONCLUSA per quell'escursione/utente (dal vivo O da un .gpx
     precedente), il file caricato la SOSTITUISCE - prima si teneva la
     registrazione dal vivo "perche' migliore", ma una con GPS fermo a
     schermo spento (buchi, distanza sottostimata) e' peggiore. Solo sulla
     propria traccia. Campi di solo-tracciamento (pausedMs...) azzerati.
  2. `public/js/storico.js`: il contatore "X file questo mese" escludeva
     solo per importedFrom, non per hikeId - ora esclude anche quelle
     collegate a un'escursione (il tetto vero, /import-gpx, gia' lo fa).
  3. DISLIVELLO DELLE TRACCE IMPORTATE = grezzo, nessuna lisciatura.
     `lib/gpx.js` nuova SOGLIA_DISLIVELLO_IMPORT_M = 0, separata dai 10 m.
     10 resta per le quote STIMATE (percorso progettato, GPS telefono);
     0 per un FILE di dispositivo (.gpx/.fit, altimetro barometrico).
     Decisione di Denis: "se importo una traccia voglio i dati della
     traccia senza correzioni". Misurato sul .fit di Denis: soglia 10 dava
     1304 m, soglia 0 da' 1555 m (il Garmin ~1503). Dettaglio e "perche'
     due valori" in lib/gpx.js sopra SOGLIA_DISLIVELLO_M, e in
     [[03-Decisioni-Architetturali]].
  4. `routes/completions.js`: se chi carica col ⬆ e' il CREATORE, il file
     aggiorna anche i numeri CONDIVISI della Hike (distanza/dislivello/
     quota max/routeSource), come complete-group. Non-creatore: solo il
     suo Completion.
  Denis ha ricaricato il .fit Garmin di "Castel San Pietro..." in
  produzione: card ora 28,6 km / 1555 m / quota max 1215, CAI 9h 6m.
  Prove: prova-punto113-gpx.js 58/58 (2 sez. nuove), + 113/116/fit/
  passo-non-misurato/93 verdi. prova-punto33 55/4/8 (i falliti/saltati
  sono il noto rumore Open-Meteo live, path NON toccato).

  FIX-DATI MIRATO per Marco (non codice, script one-off buttato):
  creato il Completion di Marco per "Castel San Pietro..."
  (`6a9907f8cd712e34f924bd4f`) con actualTimeHours 8.4425 EREDITATO da
  Denis (niente movingTimeHours: e' per-persona); Marco completedHikes
  34->35, livello invariato; hike.groupCompletedAt impostato. GATE
  RECENSIONI ORA PASSA (Denis<->Marco). Passo demo di Marco (231/330) era
  stato azzerato da un giro di prova-punto113-gpx.js (login come Marco =
  elenco[0]) - ripristinato a mano nello stesso script. Vedi
  [[07-Trappole-Tecniche]] (il test spazza il passo del demo).

PUNTO 1, ANCORA APERTO - E' DA QUI CHE SI RIPARTE:
  a fine tracciamento collegato a un'escursione (trackingState.hikeId),
  aprire la checklist "chi era con te" (riusare il modale complete-group)
  invece di completare solo se stessi. Probabile: far confluire
  completeLinkedHike (tracking.js:345) dentro il percorso di complete-group,
  con la traccia appena registrata gia' pronta. Cosi' i partecipanti
  ottengono un Completion e non serve piu' il fix a mano.
  RICHIESTA 2 di Denis (traccia per-partecipante) e' GIA' quasi possibile:
  un partecipante CON un Completion vede gia' il tasto ⬆ (storico.js) e
  puo' caricare il PROPRIO .gpx/.fit -> aggiorna il suo Completion + crea
  la sua sessione. Manca solo il Completion di partenza, che lo da' il
  punto 1. Dettaglio in cose_da_fare.txt "COMPLETAMENTO DA TRACCIAMENTO
  DAL VIVO NON CHIEDE 'CHI ERA CON TE'".

--- NUOVO, SOLO DA SEGNARE (nessun codice) ---
Denis ha chiesto se si puo' introdurre "il Google authenticator" e/o
"accedi con Google" per l'iscrizione. Sono due cose diverse: 2FA TOTP
(standard aperto, non una dipendenza Google - segnato DA FARE, vedi sotto)
e login OAuth "Accedi con Google" (dipendenza vera, terza eccezione al
vincolo open source, Google di solito pretende una Privacy Policy
pubblicata che qui non esiste ancora - IN SOSPESO su decisione di Denis,
si riprende solo dopo la nota legale). Dettaglio (direzione del 2FA, cosa
resta da decidere) in cose_da_fare.txt, sezione "2FA (GOOGLE
AUTHENTICATOR) - DA FARE. LOGIN CON GOOGLE - IN SOSPESO".

>>> APERTI (follow-up sicurezza della 32a, invariati):
  - CONFORMITA' GDPR (professionista): invariata. Serve un consulente
    privacy/avvocato GDPR (non un DPO formale) per una vera Informativa
    Privacy + il testo di consenso prima di accettare un invito.
  - VERIFICA DAL VIVO di Denis su tutto l'invito squadra direzionale:
    invariata (invito a entrare in una squadra; invito squadra a
    un'escursione; uscita da una squadra e successione del creatore).

>>> LAVORO FUTURO PREPARATO, ancora agli atti (NIENTE codice scritto):
    FILONE B: collezione "I 2000 dell'Appennino" - bloccato, LA LISTA
    UFFICIALE DELLE VETTE LA FORNISCE DENIS. Piano:
      C:\Users\lenovo\.claude\plans\camoscio-2000-appennino-collezione.md

>>> AZIONI MANUALI DI DENIS, aperte (invariate dalla 30a):
  1. ACCOUNT_SCRUB_SECRET su Render (stringa a scelta, DIVERSA da
     SAFETY_CRON_SECRET) + un job giornaliero su cron-job.org verso:
       https://camoscio.onrender.com/api/users/scrub-eliminati?chiave=<segreto>
     Senza, gli account eliminati restano "Account eliminato" PER
     SEMPRE (nessuno scrub al giorno 30). Il server lo urla nei log
     all'avvio in produzione. Fail-safe, ma da fare.
  2. Ricaricare il .gpx/.fit col ⬆ per le vecchie escursioni da
     pubblicare nel feed (l'originale non era stato conservato).
  3. Camilla: `camilla2026` e' TEMPORANEA. Deve cambiarla da
     Impostazioni > Cambia password. Accede con EMAIL
     camilla.orsini@gmail.com (il login e' per email, non username).

>>> APERTO, non bloccante (invariato):
  - NOTA LEGALE: index.html fa accettare "Termini + Informativa
    privacy" senza documento dietro. Stesso professionista del punto
    GDPR sopra puo' chiuderli insieme.
  - M-5 (il vecchio, diverso dagli M della revisione sicurezza): race
    del cancella-contatto emergenza con 2 schede aperte (serve una
    DELETE per contenuto/id, non per indice posizionale).
  - TTL sulle Notification che nominano un utente: un "Mario Rossi
    ha chiesto di..." resta per sempre nelle notifiche altrui col
    nome vero anche dopo l'eliminazione di Mario. Cambio schema
    app-wide + migrazione -> a se'.
  - nascondere il cestino sulle card "completate in gruppo".

>>> ALTRE APERTE, non urgenti:
    dominio email proprio (rimandato) - causa vera delle email che non
    arrivano (spam, es. Camilla).

>>> RESTA VALIDO:
  - Hook "Stop" di auto-save: commit locale (MAI push) a fine di
    ogni risposta se ci sono modifiche non salvate. Va rifuso in un
    commit vero prima di ogni push.
  - Traduzione IT/EN (punto 102, CHIUSO): resta ITALIANO di
    proposito tutto il testo scritto dagli utenti, i nomi propri di
    luogo, i nomi dei percorsi salvati, titolo/descrizione dei badge
    personali, le schede "i" dei badge, il testo delle notifiche (lo
    genera il server), il segnaposto nativo di <input type=date>/
    <input type=file>, e componiRapporto() dentro diagnostica-gps.
    Le chiavi EN dei blocchi nuovi si aggiungono mano a mano.
  - "Account eliminato" (punto A-3.4) SI traduce:
    common.accountEliminato -> "Deleted account".

===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — congelato dal 6/08/2026: da quella data il racconto
    disteso di ogni sessione vive solo in ../camoscio memoria/06-Cronologia-Sessioni.md,
    non qui. Utile solo per il dettaglio di lavoro PRIMA di quella data.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
MAI piu' (congelato); cose_da_fare.txt quando un punto si chiude o ne arriva uno nuovo.

>>> LEZIONI OPERATIVE (git dopo un'interruzione, moduli che si collegano tardi,
    componenti condivisi fra piu' pagine, .toObject vs .toJSON, $unset di campi
    required, select('username') che scavalca la pseudonimizzazione, recupero
    password vs grazia dell'eliminazione, npm audit fix vs overrides, serializzatore
    aggiunto a una rotta senza try/catch, test verde su una premessa falsa,
    select:false non vale su un campo assegnato in memoria, select:false su
    findByIdAndUpdate va reso condizionale su una rotta condivisa, un gate privacy
    condiviso va dichiarato fail-closed, un subagent puo' dichiarare falsamente di
    aver aggiornato la memoria - due volte confermato, due funzioni di render sullo
    stesso dato ma DOM diversi non danno errore se chiami quella sbagliata, un
    history.back() e' asincrono e corre contro un pushState immediatamente successivo,
    una navigazione che cambia solo l'hash non ricarica la SPA - il codice di init
    non si rieseguisce, il service worker MV3 dell'estensione Chrome si sospende
    ("attiva" != "connessa"), una prova che fa login come account demo e lancia
    recalculateAndApplyPace ne AZZERA il passo assegnato a mano - da 16/08 zero
    osservazioni = campo tolto - se non lo ripristina in finally): tutte in
    ../camoscio memoria/07-Trappole-Tecniche.md — quella e' la sede dove si guarda
    prima di toccare codice a rischio. NB dal 06/09: il dislivello ha DUE soglie -
    0 per le tracce importate da un file di dispositivo (grezzo), 10 per le quote
    stimate (percorso progettato, GPS dal vivo). Vedi lib/gpx.js.
===================================================================
