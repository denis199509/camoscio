===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-08-30 (tredicesima sessione).

>>> IN CORSO: PUNTO 113 — LA PARTE SOCIAL. Passi 1-6 di 10 fatti e
    verificati dal vivo. Commit LOCALE b6a771c (rifusione dei 6 auto-save),
    NON PUSHATO. SI RIPARTE DAL PASSO 7.

  Cosa e': tasto "Segui" sui profili (unidirezionale, stile Instagram);
  pagina "Feed" (voce di barra dopo Dashboard) con le uscite pubblicate
  da chi segui; ogni uscita apribile su una pagina con mini-mappa della
  traccia + distanza/dislivello/tempo + didascalia; un "mi piace" (emoji
  montagna); un tasto "crea percorso" che copia la traccia altrui fra
  "I miei progetti" per riusarla. Piu' le liste "chi segui / ti segue"
  in Tribu' & Squadre + contatore sul profilo altrui.

  PIANO (10 passi fermabili) - file:
  .claude/plans/ok-adesso-ci-dovremmo-dreamy-castle.md
    1. Follow: modello + rotte + stato                       [FATTO]
    2. Tasto "Segui" sul profilo                             [FATTO]
    3. Liste follow in Tribu' & Squadre + contatore profilo  [FATTO]
    4. Pubblicazione di un'uscita                            [FATTO]
    5. Pagina Feed                                           [FATTO]
    6. Pagina uscita pubblicata + mini-mappa                 [FATTO]
    7. Mi piace (+ notifica all'autore)                      <- QUI
    8. Percorso da traccia (modello SavedRoute nuovo)        da fare
    9. Segui la linea dal vivo + aggancio a una Hike         da fare
   10. Traduzione EN di tutte le chiavi + prova + controprova da fare

  FATTO FIN QUI (dettaglio per passo in cose_da_fare.txt punto 113,
  decisioni nel vault 03 "Punto 113"):
  - models/Follow.js (collezione {followerId,followingId} + 2 indici),
    routes/follow.js su /api/follow, CamoscioState.following in refreshState
  - tasto "Segui" in renderProfileIdentity (condivisa col proprio
    profilo: gate isSelf), window.toggleFollow
  - card "Persone" a scomparsa in #social + contatore "N seguaci - N
    seguiti" su #user-profile (modale #follow-list-modal)
  - ActiveHikeSession.publishedAt + caption; POST /sessions/:id/publish +
    /unpublish (autore, solo 'ended'); tasto "Pubblica nel feed" sulle
    card di "Le mie escursioni"
  - <section id="feed"> + public/js/feed.js + routes/feed.js
    (GET /api/feed?before=<ISO>, cursore, MAI i points); indice parziale
    {userId,publishedAt} su ActiveHikeSession
  - <section id="outing-page"> + public/js/outingpage.js (IIFE);
    guardiaUscitaVisibile + GET /sessions/:id/meta e /points
    (?scopo=mini semplifica); mini-mappa = seconda istanza Leaflet

  OTTO DECISIONI DI DENIS (in cose_da_fare.txt punto 113, per esteso):
  follow unidirezionale senza conferma; pubblicazione ESPLICITA con
  didascalia; SOLO uscite con traccia GPS; "crea percorso" seguibile dal
  vivo solo come linea (NESSUN avviso di fuori-percorso); percorso copiato
  in "I miei progetti" con etichetta "da <autore>"; visibilita' SOLO ai
  follower (protetta lato server); liste follow pubbliche fra utenti
  loggati; mi piace con notifica all'autore, nessuna notifica per le
  pubblicazioni.

  DUE TRAPPOLE PAGATE (in vault 07):
  - MongoDB non ammette un campo semplice ({publishedAt: x}) e un
    operatore ($unset) nello stesso update -> $set + $unset.
  - lucide sostituisce <i data-lucide> con <svg>: un CSS che ruota "i"
    manca il bersaglio -> selezionare anche "svg".

  COSMETICO noto (non deciso di sistemarlo): la card Feed mostra due date
  (pubblicazione + data uscita); su card strette "Pubblica nel feed" va a
  capo su due righe.

>>> PRIMA DI RIPRENDERE: il commit b6a771c e' LOCALE. Se si vuole pushare
    (Denis lo deve chiedere), e' gia' un commit vero unico - basta
    `git push`. Gli auto-save successivi vanno rifusi come sempre.

>>> GIA' IN PRODUZIONE (sessioni precedenti): Punto 111 (segnalazioni
    sentiero: "risolvi" e scadenza a 90gg passano dal moderatore) e
    Punto 108 (un timbro richiede una traccia reale entro la soglia).
    Dettaglio: cose_da_fare.txt punti 111/108, vault 03/06/07.

>>> ALTRE APERTE, non urgenti (dettaglio in vault 04-Da-Fare.md):
    livelli ORIGINALI del punto 63b (gradini per la ZONA dichiarata a
    mano, DIVERSI dai livelli gia' fatti del punto 86) + calcolo
    automatico del raggio (punto 42a) - Denis deve ancora pensarci, non
    richiederlo; dominio email proprio (rimandato da lui, "non ora").

>>> RESTA VALIDO:
  - Hook "Stop" di auto-save: commit locale (MAI push) a fine di ogni
    risposta se ci sono modifiche non salvate in questo repo. Va rifuso
    in un commit vero prima di ogni push (reset --soft se sono piu' di
    uno) - fatto per i passi 1-6 (b6a771c).
  - Traduzione IT/EN (punto 102, CHIUSO): resta ITALIANO di proposito
    tutto il testo scritto dagli utenti (didascalie del feed comprese),
    i nomi propri di luogo, titolo/descrizione dei badge personali, le
    schede "i" dei badge (punto 112), il segnaposto nativo di
    <input type=date>/<input type=file>, e componiRapporto() dentro
    diagnostica-gps.html. Le chiavi EN dei passi 1-6 del punto 113 NON
    sono ancora nel dizionario: si aggiungono tutte al passo 10 (ripiego
    all'italiano inline fino ad allora, come da rollout del punto 102).
  - Punti 102, 106 e 112 CHIUSI E PUSHATI il 28/08.

===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo,
    vedi 04-Da-Fare.md del vault).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — congelato dal 6/08/2026: da quella data il racconto
    disteso di ogni sessione vive solo in ../camoscio memoria/06-Cronologia-Sessioni.md,
    non qui. Utile solo per il dettaglio di lavoro PRIMA di quella data.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
MAI piu' (congelato); cose_da_fare.txt quando un punto si chiude o ne arriva uno nuovo.

>>> I PUNTI CHIUSI FINO AL 103 (piu' 94 e 98) non sono piu' elencati qui: erano
    diventati un appello di 190 righe, cioe' esattamente "qualcosa che appartiene a
    un altro file". Tutti chiusi, committati e pushati, molti confermati dal vivo da
    Denis. Il dettaglio con le sue parole e' in cose_da_fare.txt (cercare il numero);
    il racconto per sessione in ../camoscio memoria/06-Cronologia-Sessioni.md.
    Compressione fatta il 28/08/2026, stesso criterio dello sfoltimento del 18/08.

>>> LEZIONI OPERATIVE (git dopo un'interruzione, moduli che si collegano
    tardi, componenti condivisi fra piu' pagine, trappole Android/Gradle):
    tutte in ../camoscio memoria/07-Trappole-Tecniche.md, non ripetute qui -
    quella e' la sede dove si guarda prima di toccare codice a rischio.
===================================================================
