===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-08-31 (quindicesima sessione).

>>> NIENTE IN CORSO. Tutto pushato e in produzione (Render redeploy
    automatico ai push).

>>> LA PROSSIMA SESSIONE SI PARTE DALLA REVISIONE UX (deciso da Denis a
    fine 15a sessione: "nella prossima sessione passiamo al aggiornamento
    ux"). Un amico ha valutato il sito - giudizio buono, ma "User
    Experience" poco intuitiva. NON iniziata, nessun dettaglio concordato:
    a inizio sessione chiedere a Denis i consigli dell'amico e da dove
    vuole partire, PRIMA di proporre modifiche. Dentro ci ricadono i
    titoli di sezione lunghi troncati su telefono (ex "punto 106
    residuo") - non toccarli a pezzi. Dettaglio in vault 04-Da-Fare
    ("PROSSIMO GRANDE LAVORO - revisione UX").

  ULTIMO LAVORO — QUATTRO RIFINITURE, chiuse e in produzione il 31/08
  (push 57697ac..152fb7a su camoscio/master, 2 commit).

  1. "Pubblica nel feed" si spezzava su card strette (commit fbf340a):
     .outing-card-actions ora flex-wrap + i bottoni white-space:nowrap.
  2. Due date nella card del feed (commit fbf340a): la riga autore ha
     gia' la data di pubblicazione. schedaUscitaProfilo prende
     {perFeed, dataUscitaDiversa}: nel feed il titolo senza nome e'
     "Uscita"/"Outing" e la data compare SOLO se l'escursione e' di un
     altro giorno (giorno di calendario). Chiave EN feed.uscitaSenzaNome.
     Verificato dal vivo (demo Marco): post di Denis ok, console pulita.
  3. Punto 115 residuo (commit fbf340a): sulla card "escursione
     completata" con traccia collegata, se la traccia e' stata
     RINOMINATA con la matita e' quel nome a fare da titolo (titolo
     escursione sulla riga della data). Se i nomi COINCIDONO (default)
     la card e' identica a prima. schedaEscursioneCompletata prende
     {nomeUscita} da storico.js. NON verificato dal vivo il ramo
     rinominato (nessun demo ce l'ha) - da guardare sul telefono.
  4. Coda punto 113 (commit 152fb7a): il ⬆ su un'escursione completata
     assegna i BADGE DI VETTA come /import-gpx. assegnaTimbriDallaTraccia
     spostata VERBATIM da routes/tracking.js a lib/geofenceTimbri.js
     (equivalenza: prova-punto108 24/24). routes/completions.js la
     chiama sui punti COMPLETI, badge nuovi nel campo "badge" della
     risposta; social.js mostra un toast. prova-punto113-gpx sez.7 nuova
     (35->43), prova-punto92 84/84, prova-punto113 103/103 (corretta
     un'asserzione fragile: i 4 demo seguono Denis davvero sul DB).

  DECISIONI DI DENIS in questa sessione (NON riproporre):
  - togliere+rimettere "mi piace" -> seconda notifica all'autore:
    LASCIATO com'e' (raro, innocuo; un flag costerebbe un campo).
  - titoli di sezione lunghi (punto 106): RIMANDATI alla revisione UX.

  Dettaglio: cose_da_fare.txt "QUINDICESIMA SESSIONE"; decisioni nel
  vault 03 "Coda punto 113: badge dal ⬆"; racconto nel vault 06;
  prove/LEGGIMI-PROVE.txt (sez.7 di prova-punto113-gpx).

>>> GIA' IN PRODUZIONE (14a sessione, 30/08, push a2a53a5..0d0ecc3):
    PUNTO 114 import file .fit (lib/fit.js, parseFit stessa forma di
    parseGpx; dep fit-file-parser@5.0.2). PUNTO 115 nome dell'uscita
    modificabile (ActiveHikeSession.importedName editabile, PATCH
    /sessions/:id/name). Compressione icone badge (46->8,8 MB,
    scripts/comprimi-badge.js). Verifica dal vivo permesso GPS + coda
    A/B/C (guida GPS per PC; activeHikeId senza default globale; selettore
    escursione nello Zaino). Smartwatch (Garmin/Suunto/Amazfit): valutato
    e SCARTATO - non riproporre "colleghiamo Garmin".
    >>> LIMITE .fit/.gpx, e' il FILE non il codice: un file "smart" (o da
        Garmin Connect) ha buchi -> niente split cammino/pause. Serve 1 Hz.
        Distanza/dislivello/durata totale/badge: sempre giusti.
    >>> DENIS DEVE RICARICARE il .gpx/.fit col ⬆ per le escursioni gia'
        fatte che vuole pubblicare (l'originale non era stato conservato).
    NON fatto dalla compressione icone: sgonfiare il .git da 89 MB (git
    filter-repo, delicato su repo pubblico); WebP; alleggerire lucide.js.

>>> GIA' IN PRODUZIONE (sessioni precedenti): Punto 113 (parte social:
    Segui, Feed, uscita apribile, "mi piace", "crea percorso"); Punto 111
    (segnalazioni sentiero: "risolvi" e scadenza a 90gg passano dal
    moderatore); Punto 108 (un timbro richiede una traccia reale entro la
    soglia). Dettaglio: cose_da_fare.txt punti 113/111/108, vault 03/06/07.

>>> ALTRE APERTE, non urgenti (dettaglio in vault 04-Da-Fare.md):
    livelli ORIGINALI del punto 63b (gradini per la ZONA dichiarata a
    mano, DIVERSI dai livelli gia' fatti del punto 86) + calcolo
    automatico del raggio (punto 42a) - Denis deve ancora pensarci, non
    richiederlo; dominio email proprio (rimandato da lui, "non ora").

>>> RESTA VALIDO:
  - Hook "Stop" di auto-save: commit locale (MAI push) a fine di ogni
    risposta se ci sono modifiche non salvate in questo repo. Va rifuso
    in un commit vero prima di ogni push (git reset --soft sull'ultimo
    commit vero se gli auto-save sono piu' di uno) - fatto per i passi
    7-10 (b7afa35, reset --soft 629476f sui 4 auto-save).
  - Traduzione IT/EN (punto 102, CHIUSO): resta ITALIANO di proposito
    tutto il testo scritto dagli utenti (didascalie del feed comprese),
    i nomi propri di luogo, i nomi dei percorsi salvati e l'etichetta
    "da <autore>", titolo/descrizione dei badge personali, le schede "i"
    dei badge (punto 112), il testo delle notifiche (lo genera il
    server), il segnaposto nativo di <input type=date>/<input type=file>,
    e componiRapporto() dentro diagnostica-gps.html. Le chiavi EN dei
    punti 113/114/115 sono state aggiunte tutte. Restano IT anche i NOMI
    delle uscite (punto 115: li scrive l'utente).
  - Punti 102, 108, 111, 112, 113, 114, 115 CHIUSI E IN PRODUZIONE
    (+ le 4 rifiniture del 31/08). Il punto 106 e' chiuso per la parte
    fatta il 28/08; il residuo (titoli sezione lunghi) e' nella revisione
    UX. Il ⬆ ora assegna i badge di vetta (coda 113).

===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo,
    vedi 04-Da-Fare.md del vault).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — congelato dal 6/08/2026: da quella data il racconto
    disteso di ogni sessione vive solo in ../camoscio memoria/06-Cronologia-Sessioni.md,
    non qui. Utile solo per il dettaglio di lavoro PRIMA di quella data.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
MAI piu' (congelato); cose_da_fare.txt quando un punto si chiude o ne arriva uno nuovo.

>>> I PUNTI CHIUSI FINO AL 103 (piu' 94 e 98) non sono piu' elencati qui: erano
    diventati un appello di 190 righe, cioe' esattamente "qualcosa che appartiene a
    un altro file". Tutti chiusi, committati e pushati, molti confermati dal vivo da
    Denis. Il dettaglio con le sue parole e' in cose_da_fare.txt (cercare il numero);
    il racconto per sessione in ../camoscio memoria/06-Cronologia-Sessioni.md.
    Compressione fatta il 28/08/2026, stesso criterio dello sfoltimento del 18/08.

>>> LEZIONI OPERATIVE (git dopo un'interruzione, moduli che si collegano
    tardi, componenti condivisi fra piu' pagine, trappole Android/Gradle):
    tutte in ../camoscio memoria/07-Trappole-Tecniche.md, non ripetute qui -
    quella e' la sede dove si guarda prima di toccare codice a rischio.
===================================================================
