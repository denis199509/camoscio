===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-09-05 (TRENTATREESIMA sessione — Denis ha aperto con "abbiamo
delle cose già progettate, possiamo cominciare con il codice": i tre lavori
preparati ma mai scritti (vedi 32a sessione) proposti via AskUserQuestion,
scelto il TASTO INDIETRO / HISTORY API. Implementato per intero e verificato
DAL VIVO con l'estensione Chrome (non solo a schermo: history.state e l'esito
delle promise letti con javascript_tool a ogni passo).

--- COSA E' STATO FATTO ---
Il tasto Indietro (telefono e PC) buttava fuori dal sito invece di tornare
alla pagina/sezione precedente, perche' la SPA non toccava mai la cronologia
del browser. Piano gia' pronto dalla 29a sessione (ambito pieno: sezioni +
pagine con id + modali; Indietro sulla home esce dal sito).

Nucleo in public/js/app.js: navigateTo() e' l'unico punto che fa
history.pushState (flag sopprimiPush per non ripushare quando la chiamata
arriva gia' da un popstate); un gestore popstate unico; un registro modali
(apriModaleStorico/chiudiModaleStorico/scollegaModaleStorico) che pianta una
entry all'apertura di un modale e la CONSUMA con un history.back() alla
chiusura, invece di nascondere e basta - senza, un Indietro successivo
troverebbe una entry morta e servirebbe un secondo Indietro per uscire
davvero dalla sezione sotto. squadpage.js/hikepage.js/userprofile.js/
outingpage.js passano l'id della pagina come stato.entita; gli 8 modali del
censimento della 29a agganciati uno per uno (create-hike-modal,
complete-group-modal, invite-squad-modal, follow-list-modal, generic-modal,
map-picker-modal, image-lightbox, report-panel). tracking-panel ESCLUSO di
proposito (resta un pannello di stato, non un dialogo).

>>> TRE DIFETTI VERI TROVATI E CORRETTI in questa sessione (due SOLO
    provando dal vivo, uno rileggendo il codice appena scritto):
  1. La entry d'ingresso di un deep-link (es. un link salvato su #map-section)
     non portava camoscioNav: un Indietro che vi tornava dopo aver navigato
     altrove non risincronizzava la sezione VISIBILE (l'URL cambiava, il
     contenuto restava quello di prima). Corretto con un replaceState che
     etichetta subito quella entry.
  2. apriModaleStorico non portava avanti l'entita' (es. l'id utente) della
     pagina su cui si apre un modale: chiudendo il modale con un'azione che
     naviga altrove (es. click su una riga della lista follower che apre un
     ALTRO profilo), un Indietro successivo su quella entry ormai orfana
     mostrava il profilo SBAGLIATO (l'ultimo aperto, non quello di partenza).
     Corretto portando avanti entita'/scrollToId dalla entry precedente.
  3. chiudiModaleStorico eseguiva SEMPRE la chiusura piantata all'apertura
     invece di quella passata dal bottone premuto - un Confirm sarebbe stato
     risolto come se fosse un Annulla. Trovato rileggendo il codice prima di
     testare dal vivo, corretto subito.

>>> VERIFICATO DAL VIVO (Chrome, account demo Marco Alpinista), esito atteso
    confermato per ognuno: sezione->sezione senza mai uscire dal sito;
    Dashboard->Indietro = esce (corretto, nessuna entry sotto); pagina con id
    (hike-page) torna al punto di partenza; deep-link diretto su un URL con
    hash apre subito quella sezione; create-hike-modal (Indietro chiude senza
    navigare, il tasto X consuma la entry); generic-modal (Conferma/Annulla/
    Indietro risolvono la promise col valore giusto); showAlertModal, cioe'
    l'allarme del Dead Man's Switch (punto 37) - un Indietro premuto per
    sbaglio NON lo chiude, si ripianta la stessa entry, va riconosciuto col
    bottone; image-lightbox; follow-list-modal (click su una riga chiude il
    modale E naviga su un altro profilo senza corrompere la cronologia - il
    caso piu' delicato del piano). NON riverificati uno per uno dal vivo
    (stesso aggancio, stesso pattern gia' provato corretto su due casi
    diversi): complete-group-modal, invite-squad-modal, map-picker-modal,
    report-panel.

>>> STATO A FINE 33a SESSIONE:
      - camoscio: scritto e verificato, NON ANCORA COMMITTATO (si somma al
        commit 617edde della 32a sessione, gia' in attesa di push - in
        attesa del via esplicito di Denis per entrambi).
      - Nessuna prova automatica (la navigazione frontend non ne ha, come da
        piano) - solo verifica dal vivo, sopra.
      - npm audit non toccato (nessuna dipendenza cambiata).
      - Console pulita a ogni passo della verifica dal vivo.

>>> DETTAGLIO COMPLETO (i tre bug uno per uno, cosa e' stato verificato):
    cose_da_fare.txt, sotto "TASTO INDIETRO / HISTORY API".
    Piano originale (29a sessione): C:\Users\lenovo\.claude\plans\
    camoscio-tasto-indietro-history-api.md (aggiornato con lo stato attuale).

>>> LAVORO FUTURO PREPARATO, ancora agli atti (NIENTE codice scritto):
    A) RITOCCHI VISIVI (Fascia A pronta, ~11 interventi CSS) - rapporto:
         C:\Users\lenovo\.claude\plans\camoscio-audit-visivo-29a.md
       Due decisioni aspettano Denis (Passo&Fatica colori; contrasto
       bianco su arancione) - non bloccano la Fascia A, vedi il piano.
    B) FILONE B: collezione "I 2000 dell'Appennino" - bloccato, LA
       LISTA UFFICIALE DELLE VETTE LA FORNISCE DENIS. Piano:
         C:\Users\lenovo\.claude\plans\camoscio-2000-appennino-collezione.md

>>> APERTI (follow-up sicurezza della 32a, invariati):
  - CONFORMITA' GDPR (professionista): invariata, su richiesta esplicita di
    Denis. Serve un consulente privacy/avvocato GDPR (non un DPO formale)
    per scrivere una vera Informativa Privacy + il testo di consenso prima
    di accettare un invito.
  - VERIFICA DAL VIVO di Denis su tutto l'invito squadra direzionale:
    invariata (invito a entrare in una squadra; invito squadra a
    un'escursione; uscita da una squadra e successione del creatore).

>>> AZIONI MANUALI DI DENIS, aperte (invariate dalla 30a):
  1. ACCOUNT_SCRUB_SECRET su Render (stringa a scelta, DIVERSA da
     SAFETY_CRON_SECRET) + un job giornaliero su cron-job.org verso:
       https://camoscio.onrender.com/api/users/scrub-eliminati?chiave=<segreto>
     Senza, gli account eliminati restano "Account eliminato" PER
     SEMPRE (nessuno scrub al giorno 30). Il server lo urla nei log
     all'avvio in produzione. Fail-safe, ma da fare.
  2. Ricaricare il .gpx/.fit col ⬆ per le vecchie escursioni da
     pubblicare nel feed (l'originale non era stato conservato).
  3. Camilla: `camilla2026` e' TEMPORANEA. Deve cambiarla da
     Impostazioni > Cambia password. Accede con EMAIL
     camilla.orsini@gmail.com (il login e' per email, non username).

>>> APERTO, non bloccante (invariato):
  - NOTA LEGALE: index.html fa accettare "Termini + Informativa
    privacy" senza documento dietro. Stesso professionista del punto
    GDPR sopra puo' chiuderli insieme.
  - M-5 (il vecchio, diverso dagli M della revisione sicurezza): race
    del cancella-contatto emergenza con 2 schede aperte (serve una
    DELETE per contenuto/id, non per indice posizionale).
  - TTL sulle Notification che nominano un utente: un "Mario Rossi
    ha chiesto di..." resta per sempre nelle notifiche altrui col
    nome vero anche dopo l'eliminazione di Mario. Cambio schema
    app-wide + migrazione -> a se'.
  - nascondere il cestino sulle card "completate in gruppo".

>>> ALTRE APERTE, non urgenti:
    dominio email proprio (rimandato) - causa vera delle email che non
    arrivano (spam, es. Camilla).

>>> RESTA VALIDO:
  - Hook "Stop" di auto-save: commit locale (MAI push) a fine di
    ogni risposta se ci sono modifiche non salvate. Va rifuso in un
    commit vero prima di ogni push.
  - Traduzione IT/EN (punto 102, CHIUSO): resta ITALIANO di
    proposito tutto il testo scritto dagli utenti, i nomi propri di
    luogo, i nomi dei percorsi salvati, titolo/descrizione dei badge
    personali, le schede "i" dei badge, il testo delle notifiche (lo
    genera il server), il segnaposto nativo di <input type=date>/
    <input type=file>, e componiRapporto() dentro diagnostica-gps.
    Le chiavi EN dei blocchi nuovi si aggiungono mano a mano.
  - "Account eliminato" (punto A-3.4) SI traduce:
    common.accountEliminato -> "Deleted account".

===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — congelato dal 6/08/2026: da quella data il racconto
    disteso di ogni sessione vive solo in ../camoscio memoria/06-Cronologia-Sessioni.md,
    non qui. Utile solo per il dettaglio di lavoro PRIMA di quella data.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
MAI piu' (congelato); cose_da_fare.txt quando un punto si chiude o ne arriva uno nuovo.

>>> LEZIONI OPERATIVE (git dopo un'interruzione, moduli che si collegano tardi,
    componenti condivisi fra piu' pagine, .toObject vs .toJSON, $unset di campi
    required, select('username') che scavalca la pseudonimizzazione, recupero
    password vs grazia dell'eliminazione, npm audit fix vs overrides, serializzatore
    aggiunto a una rotta senza try/catch, test verde su una premessa falsa,
    select:false non vale su un campo assegnato in memoria, select:false su
    findByIdAndUpdate va reso condizionale su una rotta condivisa, un gate privacy
    condiviso va dichiarato fail-closed, un subagent puo' dichiarare falsamente di
    aver aggiornato la memoria - due volte confermato, due funzioni di render sullo
    stesso dato ma DOM diversi non danno errore se chiami quella sbagliata, un
    history.back() e' asincrono e corre contro un pushState immediatamente successivo,
    una navigazione che cambia solo l'hash non ricarica la SPA - il codice di init
    non si rieseguisce): tutte in ../camoscio memoria/07-Trappole-Tecniche.md — quella
    e' la sede dove si guarda prima di toccare codice a rischio.
===================================================================
