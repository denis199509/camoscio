===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-08-08
===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo,
    vedi 04-Da-Fare.md del vault).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — l'ex leggimi. Il racconto disteso di ogni lavoro fatto,
    con le misure. Ci si va quando serve il dettaglio di qualcosa di gia' fatto.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
solo se c'e' un lavoro nuovo da raccontare; cose_da_fare.txt quando un punto si chiude
o ne arriva uno nuovo.



>>> DIECI PUNTI CHIUSI IN QUESTA SESSIONE (iniziata il 06/08, proseguita dopo un
    riavvio imprevisto del PC fino al 07/08 - nessun lavoro perso, ogni volta):
    58 (deploy rimasto indietro), 61, 54, 57, 63a, 59, 7, 42b, 43, 64. Il punto 43 ha
    anche riattivato il 44 (tempo previsto CAI), gia' chiuso a inizio agosto e mai
    toccato di nuovo. TUTTO PUSHATO E VERIFICATO LIVE SU RENDER. Dettaglio di ogni
    punto in cose_da_fare.txt e in ../camoscio memoria/06-Cronologia-Sessioni.md.

>>> SESSIONE CHIUSA VOLONTARIAMENTE L'08/08/2026 (Denis: "fermiamoci qui per
    adesso") - PUNTO 37, PRIMA META' FATTA, tutto committato e pushato
    (dcf02a7, db4d1d0, e004d08), verificato live su Render dopo ogni push.

>>> SEGRETO SAFETY_CRON_SECRET RUOTATO L'08/08/2026: il valore originale era
    scritto in chiaro proprio in questo file e in cose_da_fare.txt, entrambi
    nel repository camoscio, che e' PUBBLICO - trovato e sostituito nella
    sessione successiva a quella che l'aveva generato. Il valore vero NON va
    piu' scritto qui: e' stato comunicato a Denis in chat. Dettaglio in
    cose_da_fare.txt punto 37 e in ../camoscio memoria/07-Trappole-Tecniche.md.

>>> PRIMA COSA DA FARE: chiedere a Denis se ha gia' messo il valore nuovo
    (1) su Render (dashboard, Environment, variabile SAFETY_CRON_SECRET) e
    (2) su cron-job.org (account gratuito, scelto da Denis l'08/08 -
        "temporaneo, in futuro quando avremo un nostro server e dominio
        cambieremo"): job che chiama ogni 5 minuti, in GET o POST (la rotta
        accetta entrambi dall'08/08, commit e004d08)
        https://camoscio.onrender.com/api/safety/controlla-scadenze?chiave=[valore vero, non scritto qui]
    Se SI': verificare dal vivo che il ping arrivi davvero e che un timer
    scaduto per prova mandi l'email vera, POI (solo poi) aggiornare la nota
    a schermo che oggi dice ancora, correttamente, "l'invio vero non c'e'
    ancora" (public/index.html #safety-honesty-note) e il testo del modale
    locale in triggerEmergencyAlarm() (public/js/safety.js) - vincolo hard 7.
    Se NO, o se Denis vuole rimandare: si puo' passare al punto 48 (ancora
    vago, serve un esempio concreto da Denis prima di progettare), poi 45, 55
    - il punto 37 resta comunque utilizzabile cosi' com'e' (il timer e' gia'
    sul server, manca solo chi lo controlla).
    Dettaglio tecnico completo del punto 37 in cose_da_fare.txt e in
    ../camoscio memoria/06-Cronologia-Sessioni.md e 03-Decisioni-Architetturali.md.
    La parte tracciamento-a-pagina-chiusa (Screen Wake Lock) resta rimandata
    da Denis a quando si pensera' a un'app - non riproporla.

>>> PUNTI 52, 39, 65 E 66 - TUTTI CHIUSI IL 07/08/2026 (indietro del telefono nel
    wizard con la History API, mappa OpenTopoMap, due difetti diversi sulla stessa
    notifica del campanello - testo coperto a destra e poi riquadro tagliato a
    sinistra su schermo stretto). Tutti committati, pushati e verificati live su
    Render (fd5c4cf/27c220b, 65053cf/d6eb99e, ec36887/bc91141, 0dbfac7). Dettaglio
    completo in cose_da_fare.txt e in ../camoscio memoria/06-Cronologia-Sessioni.md
    e 03-Decisioni-Architetturali.md.

>>> LEZIONE (07/08/2026): dopo un'interruzione di sessione per cause sconosciute, le
    note (questo file compreso) erano rimaste ferme a un istante prima di un push gia'
    avvenuto. Dopo un'interruzione, controllare SEMPRE `git log origin/master..HEAD`
    in ogni repository PRIMA di fidarsi dell'ultima riga scritta qui. Vedi
    ../camoscio memoria/07-Trappole-Tecniche.md.

>>> DUE COSE DA SAPERE PRIMA DI TOCCARE public/index.html (trovate il 07/08/2026,
    non ovvie a prima vista):
      - create-hike-modal e complete-group-modal vivono FUORI da .page-container
        (vicino a generic-modal), non dentro una .page-section: un modale raggiungibile
        da piu' di una pagina (es. sia "Escursioni" sia "Le mie escursioni") deve stare
        li' con loro, altrimenti resta invisibile aprendolo dalla sezione "sbagliata" -
        bug vero trovato su create-hike-modal, in piedi dal punto 54 senza che nessuno
        se ne accorgesse;
      - un modulo che si collega SOLO da dentro initApp() (dopo await refreshState())
        rischia la stessa finestra "pulsante visibile ma senza listener" gia' trovata
        due volte (navigazione il 26/07, profilo il 06/08) - se un pulsante sembra
        rotto senza nessun errore in console, controllare prima QUANDO si e' agganciato
        il suo ascoltatore, non se il codice del gestore e' giusto.



>>> STATO A FINE SESSIONE (2026-08-08, chiusa su richiesta di Denis - "fermiamoci
    qui per adesso" - controllo di fine sessione fatto):
      - camoscio: TRE commit pushati (dcf02a7 codice, db4d1d0 note, e004d08 GET su
        controlla-scadenze), tutti verificati live su Render dopo il push - l'ultimo
        controllo (dopo e004d08) ha ridato 403 su una GET senza segreto, quindi il
        deploy definitivo c'e' davvero. Nota di onesta' a schermo confermata ancora
        invariata (giusto cosi': il trigger esterno non e' ancora collegato);
      - camoscio memoria (vault): committato e pushato (382d9d7) - 00, 03, 04, 06, 07
        tutti aggiornati nello stesso giro;
      - prove: prova-punto37.js (27/27, controprova fatta) piu' le 6 prove esistenti
        toccate dal nuovo campo email obbligatorio, tutte rilanciate dopo la modifica
        (195 controlli, nessuna regressione) - poi rilanciata anche dopo l'aggiunta
        del GET, ancora 27/27;
      - verificato anche dal vivo nel browser (estensione Chrome, account demo Marco
        Alpinista): aggiunto e poi rimosso un contatto di prova, attivato e disattivato
        il timer con conferma sul database vero, contatti demo ripristinati identici a
        come trovati, nessun dato di prova rimasto;
      - `git log origin/master..HEAD` vuoto in ENTRAMBI i repository, nessun server
        locale acceso, porta 3000 verificata libera.
