===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-09-08 (TRENTANOVESIMA sessione. Denis: "riprendiamo il
lavoro", poi via libera un pezzo alla volta. Seguito per intero l'ordine
deciso a fine 38a: (1) blocco 1 del 2FA -> (2) decisioni D-4/D-7 ->
(3) tutti i rilievi della revisione 35a.

CODICE: sta in 5 COMMIT AUTO-SAVE LOCALI, MAI PUSHATI (l'hook Stop). Vanno
rifusi con `git reset --soft <ultimo pushato>` in commit veri, DOPO il giro
code-reviewer + security-privacy sul cumulativo (il piano 2FA lo impone
prima di ogni push, e questa tornata tocca DMS / contatti emergenza /
consenso geo / eliminazione account). Ultimo commit pushato: 193c234.

  --- (1) BLOCCO 1 DEL 2FA TOTP: FATTO ---
  lib/totp.js (nuovo, zero dipendenze: crypto + impronta di lib/tokens.js).
  base32 RFC 4648, HOTP RFC 4226, TOTP RFC 6238, uriOtpauth, codici di
  recupero (Crockford base32, impronta sha256 con userId in testa).
  prove/prova-2fa.js SOLO la sezione 1 (vettori RFC, niente server/DB):
  62/62, con controprova (rotta la trappola 1 -> 23 rossi). Nessun file del
  sito toccato, niente lo importa ancora -> inerte. Le sezioni 2-10 e i
  blocchi 2-9 del piano restano da fare.
  NB: prova-2fa.js dice "porta 3136" ma la 3136 e' gia' di
  prova-contatti-emergenza -> i blocchi 2+ useranno 3137/3138.

  --- (2) DECISIONI D-4 e D-7: PRESE ---
  D-4 = SI: /2fa/enable pretendera' emailVerified (blocco 4).
  D-7 = 14 GIORNI di attesa del recupero ritardato.
  Registrate nel piano C:\Users\lenovo\.claude\plans\camoscio-2fa-totp.md
  (sez. 13 + costante §1b + riquadro in testa). Le "7 giorni" residue nel
  piano riferite all'ATTESA vanno lette 14; la finestra POST-maturita' resta 7.

  --- (3) RILIEVI REVISIONE 35a: TUTTI CHIUSI ---
  (code-reviewer + security-privacy girati a fine 35a, nessun CRITICO.
   Dettaglio voce per voce in cose_da_fare.txt "REVISIONE 35a: RILIEVI".)

  URGENTE - Math.max(...array) -> RangeError oltre ~1,2e5 elementi (Node 24;
    una registrazione multi-day di quote ci arriva) -> la sessione non si
    chiudeva piu' e restava aperta per sempre. Sostituito con .reduce in
    lib/gpx.js (min E max), lib/percorso.js, routes/tracking.js. Nuova
    prova-quote-traccia-lunga.js 9/9 (+ controprova d'ambiente inclusa).

  11 VOCI DI CORRETTEZZA (M-5 contatti emergenza + Punto 1 backend +
    tracking.js frontend):
    1  distanceKm arrotondato a 3 decimali in misureDaSessione (era coda binaria)
    2  maxAltitudeM aggiunto a campiTraccia del ⬆ (routes/completions.js)
    3  DELETE contatto: email SEMPRE nel filtro ({$in:[null,'']} se assente)
       - non porta piu' via l'omonimo con email
    4  registrazione senza quota (punti a 0) NON sovrascrive maxAltitude/
       elevationGain messi a mano; distanza e linea passano comunque
    5  ripiego D5 su sessione da ⬆ (importedFrom:'gpx') -> etichetta 'gpx'
       + tiene importedName, non 'live'/'Traccia registrata'
    6  validator.isEmail in validaUnContatto (era includes('@'))
    7  toast "Contatto rimosso" solo se la voce e' davvero sparita
    8  phone:'' non scritto sul sotto-documento; niente doppioni ($elemMatch
       nel filtro atomico -> 409)
    9  /end fallito: non si passa piu' l'id di una sessione ancora 'active'
       (era 409); + res.ok prima di res.json()
    10 trackingSessionId verificato come stringa; gpxText presente ma vuoto -> 400
    11 scelta checklist-vs-completamento-singolo decisa AL CLICK, non al
       renderSummary con CamoscioState vecchio
    Prove: prova-contatti-emergenza 28->48, prova-punto1-tracciamento-gruppo
    49->74 (controprove -12 / -11).

  MEDIO-1 - complete-group scriveva routePath e groupCompletedAt in due
    save() separati: finestra (e caso di guasto durevole) in cui la traccia
    GPS del creatore era pubblica a ogni loggato; se il ciclo per-confermato
    sollevava, l'escursione restava PUBBLICA PER SEMPRE. Ora un solo save()
    atomico (groupCompletedAt/participants/pendingApproval prima del ciclo).

  TTL NOTIFICATION:
    MEDIO-2 - verificato su Atlas (scripts/verifica-indici-notifiche.js,
      sola lettura): l'indice TTL createdAt_1 c'e', expireAfterSeconds 90gg,
      nessun IndexOptionsConflict inghiottito. Nessuna azione.
    BASSO-2 - i promemoria "completa l'escursione in gruppo" risorgevano ogni
      90 giorni (il TTL cancellava la guardia anti-doppione). ensureCompletion
      Reminders ora guarda solo le escursioni scadute negli ultimi 60 giorni
      (< 90 del TTL). Nuova prova-promemoria-completamento.js 7/7.
    BASSO-3 - il TTL cancellava l'unica traccia di un allarme DMS scattato coi
      nomi dei contatti non raggiunti. Nuovo User.deadManLastFired (at +
      contattiNonRaggiunti), niente TTL, privato (ALWAYS_PRIVATE_FIELDS), tolto
      da scrubAccount e da /activate + /deactivate. Riquadro persistente nel
      pannello sicurezza con "Ho capito". Nuova prova-deadman-esito-fallito.js
      8/8 (monkeypatch di inviaEmail, gestisciScadenza esportata per il test).

  BASSO-1 - i due console.error dei contatti loggano e.name/e.message, non
    l'oggetto intero (un ValidationError porta .value = dati di terzi).

  D5 (DECISIONE DI DENIS, 08/09/2026): tenuto l'automatismo del ripiego lato
    server, MA aggiunte due difese ->
    - PUT /api/hikes/:id { routePath: null } (da solo): il creatore RITIRA la
      traccia GPS auto-pubblicata anche a escursione conclusa. Unica eccezione
      al lock del punto 76; toglie routePath + azzera routeSource.
    - complete-group SALTA il ripiego D5 se il creatore ha revocato il
      consenso geo (con un trackingSessionId ESPLICITO invece procede: e' un
      atto suo). I demo bypassano (nessun consenso vero).
    prova-punto1-tracciamento-gruppo sez. 11 (12 controlli, controprova -6).

  REGRESSIONI: 28 suite tutte verdi, npm audit --omit=dev = 0.

  DA VERIFICARE DAL VIVO (frontend, niente prove di navigazione automatiche):
   - correttezza #9 e #11 in tracking.js (checklist vs singolo, /end fallito);
   - il riquadro "ultimo allarme fallito" nel pannello sicurezza (BASSO-3).

  --- GIRO code-reviewer + security-privacy: FATTO (08/09) ---
  Girati sul cumulativo `git diff 193c234` (6 auto-save + working tree).
  NESSUN CRITICO. Nessuna regressione di sicurezza in cio' che la 39a ha
  introdotto (security-privacy ha ESEGUITO i controlli). Il consolidato
  completo dei rilievi e' in cose_da_fare.txt "REVISIONE DEL CUMULATIVO
  39a". In sintesi:

  * ALTO-1 (PREESISTENTE, serve una DECISIONE di Denis): routes/
    completions.js - il creatore che si auto-completa e carica il .gpx
    col ⬆ scrive hike.routePath SENZA groupCompletedAt -> visibile a ogni
    loggato (hikeVisibileA non toglie routePath ai non-partecipanti).
    Gemello identico di MEDIO-1. Opzioni: (a) filtro in hikeVisibileA per
    kind 'live'/'fit'; (b) "e' pubblico di proposito" -> informativa +
    avviso; (c) lasciarlo aperto come rilievo dedicato (il push va avanti
    lo stesso).
  * MEDIO-2 (serve una DECISIONE): la via di ritiro D5 (PUT {routePath:
    null}) e' provata ma NESSUN pulsante la chiama, e il modale non
    avvisa sul ripiego automatico. Da decidere se cablare nota+pulsante
    ora o in una passata frontend dedicata.
  * BATCH DI FIX CHIARI da applicare (nessuna decisione): lib/totp.js
    (guardia segreto vuoto -> HMAC con chiave vuota non lancia su Node 24;
    tetto su `finestra`; TOGLIERE il rifiuto del modulo in
    generaCodiciRecupero - 256 E' 32*8, il rifiuto INTRODUCE il bias, e'
    un errore ereditato dal piano §7; scartoDiPasso con adessoMs).
    deadManLastFired (ramo "nessun contatto con email"; togliere il nome
    dal $pull della DELETE contatto - sopravvive nell'export GDPR; tetto
    180gg in lettura). DMS corse fra schede (endpoint dedicato "Ho
    capito", non /deactivate; CAS in gestisciScadenza su deadManExpiresAt;
    sicurezzaLimiter su /deactivate). salvaNuovoContatto legge body.error
    + stringere il check email client. Ramo D5 della PUT -> `else if`.
    3 chiavi i18n EN. prova-deadman-esito-fallito con scadenza futura.
    2 commenti + nota quoteVere (quote parziali).

  ►►► DA DOVE SI RIPARTE:
   1. Decidere ALTO-1 e MEDIO-2 con Denis.
   2. Applicare il BATCH DI FIX CHIARI (+ le decisioni prese).
   3. Rifondere i 6 auto-save in commit veri (git reset --soft 193c234),
      poi push (col via di Denis).
   4. Verifiche dal vivo di Denis: correttezza #9/#11 in tracking.js,
      riquadro "ultimo allarme fallito", + prova al telefono del Punto 1.
   5. 2FA: blocco 2 del piano (schema User + reti di sicurezza), poi
      3..9. Confine non negoziabile: blocco 3 prima del 4.

--- 2FA TOTP - DIREZIONE (dal piano, per contesto) ---
"Accedi con Google" (OAuth) resta IN SOSPESO (33a): serve prima l'informativa
privacy vera. Il 2FA TOTP invece e' coerente col vincolo open source.
Opzione C sul reset: recupero via email SENZA secondo fattore, 14 giorni di
attesa + banner in-app globale (la difesa vera: se la casella e' compromessa
l'email di avviso la legge l'attaccante). 9 blocchi di commit, piano §11.

>>> APERTI (invariati):
  - CONFORMITA' GDPR + NOTA LEGALE: index.html fa accettare "Termini +
    Informativa privacy" senza documento dietro. Serve un consulente privacy/
    avvocato GDPR (non un DPO) per l'Informativa vera + il testo di consenso
    prima di accettare un invito. security-privacy 35a lo segna ALTO-2.
  - VERIFICA DAL VIVO di Denis su tutto l'invito squadra direzionale (entrare
    in una squadra; invito squadra a un'escursione; uscita e successione del creatore).
  - PROVA AL TELEFONO del Punto 1 (solo Denis): registrare un'escursione vera
    collegata, terminarla, checklist col GPS caldo. Poi la RICHIESTA 2 (un
    partecipante che dopo carica la sua traccia col ⬆).
  - MEDIO-5 (security-privacy 35a): i tempi misurati dal creatore si applicano
    identici a ogni confermato e finiscono in "Velocita' Ascesa X m/h" sulla
    Dashboard. GDPR art. 5(1)(d). MA E' LA DECISIONE CHE DENIS HA GIA' PRESO
    APPOSTA (33a/35a: tempo di cammino uniforme, il singolo lo sostituisce col
    ⬆). Se un domani si vuole chiudere: campo Completion.tempiDalCreatore +
    etichetta, oppure non propagare i tempi ai non-creatori nella corsia D5.
  - #9CA3AF degli assi Chart.js -> --color-text-secondary (piccolo cambio
    visivo, punto a se' fuori audit).

>>> LAVORO FUTURO PREPARATO, agli atti (NIENTE codice):
    FILONE B: collezione "I 2000 dell'Appennino" - bloccato, LA LISTA
    UFFICIALE DELLE VETTE LA FORNISCE DENIS. Piano:
      C:\Users\lenovo\.claude\plans\camoscio-2000-appennino-collezione.md

>>> AZIONI MANUALI DI DENIS, aperte:
  1. ACCOUNT_SCRUB_SECRET su Render (stringa a scelta, DIVERSA da
     SAFETY_CRON_SECRET) + un job giornaliero su cron-job.org verso:
       https://camoscio.onrender.com/api/users/scrub-eliminati?chiave=<segreto>
     Senza, gli account eliminati restano "Account eliminato" PER SEMPRE.
     Il server lo urla nei log all'avvio in produzione. Fail-safe, ma da fare.
  2. Ricaricare il .gpx/.fit col ⬆ per le vecchie escursioni da pubblicare
     nel feed (l'originale non era stato conservato).

>>> ALTRE APERTE, non urgenti:
    dominio email proprio (rimandato) - causa vera delle email in spam.

>>> RESTA VALIDO:
  - Hook "Stop" di auto-save: commit locale (MAI push) a fine di ogni
    risposta se ci sono modifiche non salvate. Va rifuso in un commit vero
    prima di ogni push.
  - Traduzione IT/EN (punto 102, CHIUSO): resta ITALIANO di proposito tutto
    il testo scritto dagli utenti, i nomi di luogo, i percorsi salvati,
    titolo/descrizione dei badge, le schede "i", il testo delle notifiche
    (lo genera il server), il segnaposto nativo di <input type=date/file>,
    e componiRapporto() dentro diagnostica-gps. Le chiavi EN dei blocchi
    nuovi si aggiungono mano a mano.
  - "Account eliminato" (punto A-3.4) SI traduce: common.accountEliminato ->
    "Deleted account".

--- LAVORI DELLE SESSIONI 33a-35a, FATTI E PUSHATI (per contesto) ---
  TASTO INDIETRO / History API (33a): riscritto il nucleo di navigazione in
    public/js/app.js. Pushato (7793426).
  RITOCCHI VISIVI FASCIA A/B/C (33a-37a): audit dell'agente designer,
    C:\Users\lenovo\.claude\plans\camoscio-audit-visivo-29a.md. Tutto
    pushato (fino a 6368ad3). Non fatte: B1 (empty-state), B5 (scala testo).
  PUNTO 1 (35a): completamento di gruppo da tracciamento dal vivo - la
    registrazione diventa la traccia CONDIVISA. Backend + frontend, pushato
    (in 7496ea3..844967b). Piano:
      C:\Users\lenovo\.claude\plans\camoscio-punto1-completamento-tracciamento.md
  TRE PICCOLI PUNTI (35a): cestino card completate (Denis: "resta cosi");
    M-5 rotte atomiche POST/DELETE contatti emergenza; TTL 90gg Notification.
    Pushati.

===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — congelato dal 6/08/2026: da quella data il racconto
    disteso di ogni sessione vive solo in ../camoscio memoria/06-Cronologia-Sessioni.md,
    non qui. Utile solo per il dettaglio di lavoro PRIMA di quella data.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
MAI piu' (congelato); cose_da_fare.txt quando un punto si chiude o ne arriva uno nuovo.

>>> LEZIONI OPERATIVE (git dopo un'interruzione, moduli che si collegano tardi,
    componenti condivisi fra piu' pagine, .toObject vs .toJSON, $unset di campi
    required, select('username') che scavalca la pseudonimizzazione, recupero
    password vs grazia dell'eliminazione, npm audit fix vs overrides, serializzatore
    aggiunto a una rotta senza try/catch, test verde su una premessa falsa,
    select:false non vale su un campo assegnato in memoria, select:false su
    findByIdAndUpdate va reso condizionale su una rotta condivisa, un gate privacy
    condiviso va dichiarato fail-closed, un subagent puo' dichiarare falsamente di
    aver aggiornato la memoria, due funzioni di render sullo stesso dato ma DOM
    diversi non danno errore se chiami quella sbagliata, un history.back() e'
    asincrono e corre contro un pushState immediato, una navigazione che cambia solo
    l'hash non ricarica la SPA, il service worker MV3 dell'estensione Chrome si
    sospende ("attiva" != "connessa"), una prova che fa login come account demo e
    lancia recalculateAndApplyPace ne AZZERA il passo assegnato a mano,
    Math.max(...array) va in RangeError oltre ~1,2e5 elementi - usare .reduce,
    flippare isDemoAccount su un demo in una prova rompe .save() - usare un utente
    vero temporaneo, monkeypatch di un import destrutturato va fatto PRIMA di
    require del modulo che lo destruttura): tutte in
    ../camoscio memoria/07-Trappole-Tecniche.md — quella e' la sede dove si guarda
    prima di toccare codice a rischio. NB dal 06/09: il dislivello ha DUE soglie -
    0 per le tracce importate da un file di dispositivo (grezzo), 10 per le quote
    stimate (percorso progettato, GPS dal vivo). Vedi lib/gpx.js.
===================================================================
