===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-09-18 (55a sessione, CHIUSA in sicurezza). Piano CSP completo,
con tutte le decisioni gia' prese da Denis (D-1...D-7):
C:\Users\lenovo\.claude\plans\camoscio-csp-header-sicurezza.md - non
richiede altre domande, solo eseguire la tappa rimasta (in pausa, vedi sotto).

  >>> DA DOVE SI RIPARTE (56a sessione):
   1. CSP: TAPPE 1-4 di 5 chiuse per davvero. TAPPA 5 (enforcement, 1 riga)
      PROPOSTA nella 55a e MESSA IN PAUSA su richiesta di Denis (telefono
      non a portata di mano in quel momento - non rifiutata, solo
      rimandata). Condizione per procedere: zero violazioni Report-Only su
      desktop E telefono Android (debug remoto Chrome - rischio noto: il
      bridge Capacitor potrebbe non reggere la CSP, vedi §2.1 del piano).
      Il giro telefono NON e' stato fatto: serve Denis col telefono in
      mano, non si puo' fare da soli.
   2. Il resto del backlog MEDIO/BASSO della verifica generale (CSP a
      parte): otto chiusi finora (canale notifiche/DMS, Hike.routePath
      a select:false il 18/09 nella 54a; punti GPS orfani, la controprova
      mappa/tracciamento, tracking.js #9/#11, il riquadro "ultimo allarme
      fallito" e la nota nel modale "Completa" nella 55a - vedi
      06-Cronologia-Sessioni.md sezione "55a" per il dettaglio di ognuno),
      il resto resta in coda - vedi 04-Da-Fare.md.
   3. Il debito tecnico minore (Marche/Molise nell'indice sentieri, file
      LICENSE, sgonfiare il .git) e' stato riproposto a Denis: ognuno ha
      bisogno di una sua decisione o di un lavoro di misura prima, nessuno
      scelto per la 55a - vedi 04-Da-Fare.md, sezione "Debito tecnico".
   4. Nessun'altra richiesta di Denis in sospeso al momento.

FATTO nella 55a sessione (dettaglio completo per ognuno in
../camoscio memoria/06-Cronologia-Sessioni.md, sezione "55a" - qui solo
l'elenco, per restare corto):
 - M-5: verificato gia' risolto dal codice dalla 35a (deadManContactIndex
   rimosso, DELETE contatto gia' atomico per contenuto) - nota vault
   corretta, nessun codice toccato.
 - Punti GPS orfani in IndexedDB: CHIUSO (nuova idbPurgeOrphanedPoints in
   idb.js, due agganci in tracking.js).
 - Controprova rigorosa mappa/tracciamento (dalla 47a): CHIUSA (condizione
   avversa riprodotta per davvero, boundaries rallentata ad arte -
   modifica temporanea, mai committata).
 - Tracking.js #9 e #11 (dalla 35a): CHIUSI (verificati dal vivo con
   un'escursione e sessione di prova reali).
 - Pannello Sicurezza "ultimo allarme fallito" + nota nel modale
   "Completa": CHIUSI (il riquadro vive su Mappa, non su Sicurezza -
   corretta la nota del vault).
 - Tappa 5 CSP proposta da Denis, messa in pausa su sua richiesta.
 - Responsive Dashboard <900px: trovata prova statica forte nel CSS (il
   collasso a una colonna e' gia' misurato/corretto - styles.css:5677-5680)
   ma il vero test dal vivo su schermo stretto non e' riuscito (resize_window
   non cambia window.innerWidth in questo ambiente di automazione - vedi
   07-Trappole-Tecniche.md). Denis ha scelto di tenerlo aperto per un giro
   vero col telefono, non chiuso sulla sola prova statica.
 - Trovato per strada, non collegato a nessun fix: un tip pubblicitario di
   dotenv (pacchetto vero, non manomesso) che nomina www.vestauth.com e
   spedisce istruzioni per agenti AI in node_modules/dotenv/skills/ -
   indagato, concluso innocuo, non seguito. Vedi 07-Trappole-Tecniche.md.
 - Fine sessione: commit locali rifusi in uno pulito e PUSHATO (47ac9b4),
   dopo aver rilanciato 315/315 controlli automatici rilevanti (nessuna
   regressione). Vedi "RESTA VALIDO" sotto.

>>> APERTI (invariati salvo dove segnato):
  - CONFORMITA' GDPR + NOTA LEGALE: index.html fa accettare "Termini +
    Informativa privacy" senza documento dietro. Serve un consulente privacy/
    avvocato GDPR (non un DPO) per l'Informativa vera + il testo di consenso
    prima di accettare un invito. security-privacy 35a lo segna ALTO-2. Nel
    frattempo si sono accumulati altri trattamenti da far validare insieme:
    (a) l'automatismo D5 pubblica una traccia GPS al gruppo senza opt-in
    esplicito; (b) deadManLastFired conserva i nomi di contatti di emergenza
    non-utenti; (c) AccountRecovery (2FA) conserva per ~21 giorni la traccia
    di un tentativo di presa dell'account; (d) selezionare un RouteDraft
    proprio come "percorso da seguire" puo' ancora mandare la geometria a
    open-meteo per il route planner; (e) (44a) trasferimenti a terzi mai
    dichiarati: opentopomap.org, nominatim/overpass (ricerca luoghi),
    open-meteo (quote); (f) (44a) import-gpx e completions/:id/gpx creano
    una sessione di posizione fuori dal gate di consenso geolocalizzazione;
    (g) (44a) nessun TTL sulle tracce GPS, restano per sempre anche dopo
    l'eliminazione dell'account; (h) (45a) open-meteo riceve dal BROWSER
    posizione esatta (backpack.js manda anche ritrovo+data futura) - qui il
    terzo vede l'utente direttamente, non passa dal server; (i) (45a) il
    consenso geo resta valido anche se la scrittura che lo registra fallisce,
    e non ha una data; (j) (45a) il logout non cancella i dati locali sul
    dispositivo (posizione reale, zona di casa, stato DMS, coda GPS); (k)
    (45a) la posizione viaggia su ogni messaggio della chat mesh senza
    informativa ne' interruttore; (l) (45a) GET /api/reports espone a tutti
    chi ha segnalato, dove e quando; (m) (45a, PARZIALMENTE CHIUSO 50a) nessuna
    scadenza sui dati locali - la cache tile ora ha un tetto/pulizia, la coda
    GPS orfana ancora no (vedi debito tecnico sotto); (n) (45a, CHIUSO -
    verificato conforme) le foto NON portano EXIF/GPS, tutti gli input passano
    da un ricompressore canvas che li distrugge - da nominare comunque come
    rassicurazione quando l'informativa esiste.
  - VERIFICHE DAL VIVO ancora aperte, serve il telefono (come la Tappa 5
    CSP): la prova al telefono del Punto 1 (tracciamento di gruppo) +
    RICHIESTA 2 (un partecipante che dopo carica la sua traccia); il
    Responsive Dashboard <900px (prova statica forte gia' fatta, vedi FATTO
    sopra - Denis ha scelto di tenerlo aperto per un giro vero).
  - MEDIO-5 (security-privacy 35a): i tempi misurati dal creatore si applicano
    identici a ogni confermato e finiscono in "Velocita' Ascesa X m/h" sulla
    Dashboard. GDPR art. 5(1)(d). MA E' LA DECISIONE CHE DENIS HA GIA' PRESO
    APPOSTA (33a/35a: tempo di cammino uniforme, il singolo lo sostituisce
    col ⬆).
  - INVITO SQUADRA DIREZIONALE: solo un caso su tre confermato dal vivo
    (43a) - Denis ha scelto di chiudere comunque il punto cosi'.

>>> LAVORO FUTURO PREPARATO, agli atti (NIENTE codice):
    FILONE B: collezione "I 2000 dell'Appennino" - bloccato, LA LISTA
    UFFICIALE DELLE VETTE LA FORNISCE DENIS. Piano:
      C:\Users\lenovo\.claude\plans\camoscio-2000-appennino-collezione.md

>>> AZIONI MANUALI DI DENIS, aperte:
  1. ACCOUNT_SCRUB_SECRET su Render (stringa a scelta, DIVERSA da
     SAFETY_CRON_SECRET) + un job giornaliero su cron-job.org verso:
       https://camoscio.onrender.com/api/users/scrub-eliminati?chiave=<segreto>
     Senza, gli account eliminati restano "Account eliminato" PER SEMPRE.
     NB: il job SAFETY_CRON_SECRET (scadenze DMS) ora fa ANCHE la spazzata
     dei deadManLastFired oltre 180 giorni - se non esiste ancora, va creato.
  2. Ricaricare il .gpx/.fit col ⬆ per le vecchie escursioni da pubblicare
     nel feed (l'originale non era stato conservato).

>>> ALTRE APERTE, non urgenti:
    dominio email proprio (rimandato) - causa vera delle email in spam.

>>> RESTA VALIDO:
  - Hook "Stop" di auto-save: commit locale (MAI push) a fine di ogni
    risposta se ci sono modifiche non salvate. Va rifuso in un commit vero
    prima di ogni push (git status/git log origin/master..HEAD per il
    conteggio esatto: crescono a ogni risposta con modifiche). Il codice
    della 52a-53a (Tappe 1-2 CSP) e' stato pushato il 17/09 (cc070d7). Il
    codice della 54a (Tappe 3-4 CSP, canale DMS, Hike.routePath) e della
    55a (punti GPS orfani) e' stato rifuso in UN commit pulito e PUSHATO
    il 18/09 (47ac9b4, 20 file) - richiesto esplicitamente da Denis a fine
    sessione, dopo aver rilanciato 315/315 controlli automatici rilevanti
    (prova-punto77, prova-punto1-tracciamento-gruppo, prova-invito-squadra,
    prova-promemoria-completamento, prova-deadman-esito-fallito,
    prova-partecipanti-forzati, prova-punto111 - tutti verdi, nessuna
    regressione). Repo camoscio e vault entrambi allineati al loro remoto:
    nessun commit locale in sospeso a fine 55a. Il resto del lavoro della
    55a (controprova mappa/tracciamento, tracking.js #9/#11, pannello
    Sicurezza, nota modale, responsive dashboard) non ha lasciato codice
    permanente (solo prove contro un server locale, nessun file toccato).
  - Traduzione IT/EN (punto 102, CHIUSO): resta ITALIANO di proposito tutto
    il testo scritto dagli utenti, i nomi di luogo, i percorsi salvati,
    titolo/descrizione dei badge, le schede "i", il testo delle notifiche
    (lo genera il server), il segnaposto nativo di <input type=date/file>,
    e componiRapporto() dentro diagnostica-gps. Le chiavi EN dei blocchi
    nuovi si aggiungono mano a mano.
  - "Account eliminato" (punto A-3.4) SI traduce: common.accountEliminato ->
    "Deleted account".

--- LAVORI PRECEDENTI FATTI (per contesto, dal piu' vecchio) ---
  TASTO INDIETRO / History API (33a): riscritto il nucleo di navigazione in
    public/js/app.js. Pushato (7793426).
  RITOCCHI VISIVI FASCIA A/B/C (33a-37a): audit dell'agente designer.
    Pushati (fino a 6368ad3). Non fatte: B1 (empty-state), B5 (scala testo).
  PUNTO 1 (35a): completamento di gruppo da tracciamento dal vivo. Pushato.
  TRE PICCOLI PUNTI (35a): cestino card completate; M-5 rotte atomiche
    contatti emergenza; TTL 90gg Notification. Pushati.
  2FA COMPLETO, blocchi 1-9 + revisione (35a-47a): TUTTO CHIUSO, inclusi i
    due buchi di test e l'i18n EN (vedi 06-Cronologia-Sessioni.md 47a).
  43a SESSIONE (12/09): foto profilo, prova 2FA telefono, invito squadra,
    rifugi/assi/chat/percorso da seguire + revisione cumulativo - vedi
    dettaglio in cose_da_fare.txt.
  44a-45a-46a SESSIONI (12-14/09): verifica generale del codice a blocchi
    (routes/, lib/, public/js/) - 3/3 CRITICI + 17/18 ALTO corretti e
    verificati - vedi 06-Cronologia-Sessioni.md.
  47a SESSIONE (14/09): ultimo ALTO chiuso (spacchettamento di initApp() su
    11 file) - VERIFICA GENERALE CHIUSA PER INTERO (3/3 CRITICI, 18/18 ALTO).
    Poi BLOCCO 9 del piano 2FA CHIUSO. Auto-save di 45a/46a/47a rifusi e
    PUSHATI (a52d506). Vault pushato (610346d).
  48a-51a SESSIONI (14-16/09): sei MEDIO/BASSO della verifica generale
    chiusi uno alla volta ("oggi" UTC/locale, formula ore-CAI triplicata,
    backpack.js/quota mancante, codici 2FA nella finestra di stampa, cache
    tile offline senza tetto, tetto complessivo tile esplicite) - vedi
    06-Cronologia-Sessioni.md, sezioni "48a"-"51a". Auto-save (blocco 9 2FA
    + 48a-51a, 23 commit) rifusi e PUSHATI il 16/09 (05e2c3c).
  52a SESSIONE (16/09): CSP/header di sicurezza, piano progettato con
    l'agente architect (70 handler on* trovati, non 5). TAPPA 1 DI 5 CHIUSA
    (script inline delle 5 pagine statiche estratti, 5 onclick di
    index.html agganciati con addEventListener) - vedi
    06-Cronologia-Sessioni.md, sezione "52a".
  53a SESSIONE (17/09): TAPPA 2 DI 5 CHIUSA (i 65 handler onclick/onchange
    generati da JS in 13 file, data-*+listener) - vedi
    06-Cronologia-Sessioni.md, sezione "53a". Tappe 1+2 rifuse in un
    commit e PUSHATE su richiesta di Denis (cc070d7).
  54a SESSIONE (18/09): TAPPE 3+4 DI 5 CSP CHIUSE (finestra stampa 2FA
    w.close nativo; middleware/securityHeaders.js nuovo, Report-Only
    attivo). Canale DMS che mentiva su un guasto (GET /api/notifications)
    CHIUSO. Hike.routePath a select:false CHIUSO (progettato con
    l'architect, 8 file) - vedi 06-Cronologia-Sessioni.md, sezione "54a".
  55a SESSIONE (18/09): M-5 verificato gia' chiuso dalla 35a (nota vault
    corretta). Punti GPS orfani in IndexedDB CHIUSO (idbPurgeOrphanedPoints).
    Tappa 5 CSP proposta e messa in pausa (serve il telefono di Denis).
    Quattro "verifiche dal vivo mai fatte" chiuse per davvero (non solo
    lette dal codice): controprova rigorosa mappa/tracciamento (dalla
    47a), tracking.js #9/#11 (dalla 35a), pannello Sicurezza "ultimo
    allarme fallito" + nota nel modale "Completa" (dalla 39a). Responsive
    Dashboard <900px: prova statica forte ma non dal vivo (limite dello
    strumento resize_window in questo ambiente) - lasciato aperto su
    scelta di Denis. Debito tecnico minore riproposto, nessuno scelto
    (ognuno serve una decisione di Denis o una misura prima) - vedi
    06-Cronologia-Sessioni.md, sezione "55a". Auto-save di 54a+55a rifusi
    in UN commit e PUSHATI il 18/09 (47ac9b4, 20 file), dopo 315/315
    controlli automatici rilevanti verdi.

===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — congelato dal 6/08/2026: da quella data il racconto
    disteso di ogni sessione vive solo in ../camoscio memoria/06-Cronologia-Sessioni.md,
    non qui. Utile solo per il dettaglio di lavoro PRIMA di quella data.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
MAI piu' (congelato); cose_da_fare.txt quando un punto si chiude o ne arriva uno nuovo.

>>> LEZIONI OPERATIVE: tutte in ../camoscio memoria/07-Trappole-Tecniche.md — quella
    e' la sede dove si guarda prima di toccare codice a rischio. Nella 55a: navigate()
    a un hash diverso sulla STESSA tab gia' caricata non attiva il router dell'app (lo
    stato JS resta ma la sezione visibile no) - serve una tab nuova o un click vero.
    Un handler reale azionato su stato globale non controllato prima puo' innescare un
    effetto collaterale vero (successo un completamento singolo per errore in un primo
    tentativo di prova). Il resto (dalla 54a a ritroso, IndexedDB, dato mancante che
    alimenta un giudizio binario, correggere un valore condiviso solo su un lato,
    RangeError su array grandi, formule duplicate che divergono, rate-limiter per IP fra
    estranei, ecc.) e' tutto nel file del vault, non ripetuto qui per tenerlo corto.
===================================================================
