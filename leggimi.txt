===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-09-03 (VENTISETTESIMA sessione — DUE lavori grossi.
(a) PUNTO 116: la traccia importata (.gpx/.fit) si disegna come LINEA
sulla mappa. (b) INVITO SQUADRA DIREZIONALE, chiuso per intero in 4
tappe + consenso squadra (rimandato dalla 26a). Push
`a090723..adf9ff2` su `master`, Render redeploy automatico.
--- (a) PUNTO 116 --- L'import .gpx in creazione ESISTEVA GIA' (tendina "Quota,
dislivello e distanza", routeSource kind:'gpx') ma calcolava solo i 3
numeri e BUTTAVA la geometria; ora si salva Hike.routePath ([[lng,lat]],
Douglas-Peucker 18 m, tetto 400 punti, disciplina di SavedRoute) e si
disegna su Esplora (loadActiveHikeOnMap, prima disegnava un percorso
FINTO di 5 punti) + mini-mappa nel tab Dettagli di hike-page. Aggiunto
anche il .fit: l'enum routeSource.kind non aveva 'fit' (bug latente dal
114). gpx/fit/saved valorizzano routePath, draft NO (si ricalcola). Un
solo tasto per i due formati, ramo dall'estensione. Costanti
SEMPLIFICA_PERCORSO_M / MAX_PUNTI_PERCORSO spostate in lib/percorso.js.
Prova prova-punto116 42/42 (7 sez.) + 7 regressioni verdi. Costo spazio
MISURATO: ~1,5-2 KB/escursione media, tetto ~12,7 KB — trascurabile.
SOLO IN AVANTI: le escursioni gia' create con un .gpx non hanno il file,
la linea compare re-importando. Dettaglio: vault 03 ("Traccia importata
-> linea sulla mappa") + 06 + 07, cose_da_fare.txt "PUNTO 116".
--- (b) INVITO SQUADRA DIREZIONALE --- 4 tappe, 0fce557->adf9ff2,
progetto dell'agente architect. Il gruppo mesh/SOS di un'escursione E'
`hike.participants`, e la corsia "compagno di squadra" della 26a NON era
un vero controllo di relazione: POST /api/squads scriveva `members` dal
client senza consenso ne' tetto, e non c'era rotta per uscire da una
squadra. Ora: Hike.pendingInvites + Squad.pendingInvites (default
undefined), semantica OPPOSTA a pendingApproval/pendingRequests (decide
LA PERSONA). Rotte: invite-squad / invite (il server legge i membri dal
doc Squad, mai id dal client), invite-response {accept} (CAS, rifiuto
mai bloccato, mai scritturaLimiter), DELETE /api/squads/:id/members/
:userId (LA PORTA D'USCITA che non esisteva - creatore che esce ->
creatorId al piu' anziano; ultimo membro -> squadra + chat sciolte).
lib/squad.js + lib/squadAdmin.js (promuoviSeSenzaAdmin estratta da
riassegnaAdminSquadre). Tappa 3 CHIUDE la corsia PUT: aggiungere un
compagno a mano e' IMPOSSIBILE (rischio deploy: scheda vecchia -> 403).
Tappa 4 (consenso squadra): migrazione NESSUNA. Prove nuove:
prova-invito-direzionale 41/41, prova-squadre-consenso 44/44; inversioni
verdi in prova-invito-squadra 60/60 e prova-partecipanti-forzati 27/27.
Dettaglio: vault 03 ("Invito squadra direzionale") + 06 + 07 ("il
creatore di una squadra e' membro per calcolo"), cose_da_fare.txt
"INVITO SQUADRA DIREZIONALE - CHIUSO PER INTERO".)

>>> SI LAVORA SU `master`, hotfix normali (commit vero + push -> Render
    ridistribuisce). L'hook di auto-save committa in locale (mai push):
    va rifuso in UN commit vero (`git reset <base>` + commit per pezzo)
    prima di ogni push. Fatto cosi' per il lavoro di oggi.

    STATO A FINE 27a SESSIONE:
      - camoscio       origin/master = `NOTE-27C` (codice a `adf9ff2`,
                       sopra solo il commit di note). Working tree PULITO.
      - camoscio memoria (vault) origin/master = `3d5cf5d`. Working tree PULITO.
      - Tutto pushato in entrambi i repo, niente in coda.
      - Prove verdi: prova-punto116 42/42, prova-invito-direzionale
        41/41, prova-squadre-consenso 44/44, prova-invito-squadra 60/60,
        prova-partecipanti-forzati 27/27, prova-eliminazione-account
        57/57, prova-elimina-escursione 52/52, prova-punto77 17/17,
        prova-punto93 28/28.

      >>> LA PROSSIMA SESSIONE PARTE DA QUI: HOTFIX SICUREZZA <<<
      L'agente security-privacy ha rivisto l'invito squadra (gia' in
      produzione): 1 CRITICO, 3 ALTO, 6 MEDIO, 6 BASSO. Rapporto:
        C:\Users\lenovo\.claude\plans\camoscio-review-sicurezza-invito-squadra.md
      Il TRIAGE completo (con le parole di Denis "salva tutto quanto
      riprendiamo nella prossima sessione") sta in cose_da_fare.txt,
      blocco "REVISIONE SICUREZZA".
      In breve:
       - C-1 (CRITICO): la corsia pendingApproval e' sopravvissuta - un
         partecipante non creatore propone V su una hike ad approvazione
         manuale, il creatore la approva, V finisce nel gruppo mesh/SOS
         senza dire si'. SERVE LA DECISIONE DI DENIS (chiudere = rovescia
         la sua risposta #2 "la proposta resta"). Consiglio: chiudere.
       - Fixare comunque (bug introdotti da questo lavoro): A-2 (DELETE
         members senza check appartenenza sulla corsia "se stesso"), M-1
         (nuovo proprietario squadra senza filtro vitalita'), A-1
         (Squad.name senza tetto x50 notifiche), M-3 (Squad.pendingInvites
         esce a tutti), B-1 (race sul $unset), B-2 (filtro
         pendingDeletionAt).
       - Da decidere con Denis: A-3 (npm audit fix), M-2 (tetto
         participants/pendingApproval in PUT), M-4 (scrub non fa $pull
         dagli inviti), M-5 (invitoLimiter), M-6 (complete-group non
         verifica la partecipazione - PREESISTENTE, la memoria diceva il
         contrario).
      Piano: decidere C-1, un unico giro di hotfix, prove, commit+push,
      secondo giro dell'agente.

      - APERTO ANCHE (non blocca l'hotfix): verifica dal vivo di Denis -
        (1) punto 116: linea su Esplora + card "Percorso previsto" nel tab
        Dettagli. (2) invito squadra: card Accetta/Rifiuta, gruppo
        "Inviti", creare una squadra -> inviti (non membri diretti),
        "Lascia la squadra", squadra rimasta vuota -> sparisce.
      - RISCHIO DEPLOY (accettato, gia' pushato): una scheda aperta col
        codice pre-tappa-2 che fa PUT {participants:[compagno]} prende
        un 403 col messaggio che lo spiega.

>>> AZIONI MANUALI DI DENIS, aperte:
  1. NUOVA — ACCOUNT_SCRUB_SECRET su Render (stringa a scelta, DIVERSA da
     SAFETY_CRON_SECRET) + un job giornaliero su cron-job.org (stesso
     account del ping DMS) verso:
       https://camoscio.onrender.com/api/users/scrub-eliminati?chiave=<segreto>
     Senza, gli account eliminati restano "Account eliminato" PER SEMPRE
     (nessuno scrub dei dati personali al giorno 30). Il server lo urla
     nei log all'avvio in produzione. Fail-safe, ma da fare.
  2. Ricaricare il .gpx/.fit col ⬆ per le vecchie escursioni da
     pubblicare nel feed (l'originale non era stato conservato).
  3. Camilla: `camilla2026` e' TEMPORANEA. Deve cambiarla da
     Impostazioni > Cambia password. Accede con EMAIL
     camilla.orsini@gmail.com (il login e' per email, non per username).

>>> APERTO, non bloccante:
  - PROVA MOBILE v2 sul telefono, SUL SITO LIVE: drawer/hamburger, i 4
    tab di hike-page (Dettagli/Chat/Carpooling/Zaino), la nuova
    #settings, il layout stretto. Il drawer non e' MAI stato provato su
    un telefono vero. Ritocchi = hotfix su `master`.
  - RESPONSIVE Dashboard <900px (regole @media di Fasi 4-5 viste solo in
    simulazione): 3 badge in fila, "Il tuo <anno>" a 2x2, "Vedi tutti i
    badge" a piena larghezza fuori dall'angolo del FAB.
  - NOTA LEGALE: index.html fa accettare "Termini + Informativa privacy"
    senza documento dietro. Serve un professionista. Gia' cosi' da prima,
    non e' una regressione — ma da non dimenticare prima di un uso
    pubblico vero.
  - M-5: race del cancella-contatto emergenza con 2 schede aperte (serve
    una DELETE per contenuto/id, non per indice posizionale). E' il piu'
    piccolo punto aperto — buon candidato per la prossima sessione.
  - TTL sulle Notification che nominano un utente (dall'agente): un
    "Mario Rossi ha chiesto di..." resta per sempre nelle notifiche
    altrui, col nome vero anche dopo l'eliminazione di Mario. Il fix
    (nomeVisibile, 24a sessione) copre solo le notifiche NUOVE. Cambio
    schema app-wide + migrazione -> a se'.
  - INVITO SQUADRA DIREZIONALE "decide l'invitato": FATTO nella 27a
    sessione (4 tappe + consenso squadra, 0fce557->adf9ff2). Vedi il
    blocco (b) del frontmatter qui sopra e cose_da_fare.txt.

>>> COSA E' STATO FATTO OGGI (dettaglio per esteso: vault 03/06/07 e
    cose_da_fare.txt "PUNTO 116" + "INVITO SQUADRA DIREZIONALE"):

  (a) PUNTO 116 — la traccia importata (.gpx/.fit) in creazione
  escursione si salva come polyline (Hike.routePath) e si disegna come
  linea su Esplora + mini-mappa nel tab Dettagli. Enum routeSource.kind
  + 'fit'. Commit 89d2e0b. Prova prova-punto116 42/42.

  (b) INVITO SQUADRA DIREZIONALE — rimandato dalla 26a, chiuso per
  intero in 4 tappe (0fce557->adf9ff2), progetto dell'agente architect
  (due giri: lato escursione, poi consenso squadra). Hike.pendingInvites
  + Squad.pendingInvites; rotte invite-squad/invite + invite-response;
  DELETE /api/squads/:id/members/:userId (la porta d'uscita che non
  c'era). Tappa 3 chiude la corsia PUT (aggiungere un compagno a mano e'
  impossibile) e con essa il buco §0. Tappa 4 (consenso squadra):
  migrazione nessuna. lib/squad.js + lib/squadAdmin.js nuovi
  (promuoviSeSenzaAdmin estratta da riassegnaAdminSquadre).
  Prove: prova-invito-direzionale 41/41, prova-squadre-consenso 44/44,
  + inversioni verdi (prova-invito-squadra 60/60, prova-partecipanti-
  forzati 27/27). Il dettaglio delle 6 decisioni di Denis e' in
  cose_da_fare.txt e vault 03.

>>> GIA' IN PRODUZIONE prima di oggi (origin/master era `d363eea` prima
    della 27a, `adf9ff2` dopo):
    26a sessione — DELETE /api/hikes/:id (il creatore cancella una sua
    escursione) + due vulnerabilita' preesistenti (creatore aggiungeva
    chiunque; complete-group senza tetto), 7d2de46+85d537b.
    25a sessione — tre rifiniture v2 (menu "Le mie escursioni"/"Community"
    aprono solo la tendina c0de245; Terminillo chiusa in gruppo non resta
    "In programma" c278e25; niente iscrizioni oltre il giorno previsto,
    409 HIKE_DATE_PASSED d363eea). 24a sessione — menu "Squadre" nella
    sidebar (122cb15); punto A-3.4
    eliminazione account, soft-delete in due tempi + giro agente
    security-privacy 1 critico + 5 alti chiusi (543cc4c); password
    temporanea per Camilla (camilla2026, DA CAMBIARE). Prima ancora:
    TUTTA LA REVISIONE UX "Camoscio v2" (merge del 02/09) — Dashboard
    rifatta, riordino menu a gruppi + drawer mobile, Blocco 0
    zaino/carpooling per-partecipanti, lotto sicurezza. Dettaglio:
    vault 04-Da-Fare ("[ARCHIVIO] revisione UX del sito") + 06
    ("Sedicesima"..."Ventitreesima sessione"). Prima ancora: traduzione
    IT/EN (102), timbri con traccia reale (108), moderazione
    segnalazioni (111), parte social — Segui/Feed/"mi piace"/"crea
    percorso" (113), import .fit (114), nome uscita editabile (115),
    hotfix Ju Busciu sogliaM (efd1290).
    >>> LIMITE .fit/.gpx, e' il FILE non il codice: un file "smart" (o da
        Garmin Connect) ha buchi -> niente split cammino/pause. Serve 1 Hz.
    >>> Smartwatch (Garmin/Suunto/Amazfit): valutato e SCARTATO.

>>> ALTRE APERTE, non urgenti (dettaglio in vault 04-Da-Fare.md):
    livelli ORIGINALI del punto 63b (gradini per la ZONA dichiarata a
    mano, DIVERSI dai livelli gia' fatti del punto 86) + calcolo
    automatico del raggio (punto 42a) — Denis deve ancora pensarci, non
    richiederlo; dominio email proprio (rimandato da lui, "non ora") —
    e' anche la causa vera delle email che non arrivano (spam).
    FILONE B: collezione "I 2000 dell'Appennino" dentro la pagina
    Progressi — bloccato, LA LISTA UFFICIALE DELLE VETTE LA FORNISCE
    DENIS. Piano: C:\Users\lenovo\.claude\plans\camoscio-2000-appennino-collezione.md

>>> RESTA VALIDO:
  - Hook "Stop" di auto-save: commit locale (MAI push) a fine di ogni
    risposta se ci sono modifiche non salvate. Va rifuso in un commit
    vero prima di ogni push/tappa.
  - Traduzione IT/EN (punto 102, CHIUSO): resta ITALIANO di proposito
    tutto il testo scritto dagli utenti (didascalie del feed comprese),
    i nomi propri di luogo, i nomi dei percorsi salvati e l'etichetta
    "da <autore>", titolo/descrizione dei badge personali, le schede "i"
    dei badge (punto 112), il testo delle notifiche (lo genera il
    server), il segnaposto nativo di <input type=date>/<input type=file>,
    e componiRapporto() dentro diagnostica-gps.html. Le chiavi EN dei
    blocchi nuovi si aggiungono mano a mano (scelta di Denis).
  - "Account eliminato" (punto A-3.4) SI traduce (e' interfaccia):
    common.accountEliminato -> "Deleted account".

===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — congelato dal 6/08/2026: da quella data il racconto
    disteso di ogni sessione vive solo in ../camoscio memoria/06-Cronologia-Sessioni.md,
    non qui. Utile solo per il dettaglio di lavoro PRIMA di quella data.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
MAI piu' (congelato); cose_da_fare.txt quando un punto si chiude o ne arriva uno nuovo.

>>> LEZIONI OPERATIVE (git dopo un'interruzione, moduli che si collegano tardi,
    componenti condivisi fra piu' pagine, .toObject vs .toJSON, $unset di campi
    required, select('username') che scavalca la pseudonimizzazione, recupero
    password vs grazia dell'eliminazione): tutte in
    ../camoscio memoria/07-Trappole-Tecniche.md — quella e' la sede dove si guarda
    prima di toccare codice a rischio.
===================================================================
