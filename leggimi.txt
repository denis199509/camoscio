===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-09-05 (TRENTADUESIMA sessione — chiusi gli ultimi tre
follow-up di sicurezza (B-5, M-4, B-2), poi un giro `security-privacy` sul
cumulativo: GO, con 3 MEDIO + 4 BASSO chiusi nello stesso blocco (uno era
un difetto funzionale vero, non solo sicurezza — vedi sotto). Denis ha
anche chiuso a voce altri tre punti aperti da tempo.

--- COSA E' STATO CHIUSO ---
Denis: "cominciamo con b-2, m-4 e b-5" -> i tre lavorati in ordine dal
piu' piccolo, ognuno con prova nuova + controprova.

 B-5   La propria foto profilo non si sincronizzava piu' fra schede/
       dispositivi diversi (regressione accettata dal fix MEDIO della
       31a, che ha tolto profilePhoto da GET /api/users). Un listener
       visibilitychange (public/js/app.js) ri-scarica SOLO la propria
       foto quando la scheda torna visibile - non a ogni cambio
       sezione, per non reintrodurre lo spreco di banda appena tolto.
 M-4   GET /api/users caricava email/emergencyContacts/birthDate/
       homeCity per INTERO per ogni utente, per poi cancellarli in JS -
       minimizzazione mancata su dati di TERZE PERSONE. La query verso
       gli altri usa ora una proiezione che li esclude; il proprio
       documento resta separato (altrimenti currentUser perderebbe i
       propri campi). Tolto anche l'N+1 di areSquadmates.
 B-2   Tre punti rimandavano l'errore di un percorso al client senza
       controllare se fosse sicuro da mostrare. Il marcatore .utente
       che sembrava "da inventare" esisteva gia' (ErroreGpx/ErroreFit)
       - si perdeva solo a ogni ri-lancio. Nuovo erroreUtente() applicato
       a tutti i punti throw di lib/percorso.js.

 GIRO security-privacy sul cumulativo: GO. MEDIO-1 era un difetto
 FUNZIONALE vero (non solo sicurezza): il tentativo di ridisegnare la
 pagina Profilo dopo il sync chiamava la funzione sbagliata (zero
 effetto sulla vista) - corretto e verificato dal vivo con l'estensione
 Chrome, due schede reali. MEDIO-2 (secchio di lettura foto condiviso
 con chi guarda il profilo di altri - soglia 5 min), MEDIO-3 (il Set
 precalcolato ora porta con se' il viewerId per cui e' stato calcolato,
 fail-closed se non combacia), BASSO-1..4 (try/catch mancante, log
 dell'errore originale su un fallimento imprevisto, due rifiniture alle
 prove, due difese a costo zero). Tutto chiuso nel commit 617edde.

>>> STATO A FINE 32a SESSIONE:
      - camoscio: 1 commit avanti a origin/master (617edde), NON
        PUSHATO - in attesa del via esplicito di Denis. Working tree
        pulito, gli auto-save di sessione sono stati rifusi con
        `git reset --soft` prima di committare.
      - Prove verdi: prova-lista-utenti-minimizzata 31/31 (nuova),
        prova-percorso-errori-sicuri 19/19 (nuova), prova-punto93
        28/28, prova-punto116 42/42, prova-punto113-gpx 43/43,
        prova-fit 33/33, prova-foto-profilo-privacy 18/18, prova-
        foto-profilo 14/14, prova-eliminazione-account 57/57, prova-
        squadre-consenso 71/71, prova-configurazione 9/9, prova-
        validazione-registrazione 10/10, prova-registrazione-senza-
        email 7/7.
      - npm audit --omit=dev = 0.
      - Verificato DAL VIVO con l'estensione Chrome (account demo
        Marco Alpinista, due schede): la foto cambiata "da un altro
        dispositivo" si vede sia sull'header sia sulla pagina Profilo
        senza ricaricare, nessun errore in console.

>>> APERTI (follow-up sicurezza, invariati da questo giro):
  - B-2 residuo: nessuno, il punto e' chiuso per intero.
  - M-4 residuo: nessuno, il punto e' chiuso per intero.
  - B-5 residuo: nessuno, il punto e' chiuso per intero.
  - CONFORMITA' GDPR (professionista): invariata, su richiesta
    esplicita di Denis ("lasciamo ancora aperto il punto con il
    professionista"). Serve un consulente privacy/avvocato GDPR (non
    un DPO formale) per scrivere una vera Informativa Privacy + il
    testo di consenso prima di accettare un invito.
  - VERIFICA DAL VIVO di Denis su tutto l'invito squadra direzionale:
    invariata. Spiegato in questa sessione il tipo di prova da fare:
    (1) invito a entrare in una squadra, (2) invito squadra a
    un'escursione (il controllo piu' importante: il creatore non deve
    avere piu' modo di aggiungere un compagno di squadra come
    partecipante SENZA passare dall'invito), (3) uscita da una squadra
    e successione del creatore. Serve piu' di un account (demo +
    Camilla, o due dispositivi/browser).

>>> ATTENZIONE, giro agente di questa sessione (SECONDA VOLTA, dopo la
    31a): l'agente `security-privacy` ha dichiarato di aver aggiornato
    file nella memoria personale di Claude Code - VERIFICATO E
    RISULTATO FALSO di nuovo, nessuno di quei file esiste, MEMORY.md
    non toccato. Confermato pattern: non fidarsi delle affermazioni
    collaterali di un agente sulle proprie azioni, solo dei rilievi
    tecnici verificabili leggendo il codice vero (quelli restano
    affidabili, verificati due volte su due).

>>> SI LAVORA SU `master`, hotfix normali (commit vero + push -> Render
    ridistribuisce). L'hook di auto-save committa in locale (mai push):
    va rifuso in UN commit vero prima di ogni push
    (`git reset --soft origin/master`) - fatto anche in questa sessione.

>>> AZIONI MANUALI DI DENIS, aperte (invariate dalla 30a):
  1. ACCOUNT_SCRUB_SECRET su Render (stringa a scelta, DIVERSA da
     SAFETY_CRON_SECRET) + un job giornaliero su cron-job.org verso:
       https://camoscio.onrender.com/api/users/scrub-eliminati?chiave=<segreto>
     Senza, gli account eliminati restano "Account eliminato" PER
     SEMPRE (nessuno scrub al giorno 30). Il server lo urla nei log
     all'avvio in produzione. Fail-safe, ma da fare.
  2. Ricaricare il .gpx/.fit col ⬆ per le vecchie escursioni da
     pubblicare nel feed (l'originale non era stato conservato).
  3. Camilla: `camilla2026` e' TEMPORANEA. Deve cambiarla da
     Impostazioni > Cambia password. Accede con EMAIL
     camilla.orsini@gmail.com (il login e' per email, non username).

>>> CHIUSI A VOCE in questa sessione, nessun codice:
  - PROVA MOBILE v2 sul telefono, sul sito live: Denis la considera
    verificata.
  - RESPONSIVE Dashboard <900px: Denis la considera verificata.
  - Punto 63b (livelli originali della zona "esperto locale" dichiarata
    a mano) e punto 42a (calcolo automatico del raggio): Denis ha detto
    che NON SERVONO PIU'. Tolti dagli aperti.

>>> APERTO, non bloccante (invariato):
  - NOTA LEGALE: index.html fa accettare "Termini + Informativa
    privacy" senza documento dietro. Stesso professionista del punto
    GDPR sopra puo' chiuderli insieme.
  - M-5 (il vecchio, diverso dagli M della revisione sicurezza): race
    del cancella-contatto emergenza con 2 schede aperte (serve una
    DELETE per contenuto/id, non per indice posizionale).
  - TTL sulle Notification che nominano un utente: un "Mario Rossi
    ha chiesto di..." resta per sempre nelle notifiche altrui col
    nome vero anche dopo l'eliminazione di Mario. Cambio schema
    app-wide + migrazione -> a se'.
  - nascondere il cestino sulle card "completate in gruppo".

>>> LAVORO FUTURO PREPARATO (progetti agli atti, NIENTE codice
    scritto). RICHIESTA ESPLICITA DI DENIS: proporglielo tu stesso
    all'inizio della prossima sessione se non lo porta su lui stesso
    ("faremmo quello in caso mi dimentico proponi tu stesso quella
    parte") - non aspettare che lo chieda.
    A) TASTO INDIETRO / HISTORY API - piano completo:
         C:\Users\lenovo\.claude\plans\camoscio-tasto-indietro-history-api.md
    B) RITOCCHI VISIVI (Fascia A pronta, ~11 interventi CSS) - rapporto:
         C:\Users\lenovo\.claude\plans\camoscio-audit-visivo-29a.md
       Due decisioni aspettano Denis (Passo&Fatica colori; contrasto
       bianco su arancione) - vedi il piano per il dettaglio.
    C) FILONE B: collezione "I 2000 dell'Appennino" - bloccato, LA
       LISTA UFFICIALE DELLE VETTE LA FORNISCE DENIS. Piano:
         C:\Users\lenovo\.claude\plans\camoscio-2000-appennino-collezione.md

>>> ALTRE APERTE, non urgenti:
    dominio email proprio (rimandato) - causa vera delle email che non
    arrivano (spam, es. Camilla).

>>> RESTA VALIDO:
  - Hook "Stop" di auto-save: commit locale (MAI push) a fine di
    ogni risposta se ci sono modifiche non salvate. Va rifuso in un
    commit vero prima di ogni push.
  - Traduzione IT/EN (punto 102, CHIUSO): resta ITALIANO di
    proposito tutto il testo scritto dagli utenti, i nomi propri di
    luogo, i nomi dei percorsi salvati, titolo/descrizione dei badge
    personali, le schede "i" dei badge, il testo delle notifiche (lo
    genera il server), il segnaposto nativo di <input type=date>/
    <input type=file>, e componiRapporto() dentro diagnostica-gps.
    Le chiavi EN dei blocchi nuovi si aggiungono mano a mano.
  - "Account eliminato" (punto A-3.4) SI traduce:
    common.accountEliminato -> "Deleted account".

===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — congelato dal 6/08/2026: da quella data il racconto
    disteso di ogni sessione vive solo in ../camoscio memoria/06-Cronologia-Sessioni.md,
    non qui. Utile solo per il dettaglio di lavoro PRIMA di quella data.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
MAI piu' (congelato); cose_da_fare.txt quando un punto si chiude o ne arriva uno nuovo.

>>> LEZIONI OPERATIVE (git dopo un'interruzione, moduli che si collegano tardi,
    componenti condivisi fra piu' pagine, .toObject vs .toJSON, $unset di campi
    required, select('username') che scavalca la pseudonimizzazione, recupero
    password vs grazia dell'eliminazione, npm audit fix vs overrides, serializzatore
    aggiunto a una rotta senza try/catch, test verde su una premessa falsa,
    select:false non vale su un campo assegnato in memoria, select:false su
    findByIdAndUpdate va reso condizionale su una rotta condivisa, un gate privacy
    condiviso va dichiarato fail-closed, un subagent puo' dichiarare falsamente di
    aver aggiornato la memoria - due volte confermato, due funzioni di render sullo
    stesso dato ma DOM diversi non danno errore se chiami quella sbagliata): tutte in
    ../camoscio memoria/07-Trappole-Tecniche.md — quella e' la sede dove si guarda
    prima di toccare codice a rischio.
===================================================================
