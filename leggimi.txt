===================================================================
CAMOSCIO — LEGGIMI: DA DOVE SI RIPARTE
Aggiornato: 2026-09-05 (TRENTUNESIMA sessione — chiusi i due follow-up
di sicurezza rimasti dalla 30a: BASSO (messaggio di errore di
validazione in POST /api/auth/register rispecchiava dati grezzi) e
MEDIO (GET /api/users caricava la foto di ogni utente in RAM). Poi un
giro `security-privacy` sul cumulativo: GO, con un ALTO vero
(regressione propria sull'export GDPR) + alcuni MEDIO/BASSO, tutti
chiusi nello stesso blocco.

--- COSA E' STATO CHIUSO ---
Denis: "sistemiamo tutti i punti sulla sicurezza" -> i due punti
lavorati in ordine (piu' piccolo prima), ognuno con prova nuova +
controprova sul codice precedente.

 BASSO   Il catch di POST /api/auth/register rimandava e.message di
         un ValidationError Mongoose (incorpora il VALORE che ha
         fallito, non solo il nome del campo) sia al client sia al
         log. I campi piu' esposti (emergencyContacts compreso) erano
         gia' pre-validati a mano con messaggi sicuri; il vettore
         dimostrato e' un birthDate non parsabile. Messaggio generico,
         log solo dei nomi dei campi.
 MEDIO   GET /api/users caricava profilePhoto (fino a ~800 KB) per
         OGNI utente a ogni refreshState - stesso muro RAM gia' chiuso
         su Squad.photo (28a). select:false + nuova GET
         /api/users/:id/photo, SOGGETTA a privacySetting (a differenza
         della foto squadra) - campiPrivacyVisibiliA estratta da
         serializeUserForViewer per non duplicare la regola. Tre file
         client aggiornati di conseguenza (app.js/profile.js/
         userprofile.js).

 GIRO security-privacy sul cumulativo: GO, con un ALTO vero (l'export
 GDPR aveva smesso di contenere la foto, regressione propria) + M-1/M-2
 (select incondizionato su PUT /:id riapriva la stessa asimmetria RAM
 su un'altra rotta, + il gemello lato client) + M-3 (gate privacy
 condiviso reso fail-closed) + B-1 (stesso principio del BASSO sul
 ramo 11000 accanto) + B-3/B-4 (prova e LEGGIMI-PROVE.txt completati).
 Tutti chiusi nello stesso blocco (commit a6fc5b3).

>>> STATO A FINE 31a SESSIONE:
      - camoscio: 3 commit avanti a origin/master (64a21d1, f6bb53a,
        a6fc5b3), NON PUSHATI - in attesa del via esplicito di Denis.
        Working tree pulito, niente auto-save in sospeso.
      - Prove verdi: prova-validazione-registrazione 10/10 (nuova),
        prova-foto-profilo-privacy 18/18 (nuova), prova-foto-profilo
        14/14, prova-registrazione-senza-email 7/7, prova-
        configurazione 9/9, prova-eliminazione-account 57/57, prova-
        squadre-consenso 71/71.
      - npm audit --omit=dev = 0.
      - L'estensione Chrome non era collegata: le 3 modifiche client
        (app.js/profile.js/userprofile.js) NON sono state verificate
        dal vivo in un browser, solo per lettura attenta del codice +
        il ragionamento del giro agente su ordine delle operazioni.

>>> APERTI (follow-up sicurezza, NUOVI da questo giro, non bloccano):
  - B-2: altri 3 punti (completions.js/hikes.js/lib/percorso.js)
    rimandano e.message grezzo al client - oggi senza perdita vera,
    ma servirebbe un marcatore tipo e.utente su ~10 punti di
    lib/percorso.js per chiuderlo senza degradare i messaggi gia'
    sicuri (il pattern di tracking.js non si trasporta 1:1). Lavoro a
    se'.
  - M-4: GET /api/users carica ancora emergencyContacts/email/
    birthDate/homeCity per intero per ogni utente (minimizzazione
    mancata, non un rischio OOM come la foto) + N+1 query
    areSquadmates per ogni utente SoloAmici.
  - B-5: la propria foto non si sincronizza piu' fra schede/
    dispositivi diversi finche' non si ricarica la pagina - non e'
    sicurezza (dice l'agente stesso), prezzo accettabile.
  - CONFORMITA' GDPR (professionista): invariata. Spiegata a Denis a
    voce in questa sessione - serve un consulente privacy/avvocato
    GDPR (non un DPO formale) per scrivere una vera Informativa
    Privacy + il testo di consenso prima di accettare un invito.
  - VERIFICA DAL VIVO di Denis su tutto l'invito squadra: invariata.

>>> ATTENZIONE, giro agente di oggi: l'agente `security-privacy` ha
    dichiarato di aver aggiornato 4 file nella memoria personale di
    Claude Code - VERIFICATO E RISULTATO FALSO, nessuno di quei file
    esiste, MEMORY.md non toccato. Non e' stato chiesto nel prompt e
    non c'e' un meccanismo reale per farlo da quell'agente. I rilievi
    TECNICI restano validi (verificati leggendo il codice vero), ma
    non fidarsi delle affermazioni collaterali di un agente sulle
    proprie azioni.

>>> SI LAVORA SU `master`, hotfix normali (commit vero + push -> Render
    ridistribuisce). L'hook di auto-save committa in locale (mai push):
    va rifuso in UN commit vero prima di ogni push
    (`git reset --soft origin/master`) - fatto anche in questa sessione
    (un auto-save rifuso nel commit f6bb53a).

>>> AZIONI MANUALI DI DENIS, aperte (invariate dalla 30a):
  1. ACCOUNT_SCRUB_SECRET su Render (stringa a scelta, DIVERSA da
     SAFETY_CRON_SECRET) + un job giornaliero su cron-job.org verso:
       https://camoscio.onrender.com/api/users/scrub-eliminati?chiave=<segreto>
     Senza, gli account eliminati restano "Account eliminato" PER
     SEMPRE (nessuno scrub al giorno 30). Il server lo urla nei log
     all'avvio in produzione. Fail-safe, ma da fare.
  2. Ricaricare il .gpx/.fit col ⬆ per le vecchie escursioni da
     pubblicare nel feed (l'originale non era stato conservato).
  3. Camilla: `camilla2026` e' TEMPORANEA. Deve cambiarla da
     Impostazioni > Cambia password. Accede con EMAIL
     camilla.orsini@gmail.com (il login e' per email, non username).

>>> APERTO, non bloccante (da prima della 28a, invariato):
  - PROVA MOBILE v2 sul telefono, SUL SITO LIVE: drawer/hamburger, i
    4 tab di hike-page, la nuova #settings, il layout stretto.
  - RESPONSIVE Dashboard <900px (regole @media viste solo in
    simulazione).
  - NOTA LEGALE: index.html fa accettare "Termini + Informativa
    privacy" senza documento dietro. Stesso professionista del punto
    GDPR sopra puo' chiuderli insieme.
  - M-5 (il vecchio, diverso dagli M della revisione): race del
    cancella-contatto emergenza con 2 schede aperte (serve una
    DELETE per contenuto/id, non per indice posizionale).
  - TTL sulle Notification che nominano un utente: un "Mario Rossi
    ha chiesto di..." resta per sempre nelle notifiche altrui col
    nome vero anche dopo l'eliminazione di Mario. Cambio schema
    app-wide + migrazione -> a se'.
  - nascondere il cestino sulle card "completate in gruppo".

>>> LAVORO FUTURO PREPARATO (progetti agli atti, NIENTE codice
    scritto - invariato dalla 30a, si riparte da questi se richiesto):
  A) TASTO INDIETRO / HISTORY API - piano completo:
       C:\Users\lenovo\.claude\plans\camoscio-tasto-indietro-history-api.md
  B) RITOCCHI VISIVI (Fascia A pronta, ~11 interventi CSS) - rapporto:
       C:\Users\lenovo\.claude\plans\camoscio-audit-visivo-29a.md
     Due decisioni aspettano Denis (Passo&Fatica colori; contrasto
     bianco su arancione) - vedi il piano per il dettaglio.

>>> ALTRE APERTE, non urgenti (dettaglio in vault 04-Da-Fare.md):
    livelli ORIGINALI del punto 63b + calcolo automatico del raggio
    (punto 42a) — Denis deve ancora pensarci; dominio email proprio
    (rimandato) — causa vera delle email che non arrivano (spam).
    FILONE B: collezione "I 2000 dell'Appennino" — bloccato, LA LISTA
    UFFICIALE DELLE VETTE LA FORNISCE DENIS. Piano:
      C:\Users\lenovo\.claude\plans\camoscio-2000-appennino-collezione.md

>>> RESTA VALIDO:
  - Hook "Stop" di auto-save: commit locale (MAI push) a fine di
    ogni risposta se ci sono modifiche non salvate. Va rifuso in un
    commit vero prima di ogni push.
  - Traduzione IT/EN (punto 102, CHIUSO): resta ITALIANO di
    proposito tutto il testo scritto dagli utenti, i nomi propri di
    luogo, i nomi dei percorsi salvati, titolo/descrizione dei badge
    personali, le schede "i" dei badge, il testo delle notifiche (lo
    genera il server), il segnaposto nativo di <input type=date>/
    <input type=file>, e componiRapporto() dentro diagnostica-gps.
    Le chiavi EN dei blocchi nuovi si aggiungono mano a mano.
  - "Account eliminato" (punto A-3.4) SI traduce:
    common.accountEliminato -> "Deleted account".

===================================================================

QUESTO FILE DICE SOLO UNA COSA: a che punto siamo adesso e da dove si ricomincia.
Si legge in cinque minuti, e va tenuto corto. Se cresce oltre un paio di pagine vuol
dire che ci e' finito dentro qualcosa che appartiene a un altro file.

DOVE STA IL RESTO, in ordine di lettura:

 1. ../camoscio memoria/  — IL VAULT, ed e' la fonte principale.
    Vincoli che valgono sempre, decisioni gia' prese e perche', trappole gia' pagate,
    cosa e' davvero aperto. Si legge PRIMA di questo file. Sta in una cartella
    sorella apposta: NON e' su GitHub PUBBLICO (ha un remoto privato tutto suo).
 2. cose_da_fare.txt      — le richieste di Denis CON LE SUE PAROLE, e il ragionamento
    di ogni punto chiuso. E' l'archivio: cresce, e va bene cosi'.
 3. cronologia.txt        — congelato dal 6/08/2026: da quella data il racconto
    disteso di ogni sessione vive solo in ../camoscio memoria/06-Cronologia-Sessioni.md,
    non qui. Utile solo per il dettaglio di lavoro PRIMA di quella data.
 4. prove/LEGGIMI-PROVE.txt — come si lanciano le prove automatiche.

REGOLA DI FINE SESSIONE: si aggiornano il vault e questo file SEMPRE; cronologia.txt
MAI piu' (congelato); cose_da_fare.txt quando un punto si chiude o ne arriva uno nuovo.

>>> LEZIONI OPERATIVE (git dopo un'interruzione, moduli che si collegano tardi,
    componenti condivisi fra piu' pagine, .toObject vs .toJSON, $unset di campi
    required, select('username') che scavalca la pseudonimizzazione, recupero
    password vs grazia dell'eliminazione, npm audit fix vs overrides, serializzatore
    aggiunto a una rotta senza try/catch, test verde su una premessa falsa,
    select:false non vale su un campo assegnato in memoria, select:false su
    findByIdAndUpdate va reso condizionale su una rotta condivisa, un gate privacy
    condiviso va dichiarato fail-closed): tutte in
    ../camoscio memoria/07-Trappole-Tecniche.md — quella e' la sede dove si guarda
    prima di toccare codice a rischio.
===================================================================
