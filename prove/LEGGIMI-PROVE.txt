PROVE AUTOMATICHE - recupero password (punto 7) e verifica email (punto 34)
===========================================================================

A COSA SERVONO
--------------
Sono gli script scritti il 2026-07-28 per verificare il recupero password e la verifica
dell'indirizzo email. Vivevano nella cartella temporanea, che prima o poi si svuota: sono
stati salvati qui perche' RIFARLI DA ZERO costerebbe piu' che rileggerli, e perche'
alcuni controllano cose che a occhio non si vedono - per esempio che tre risposte diverse
del server restino IDENTICHE fra loro.

NON sono nel package.json e non girano da soli: si lanciano a mano quando si tocca
qualcosa che riguarda accessi, password o email.


COME SI LANCIANO
----------------
Serve il server acceso su localhost:3000, con il suo output SCRITTO SU UN FILE - le prove
leggono di li' i link che sarebbero finiti nelle email (in locale, senza chiavi Mailjet,
il contenuto delle email viene stampato sul terminale invece di partire).

    node server.js > prove/server-prove.log 2>&1

poi, in un'altra finestra:

    node prove/prova-punto7.js                      53 controlli
    node prove/prova-verifica-email.js              34 controlli
    node prove/prova-guasto-invio.js                 6 controlli
    node prove/prova-configurazione.js               9 controlli  (non serve il server)
    node prove/prova-registrazione-senza-email.js    7 controlli  (ne avvia uno suo sulla 3100)

La prova del PASSO NON MISURATO (16/08/2026) non vuole il server acceso, ma legge il
database (in SOLA lettura: l'unica interrogazione vera e' su un id inesistente, quindi non
crea e non pulisce niente):

    node prove/prova-passo-non-misurato.js          28 controlli

    Sorveglia la regola "non misurato non e' 350": che un utente nuovo nasca SENZA
    averagePaceUp/averagePaceDown, che recalculatePersonalPace torni null a zero
    osservazioni e applyPersonalPace tolga il campo invece di scriverci un numero, e che
    calculateHikeTimes continui a dare un tempo previsto dichiarando pero' che non e' "il
    tuo passo". Controprova fatta il giorno stesso sul codice di prima (git worktree su
    HEAD): 7 controlli cadono, la prova sta guardando davvero quello che dice.

La prova del PUNTO 92 (il passo personale misurato anche dalle TRACCE, e misurato
invertendo la formula CAI invece di dividere dislivello per ore):

    node prove/prova-punto92.js                     84 controlli  (62 senza il server)

    Le prime sette sezioni non vogliono il server e girano su un userId SINTETICO - un
    ObjectId che non appartiene a nessuno - cosi' nessun account vero o demo viene sfiorato:
    recalculatePersonalPace interroga tracce e completamenti per userId, non ha bisogno che
    l'utente esista. La sezione 8 vuole il server acceso, carica un .gpx sintetico di 40
    minuti (241 punti, con dentro una sosta vera di 5 minuti) sull'ULTIMO account demo - i
    primi tre sono gia' impegnati da prova-punto77.js - e lo rimette esattamente com'era.
    Senza server la sezione 8 si salta da sola e lo dice.
    Sorveglia: che chi cammina come il CAI risulti misurato 400 m/h e non ~267; che il file
    cai-tempi.js dia gli stessi numeri letto dal server e letto come nel browser; che una
    traccia produca un'osservazione senza bisogno di un Completion; che i tempi scritti a
    mano non facciano piu' passo; che traccia collegata + completamento sullo stesso hikeId
    contino UNA volta sola; che la query sulle tracce non si porti mai dietro il campo
    "points" (vincolo hard sulla RAM: si intercetta cosa torna davvero dal database, non
    come e' scritta la query); e che movingTimeSec venga misurato sui punti COMPLETI, prima
    della semplificazione - la stessa traccia, dopo simplifyTrack, scende a 2 punti e non
    direbbe piu' niente.
    CONTROPROVA fatta il 17/08/2026 rimettendo il codice di prima un pezzo alla volta (git
    worktree + mutazioni): con la vecchia divisione dislivello/ore cadono 10 controlli, col
    ripiego su actualTimeHours 2, togliendo l'anti-doppio-conteggio 2, togliendo la
    proiezione della query 2. La prova guarda davvero quello che dice di guardare.
    ATTENZIONE, trappola gia' pagata: le sessioni stanno su MongoDB Atlas (connect-mongo),
    quindi la risposta di demo-login torna PRIMA che la sessione sia leggibile e la
    richiesta subito dopo si prende un 401 che non e' un difetto del sito. Si aspetta una
    condizione vera (GET /api/auth/me finche' non risponde), mai un tempo fisso.

La prova del PUNTO 93 (il ripiego "quote a mano" quando il dislivello di un progetto non si
puo' calcolare - fonte delle quote giu', o percorso oltre i 25 km):

    node prove/prova-punto93.js                     28 controlli  (ne avvia uno suo, sulla 3107)

    La sezione 1 chiama calcolaDaPercorso/risolviPercorso direttamente (lib/percorso.js), con
    un percorso di due punti a ~40 km in linea d'aria (deliberatamente troppo lungo, MAI "fonte
    giu'" - quel caso non e' pilotabile dal client per scelta di sicurezza, e dipendere
    dall'umore di Open-Meteo renderebbe la prova instabile). La sezione 2 passa dal server vero
    (POST e PUT /api/hikes): un tentativo senza quote a mano deve rispondere 422 e non creare
    niente, il rinvio con quoteManuali deve riuscire e portarsi dietro routeSource.
    dislivelloManuale:true, un valore assurdo (es. 99999 m) deve tornare quoteMancanti invece
    di essere accettato.
    Sorveglia anche: che la distanza sia SEMPRE calcolata e mai chiesta all'utente; che il
    motivo (fonte/troppo lungo) torni nel corpo del 422; che la modifica (PUT) si comporti
    esattamente come la creazione (POST) - due punti diversi di routes/hikes.js, stessa
    risolviPercorso.
    CONTROPROVA fatta il 18/08/2026 (git stash di lib/percorso.js): col codice di prima la
    prova si rompe subito, con l'errore vecchio non gestito - la prova guarda davvero quello
    che dice di guardare.
    Verificato ANCHE visivamente nel browser (non automatizzato in questa prova): account demo,
    riquadro comparso col messaggio giusto su un progetto apposta troppo lungo, pubblicazione
    riuscita con la nota "... - dislivello indicato da chi organizza" sulla card.

La prova dell'INVITO A UNA SQUADRA (il bug segnalato da Denis: "Invita a Gita" sceglieva
l'escursione da sola e ha invitato una squadra a un'escursione GIA' COMPLETATA) e
dell'APPROVAZIONE MANUALE che si poteva scavalcare:

    node prove/prova-invito-squadra.js              59 controlli  (ne avvia uno suo, sulla 3109)

    DUE SEZIONI, perche' i due difetti vivono in due punti diversi.
    La sezione A non vuole ne' server ne' database: carica public/js/social.js dentro un
    contesto finto (vm) e prova le due funzioni che DECIDONO - mieEscursioniAperte (quali
    escursioni sono un bersaglio legittimo) e confermaInvitoSquadra (su quale campo si
    scrive). Il server non potrebbe provarlo: la scelta sbagliata avveniva PRIMA della
    richiesta HTTP. Trucco che tiene la prova stabile: document.getElementById risponde solo
    per i tre id del riquadro di invito e null per tutti gli altri, cosi'
    renderHikesList/renderMyHikes/renderSquadsList escono subito da sole (lo fanno gia'
    quando il loro contenitore non c'e') invece di pretendere mezza pagina in un finto DOM.
    La sezione B parla col server vero, con quattro account demo e cookie veri: chi puo'
    toccare la lista partecipanti e quando. Sorveglia che un non creatore non possa piu'
    iscrivere altri (ne' se stesso, ne' approvarsi da solo) su un'escursione ad approvazione
    manuale (403), che possa invece PROPORLI (200, in pendingApproval), che senza
    approvazione manuale l'invito diretto continui a funzionare come sempre, che la chiusura
    di gruppo azzeri le richieste in sospeso, e che su un'escursione conclusa la lista
    partecipanti non si tocchi piu' (409, nemmeno dal creatore).
    CONTROPROVA fatta il 18/08/2026 (git worktree su HEAD + NODE_PATH sui node_modules del
    progetto): col codice di prima cadono 18 controlli della sezione B e la sezione A non
    parte nemmeno (le due funzioni non esistevano). La prova guarda davvero quello che dice.
    UN DIFETTO TROVATO DA QUESTA PROVA, lo stesso giorno e poi corretto: su un'escursione
    conclusa si potevano ancora PROPORRE nomi nuovi (pendingApproval non era in
    EDIT_LOCKED_FIELDS, che bloccava solo participants) - una richiesta che nessuno poteva
    piu' ne' accettare ne' rifiutare. Non era preesistente: la facolta' di proporre era nata
    quel giorno stesso. I tre controlli B11 lo presidiano, e pretendono 409 e non un 403
    qualunque: deve fermarsi perche' l'escursione e' CHIUSA, non per un caso dei permessi.

La prova del PUNTO 111 ("risolvi" e scadenza delle segnalazioni sentiero passano da
Denis - la richiesta di un utente diventa una RICHIESTA che il moderatore conferma o
tiene ancora; alla scadenza dei 90 giorni parte un avviso, non una cancellazione):

    node prove/prova-punto111.js                    82 controlli  (ne avvia uno suo, sulla 3111)

    DODICI SEZIONI. Due account demo: A resta un utente normale (in sez. 10 elevato a
    moderatore per un attimo e poi revocato), B viene fatto moderatore E dato
    receivesReportAlerts DIRETTAMENTE sul database, tutto revocato nel finally.
    1-7: scadenza esplicita alla creazione (expiresAt = createdAt + 90gg); "risolvi" di
    un utente = POST /:id/resolve-request che NON cancella e NON cambia lo status;
    GET /api/reports non esporta resolutionRequestedBy ne' expiryNotifiedAt; guardia
    anti-doppione atomica (una sola notifica); DELETE /:id/resolve ora gated MODERATORE;
    "Tieni ancora" (DELETE /:id/resolve-request); "Conferma la risoluzione" cancella per
    intero, foto compresa.
    8: PATCH /:id/renew (moderatore) sposta expiresAt a 90gg DA ADESSO (non vecchio +90),
    azzera expiryNotifiedAt, 403 per i non moderatori, 404 su id inesistente.
    9: controllo scadenze PIGRO agganciato a GET /api/notifications - il primo fetch di
    CHIUNQUE fa notificare i destinatari e "timbra" expiryNotifiedAt; un secondo fetch non
    rinotifica; dopo un renew la scadenza riavvisa.
    10: l'avviso va a chi ha receivesReportAlerts, NON a "tutti i moderatori"; se nessuno
    ce l'ha, ripiego rumoroso su tutti i moderatori.
    11: GET /api/reports/moderation - tre liste DISGIUNTE (scadute / risoluzioniRichieste /
    daVerificare) + totale calcolato dal server, priorita' scadute > risoluzioni > da
    verificare; resolutionRequestedBy visibile a chi modera (a differenza di GET /).
    12: indici di 'reports' - il TTL su createdAt (punti 45/109) e' SPARITO, resta il
    paracadute TTL su expiresAt a 365 giorni.
    Il finally cancella report per _id E notifiche per relatedReportId dei test (quegli
    ObjectId nascono nel run - nessun dato vero puo' combaciare; non filtrato per userId
    perche' la sez. 10b, col ripiego, crea avvisi anche per A e per altri moderatori veri).
    I passi 8 (map.js) e 9 (pagina Moderazione) sono lato client, verificati dal vivo con
    l'estensione Chrome (demo Marco moderatore, 4 report finti PROVA-P9-*, IT+EN, azione
    Rinnova end-to-end, console pulita), non da questa prova.
    CONTROPROVA fatta il 29/08/2026 (git worktree su f006edf, "Punto 108" = HEAD prima del
    111, + NODE_PATH sui node_modules del progetto, .env e prova-punto111.js copiati nel
    worktree): 14 controlli caduti nelle sez. 1-6, poi la prova si FERMA IN ERRORE a sez. 6
    perche' in sez. 5 il report e' gia' stato cancellato da un utente normale (vecchio
    DELETE /:id/resolve gated solo requireAuth - proprio il buco che il 111 chiude); le
    sez. 7-12 non vengono nemmeno raggiunte. La prova guarda davvero quello che dice.
    ATTENZIONE, pagata il 29/08/2026: il server pre-111 del worktree gira con autoIndex e
    la vecchia riga reportSchema.index({createdAt:1},{expireAfterSeconds:...}) -> avviarlo
    contro il DB Atlas condiviso RESUSCITA l'indice TTL createdAt_1 (90gg) che il punto 111
    aveva droppato. Dopo QUALSIASI controprova che tocca gli indici e avvia il server
    vecchio: ridroppare a mano (c.dropIndex('createdAt_1')) o rilanciare
    scripts/scadenza-segnalazioni-esplicita.js --scrivi. La sez. 12 di questa prova (o la
    sez. 13 di prova-punto45) lo becca subito.
    TRAPPOLA gia' pagata (29/08/2026): una verifica dal vivo che lascia sul database report
    SCADUTI fa scattare il controllo pigro al primo GET /api/notifications dell'app (che
    refreshState chiama da sola) -> nascono avvisi di scadenza per i moderatori. Il teardown
    della verifica dal vivo deve cancellare ANCHE le notifiche (filtrate sul testo finto),
    non solo i report: 4 notifiche orfane trovate cosi' col riconfronto dei conteggi.

La prova del PUNTO 113 (parte social: Segui, Feed, pagina uscita, "mi piace", "crea
percorso" da una traccia altrui, escursione con i numeri presi da un percorso salvato):

    node prove/prova-punto113.js                    103 controlli  (ne avvia uno suo, sulla 3113)

    OTTO SEZIONI. Tre account demo (A, B, C), niente permessi speciali da toccare.
    1: /api/follow - POST su se stessi 400, su id inesistente 404, follow idempotente (un
    solo documento, no doppioni), GET following/followers/counts, DELETE idempotente.
    2: POST /sessions/:id/publish|unpublish - solo l'autore (403 per un altro), $set+$unset
    (publishedAt e caption ASSENTI, non null, dopo unpublish), ri-pubblicare senza testo
    toglie la vecchia caption.
    3: GET /api/feed - solo le uscite pubblicate di chi segui, le TUE NON ci sono, MAI
    "points" ne' "offTrailBuffer" nel corpo, ordine per publishedAt desc, cursore ?before=,
    likeCount/likedByMe in ogni item, e chi non segue A ne' B non vede NESSUNA delle uscite
    di prova (NB 31/08/2026: non piu' "feed vuoto" - i 4 account demo ora seguono Denis
    davvero sul DB condiviso, da una sua verifica del 30/08; si asserisce sui soli id di
    prova, come gia' fatto per likeCount).
    4: guardia autore-o-follower su GET /sessions/:id/meta e /points - autore 200, follower
    200, estraneo su un'uscita PUBBLICATA 403, non-autore su un'uscita NON pubblicata 404
    (non 403: non si rivela che esiste); /points e' l'unica rotta con la geometria,
    ?scopo=mini semplifica SOLO la risposta (dato d'archivio intatto); /meta niente points.
    5: POST/DELETE /sessions/:id/like - toggle contatore +1/-1, POST idempotente (un solo
    Like per coppia), notifica all'autore SOLO al primo like di quella persona (con
    relatedSessionId), MAI per un like a se stessi, MAI cancellata da un unlike; estraneo
    403/404; unpublish non azzera i like; cascata Like.deleteMany nel DELETE della sessione.
    6: POST /api/routing/saved-routes - stessa guardia autore-o-follower, forma del
    documento (punti solo [lng,lat], origineUserId/origineUsername, distanza/dislivello/
    quotaMaxM copiati, NIENTE sessionId), GET/DELETE dei propri, tetto MAX_PERCORSI_SALVATI
    (51esimo -> 429), un altro utente non cancella il tuo (404). INDIPENDENZA: cancellata
    l'uscita sorgente, il SavedRoute resta e si legge ancora.
    7: Hike routeSource.kind:'saved' (POST e PUT /api/hikes) - i numeri atterrano dal
    SavedRoute, routeSource resta solo un'etichetta (no savedRouteId, no dislivelloManuale),
    e RESTANO se il SavedRoute viene poi cancellato; savedRouteId inesistente -> 400; una
    traccia senza quote -> 422 richiedeQuote -> retry con quoteManuali -> dislivelloManuale
    true (stesso ripiego del punto 93, motivo 'fonte' per non toccare risolviPercorso).
    8: indici - Follow {followerId,followingId} UNICO + {followingId}; Like {userId,sessionId}
    UNICO + {sessionId}; SavedRoute {userId}; ActiveHikeSession {userId,publishedAt:-1}
    PARZIALE su publishedAt.
    I passi 2-3 (tasto "Segui" nel profilo, liste in Tribu' & Squadre) e la parte UI del 9
    ("percorso da seguire" = sola linea di riferimento sulla mappa, NESSUN avviso di
    fuori-percorso - vincolo 7) sono lato client, verificati dal vivo con l'estensione Chrome
    (demo Marco/Luca/Sofia, IT+EN, console pulita), non da questa prova.
    Il finally cancella tutto per marca (MARCA = "PROVA-113-<timestamp>", un timestamp: nessun
    dato vero combacia) E per gli _id nati nel run. ATTENZIONE, gia' pagata: POST /api/hikes
    fa scattare la notifica "nuova escursione di squadra" ai membri delle squadre di A
    (account demo collegato a una squadra vera) - quelle notifiche non hanno relatedSessionId,
    si prendono per il testo (che contiene il titolo della hike, con MARCA). Senza quella riga
    di pulizia restavano 12 notifiche orfane, beccate dal riconfronto dei conteggi.
    CONTROPROVA fatta il 30/08/2026 (git worktree su 6271cf8, "Punto 111 deploy" = HEAD prima
    dei passi 1-6 del 113; + NODE_PATH sui node_modules del progetto, .env e prova-punto113.js
    copiati nel worktree): 18 controlli caduti nelle sez. 1-2, poi la prova si FERMA IN ERRORE
    a sez. 3 (GET /api/feed non esiste -> 404 -> corpo null -> .items su null); le sez. 4-8
    non vengono nemmeno raggiunte. La prova guarda davvero quello che dice.
    NOTA sull'autoIndex (diverso dal caso del 111): il punto 113 solo AGGIUNGE indici, non ne
    droppa - mongoose autoIndex crea gli indici mancanti ma non tocca quelli non dichiarati,
    quindi avviare il server pre-113 contro Atlas NON cancella i tre indici nuovi. Verificato
    subito dopo la controprova rilanciando la prova vera: sez. 8 verde (6/6).

La prova del FIX 30/08/2026 (punto 113): il .gpx aggiunto a un'escursione GIA' completata
(tasto ⬆) salva anche la TRACCIA come sessione collegata, cosi' l'escursione diventa
pubblicabile nel feed. Segnalato da Denis: "Alba Corno Grande" chiedeva un .gpx gia'
caricato.

    node prove/prova-punto113-gpx.js                43 controlli  (ne avvia uno suo, sulla 3115)

    SETTE SEZIONI, un account demo. Un hike completato SENZA sessione collegata (come "Alba
    Corno Grande"), poi POST /api/completions/:id/gpx: 1) crea 1 ActiveHikeSession con
    hikeId != null, importedFrom:'gpx', status ended, points, movingTimeSec, NIENTE
    openSession, non pubblicata; 2) quella sessione e' pubblicabile (POST /sessions/:id/
    publish -> 200); 3) il ri-upload aggiorna LO STESSO documento (nessun doppione),
    conserva publishedAt/caption, aggiorna importedName; 4) una sessione registrata DAL VIVO
    (importedFrom assente) NON viene toccata dal ⬆; 5) il tetto mensile di /import-gpx conta
    solo hikeId:null, quindi la sessione collegata resta fuori. GPX sintetico con zigzag
    (sin/cos) cosi' abbastanza punti sopravvivono a simplifyTrack; sosta di 15 min in mezzo.
    CONTROPROVA fatta il 30/08/2026 (git worktree su 37650c8 = HEAD prima del fix, + NODE_PATH
    sui node_modules del progetto, .env e prova copiati): 8 controlli caduti in sez. 1 (il
    vecchio /:id/gpx non crea nessuna sessione), poi la prova si FERMA IN ERRORE a sez. 2
    (nessuna sessione da pubblicare -> S._id su undefined). La prova guarda davvero quello
    che dice.
    SEZ. 6 (punto 115): un .gpx SENZA <name> (imita un .fit) collegato al ⬆ -> importedName
    = TITOLO dell'escursione (non una data nuda nel feed); PATCH /name rinomina; un
    ri-upload senza <name> NON tocca il nome scelto a mano; un ri-upload CON <name> lo
    sovrascrive (l'utente sta ricaricando quella traccia).
    SEZ. 7 (coda punto 113, 2026-08-31): il ⬆ assegna anche i BADGE DI VETTA, come "Carica
    un file .gpx" - assegnaTimbriDallaTraccia e' stata spostata da routes/tracking.js a
    lib/geofenceTimbri.js (dove sono gia' puntiTimbrabili/distanzaMinimaDaTraccia) e ora la
    chiama anche routes/completions.js. La prova crea un hike con una vetta di prova
    (stampId marcato, nessun account vero ce l'ha) sul primo punto del gpx sintetico:
    dopo il ⬆ la risposta porta il badge nuovo nel campo "badge", il timbro e' sul DB con
    la DATA DELL'ESCURSIONE, e un ri-upload non fa doppioni. La controprova (codice prima
    del 2026-08-31): il ⬆ non mette `badge` nella risposta. prova-punto108 (25/25 dopo
    l'hotfix Ju Busciu del 31/08 - vedi sotto) copre l'equivalenza matematica del
    refactor: la scansione non e' cambiata, e' solo cambiata di casa.

    prova-punto108.js - AGGIORNATA il 31/08/2026 (hotfix `efd1290` su master, campo
    `sogliaM` per-punto, Ju Busciu a 10 m): sez. 1 ha un controllo in piu' (25 totali) -
    con `sogliaM:10` un punto a 20-140 m NON tocca piu', col default a 150 si'; ha la sua
    controprova interna (rosso se sogliaPunto ignora il campo). `tracciaToccaPunto` ora
    prende l'OGGETTO del punto, non lat/lng sciolte. Nuovo hook env `PROVA_BASE_URL` per
    puntare la sez. 3 a un'altra porta invece della :3000 fissa.
    Come si lancia: `node server.js > prove/server-prove.log 2>&1` (altra finestra) poi
    `node prove/prova-punto108.js`. Senza server: solo sez. 1-2. Rilanciata il 31/08 con
    server master fresco: 25/25.
    NOTA non coperta da questa prova (e' il FILE, non il codice): se il .gpx ha buchi
    temporali/spaziali (registrazione "smart"/semplificata), movimentoSecAttendibile
    (punto 79) rifiuta lo split cammino/pause con "buchi non interpretabili" e si salva
    solo il totale. Un export GPX a risoluzione piena, o un FIT registrato a 1 Hz,
    risolve - ma un FIT esportato "smart" ha lo stesso problema (vedi prova-fit sotto).

La prova del PUNTO 114 (import .fit, formato nativo Garmin) NON vuole ne' server ne' rete:
i file .fit di prova sono sintetici, costruiti con il FitEncoder di fit-file-parser.

    node prove/prova-fit.js                         33 controlli  (non serve il server)

    Prova lib/fit.js -> parseFit(). Controlla che: 1) restituisca le STESSE chiavi di
    parseGpx (punti/nome/inizio/fine/durataIgnota/tipo/avvisi), cosi' il codice a valle
    non cambia; 2) statisticheTraccia e movimentoSecAttendibile (lib/gpx.js) girino sui
    suoi punti senza modifiche; 3) salti i record senza posizione (GPS in aggancio) senza
    contarli errore; 4) erediti la quota mancante, preferisca enhanced_altitude, tenga
    solo orari crescenti; 5) rifiuti con ErroreFit (flag .utente, messaggio coi numeri)
    i file rotti/vuoti/senza orari/con una sola posizione. Verifica dal vivo end-to-end
    fatta il 30/08/2026 col file vero di Denis (fitprova.fit, Corno Grande, non nel repo):
    448 punti, 9,07 km, +867 m, regione 41/41 dentro; movimentoSecAttendibile = null
    perche' quel file e' registrato "smart" (~58 s fra i punti) - come atteso, non e' un
    bug del parser.

La prova del PUNTO 115 (rinominare un'uscita) avvia un server suo sulla 3115:

    node prove/prova-punto115.js                   28 controlli  (ne avvia uno suo, sulla 3115)

    NOVE SEZIONI, due account demo, su una ActiveHikeSession senza nome (come un .fit appena
    importato). PATCH /api/tracking/sessions/:id/name: 1) il proprietario mette un nome
    (trim, risposta { importedName }); 2) nome vuoto -> $unset, torna alla data; 3) tetto
    120 caratteri; 4) un altro utente -> 403, nome invariato; 5) id non valido -> 400,
    inesistente -> 404. POST /publish con { name, caption }: 6) pubblica E nomina;
    7) name:'' toglie il nome ma resta pubblicata; 8) campo name assente -> non tocca il
    nome. 9) /unpublish toglie publishedAt/caption ma NON il nome. Pulizia: cancella le
    sessioni create (filtrate per _id e per importedName ^PROVA-115).
    NB (30/08/2026): dopo la verifica dal vivo del punto 114, in prova-punto113.js
    l'asserzione "ogni item del feed ha likeCount 0" e' stata ristretta ai soli item creati
    dalla prova - il feed dell'account demo ora contiene anche l'uscita .fit vera pubblicata
    da Denis, con un "mi piace" reale di Marco Alpinista. 103/103 invariato.

La prova del PUNTO 45 (moderazione segnalazioni + foto) e' stata aggiornata per il 111:
GET /pending -> GET /moderation (oggetto con tre liste + totale), DELETE /:id/resolve
gated moderatore, TTL createdAt -> expiresAt. 46 controlli, invariati nel numero.

    node prove/prova-punto45.js                     46 controlli  (ne avvia uno suo, sulla 3103)

La prova del PUNTO 35 vuole anche Puppeteer (vedi sotto), ma non legge il log:

    PUPPETEER_PATH=... node prove/prova-punto35.js  16 controlli

La prova del PUNTO 33 (dislivello dei percorsi progettati) non vuole ne' il server ne'
Puppeteer, ma INTERROGA UNA FONTE ESTERNA (Open-Meteo):

    node prove/prova-punto33.js                     33 controlli

La prova del DISLIVELLO DAL VIVO (punto 36) gira senza server per la maggior parte, ma la
sua sezione 6 - quella che conta - vuole il server acceso, perche' manda i punti alla rotta
vera invece di provare una copia della regola:

    node prove/prova-dislivello-vivo.js             28 controlli

Le due prove del PUNTO 38 (il tasto "Torna all'inizio", cioe' chiudere l'anello). Vanno
lanciate INSIEME: si dividono il lavoro, e nessuna delle due da sola basta.

    node prove/prova-punto38.js                     25 controlli  (serve il server)
    PUPPETEER_PATH=... node prove/prova-browser-punto38.js  25 controlli  (serve il server)

    La prima sorveglia i NUMERI: che il percorso torni DAVVERO sul punto di partenza, che
    la distanza raddoppi, che salita e discesa si pareggino, che la bozza salvata conservi
    i chilometri dell'anello. La seconda sorveglia l'INTERFACCIA, dove vive quasi tutto il
    punto 38 - e la controprova lo ha dimostrato: i primi controlli della prova del server
    passano anche col codice vecchio, perche' provano il motore e non il tasto.
    La prova del browser salva due schermate (p38-anello-1440.png e p38-anello-390.png):
    e' li' che si e' visto il difetto piu' serio della giornata, che nessun controllo
    automatico stava guardando.

Se il server non c'e', la sezione 6 si salta da sola e lo dice. Crea UNA sessione di prova
su un account demo e la cancella in un finally, filtrata per userId E per id.

ATTENZIONE, prova-punto33 e' l'unica prova che puo' dire SALTATO. Open-Meteo e' gratuito e ha un tetto
(10.000 chiamate al giorno, 5.000 all'ora, ~600 PUNTI al minuto). Se in quel momento sta
frenando, i controlli che dipendono da lei non provano niente: farli fallire direbbe "il
sito e' rotto" quando il sito e' a posto. Quindi si segnano SALTATO, e la prova lo scrive
a lettere chiare in fondo.
SALTATO NON VUOL DIRE PASSATO. Si rilancia piu' tardi.
Nota: la prova consuma piu' del sito - controlla DUE tracce vere di fila, cioe' ~1000
punti, mentre il sito non ne chiede mai piu' di 500 per un percorso.

Se il log sta altrove: CAMOSCIO_LOG=percorso/del/file.log node prove/...


LE DUE PROVE NEL BROWSER
------------------------
Servono PUPPETEER, che per scelta del progetto NON va installato in package.json (vedi
cronologia.txt): si installa in una cartella temporanea e gli si dice dov'e'.

    npm install puppeteer          (in una cartella qualsiasi fuori dal progetto)
    npx puppeteer browsers install chrome

    PUPPETEER_PATH=/percorso/della/cartella/node_modules/puppeteer \
      node prove/prova-browser-punto7.js             56 controlli
    PUPPETEER_PATH=... node prove/prova-browser-verifica.js   38 controlli

Girano a 1440 e a 390 px e SALVANO LE SCHERMATE qui dentro (o in CAMOSCIO_SCATTI).
GUARDARLE FA PARTE DELLA PROVA: i difetti di impaginazione non li trova nessun controllo
automatico - i due del punto 7 sono usciti solo cosi'.


REGOLE RISPETTATE DA TUTTE, da non perdere se se ne scrivono altre
------------------------------------------------------------------
- I CONTEGGI DEL DATABASE SI LEGGONO ALL'INIZIO E SI RICONFRONTANO ALLA FINE, invece di
  scriverli a mano: un controllo che dipende da quanti dati ha l'utente non sta provando
  il codice. E' cosi' che si sono scoperti 3 documenti orfani lasciati in giro.
- OGNI CANCELLAZIONE PORTA CON SE' L'userId DELL'ACCOUNT DI PROVA, sempre, anche quando
  "tanto quel valore ce l'ho solo io". Il 2026-07-27 una pulizia non filtrata ha
  cancellato un timbro VERO dell'utente.
- I DATI FINTI NON DEVONO SOMIGLIARE A QUELLI VERI: indirizzi @esempio-di-prova.invalid e
  nomi che dicono "Prova". E' l'unica difesa che funziona anche quando la prima regola
  viene dimenticata.
- LA PULIZIA STA IN UN finally, cosi' vale anche se qualcosa va storto a meta'.
- L'ERRORE SI STAMPA IN UN catch PRIMA del finally: un process.exit dentro il finally
  ucciderebbe il .catch e la prova morirebbe senza dire niente.
- SI ASPETTA UNA CONDIZIONE VERA, mai un tempo fisso.
- CONTESTI ISOLATI nel browser (browser.createBrowserContext), non browser.newPage(): le
  pagine dello stesso browser condividono i cookie, e la seconda prova risulterebbe gia'
  collegata dalla prima.


QUANDO UNA PROVA FALLISCE, SOSPETTARE PRIMA LA PROVA
-----------------------------------------------------
Il 2026-07-28 sono fallite quattro volte, e TUTTE E QUATTRO erano colpa della prova o
segnalavano qualcosa di diverso da un difetto del sito:
 - cookie condivisi fra le pagine (vedi sopra);
 - il freno anti-abuso che funzionava, esaurito da tre giri di prove di fila;
 - una prova che usciva dal logout senza ricaricare la pagina, cioe' creava uno stato che
   usando il sito non esiste: cosi' ha rivelato un difetto PREESISTENTE e scollegato
   (punto 35 di cose_da_fare.txt);
 - una prova che si aspettava il messaggio ottimista mentre il sito - giustamente - stava
   dicendo che l'invio non era configurato.
Solo la terza ha trovato qualcosa di vero, e non era quello che stava cercando.

Il 2026-07-28, seconda sessione, e' successo altre due volte:
 - prova-browser-punto7.js si fermava a 20 controlli su 56. Non era il sito: era il PUNTO
   34, fatto lo stesso giorno ma DOPO che questa prova era scritta, ad aver cambiato di
   proposito il comportamento (con un indirizzo non confermato parte l'email di conferma,
   non quella di reimpostazione). La prova aspettava un link che giustamente non arrivava
   piu' e moriva su page.goto(undefined), portandosi via anche tutto il giro a 390 px.
   REGOLA CHE NE DERIVA: quando si cambia il comportamento del sito, le prove scritte
   PRIMA di quel cambio vanno rilanciate nella stessa sessione. Una prova che fallisce per
   il motivo sbagliato e' peggio di una che manca, perche' sembra copertura;
 - la prova nuova del punto 35 fotografava lo stato appena hikes risultava popolato, ma
   refreshState carica timbri, completamenti e notifiche DOPO: la fotografia prendeva
   stamps ancora vuoto. Stessa trappola del punto 32 - si aspetta un effetto vero, non un
   momento plausibile.

E UNA REGOLA NUOVA, dalla prova del punto 35:
 - UNA PROVA CHE NON FALLISCE MAI NON STA PROVANDO NIENTE. Dopo averla scritta, si rimette
   per un attimo il codice VECCHIO e si verifica che fallisca davvero. Sulla prova del
   punto 35: 6 controlli su 16 cadono col codice di prima. Senza questo passaggio non si
   sa se sta guardando quello che dice di guardare.


RESIDUI VECCHI SUL DATABASE — NESSUNO NOTO AL MOMENTO
------------------------------------------------------
 - Le 30 notifiche orfane lasciate da 15 lanci di prova-punto77.js prima che il suo
   cleanup imparasse a toglierle (corretto il 18/08/2026) sono state ripulite lo stesso
   giorno (filtrate sul testo finto "PROVA-77-<timestamp>", verificato 0 rimaste). I
   lanci nuovi della prova non ne lasciano piu'.
